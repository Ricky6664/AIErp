# CLAUDE.md — ERP AI 全自动开发执行总指令

> **版本**：V2.0
> **创建日期**：2026-05-26
> **最后修订**：2026-05-26
> **定位**：Claude Code 命令行全自动开发执行的唯一行为准则
> **适用范围**：所有 Claude Code 会话（主Agent、子Agent、并行Agent）
> **强制级别**：本文档所有规则为强制执行，不得跳过、省略或变通

---

## 1. 项目基础信息

| 属性 | 值 |
|------|-----|
| 项目名称 | ERP AI 智能管理系统 |
| 任务文档根目录 | `erp_ai_tasks_docs/` |
| 全局规范目录 | `erp_ai_tasks_docs/`（根级 `全局规范-*.md`，共 37 个文件） |
| 优先级 | P0 核心必做（14模块）→ P1 重要（15模块）→ P2 常规（17模块） |
| 模块总数 | 46 个一级模块 |
| 叶子任务总数 | 4,716 个（P0: 2,147 / P1: 1,464 / P2: 1,105） |
| 开发起点 | P0-001-001-001-001-001（执行Spring Initializr生成项目） |

---

## 2. 文档体系与职责边界

### 2.1 管控文档（项目根目录，与 `erp_ai_tasks_docs/` 同级）

| 文档 | 职责 | 禁止包含 |
|------|------|---------|
| `CLAUDE.md`（本文件） | AI执行核心规则、会话启动协议、任务调度、自检门禁、Git管理 | 业务需求、代码、字段定义、页面逻辑 |
| `config.ini` | 运行参数配置（路径、停止开关、窗口数、补充策略、锁超时） | 业务需求、技术方案 |
| `tasks_catalog.md` | 全量叶子任务清单（4716 条），带位置编号和取任务游标 | 依赖关系、业务需求 |
| `tasks_dependency.md` | 预计算依赖关系与并行分组（46 模块依赖 + 并行分组 + 文件冲突矩阵） | 叶子任务细节 |
| `tasks_active.md` | 执行状态 + 活跃叶子任务 + fetch_cursor（工人从此认领），含当前窗口/模块/任务指针 | 模块级依赖关系 |
| `tasks_completed.md` | 已完成任务归档（编号 + 时间 + SHA），只写不读 | 业务需求、实现细节、代码内容 |

**子文档**（`ai-docs/` 目录，本文件的延伸，按需加载）：

| 子文档 | 职责 | 加载时机 |
|--------|------|---------|
| `ai-docs/auto-continuous-dev.md` | 工人循环详细规则（两级查找、补充逻辑、停止开关、进度隔离） | 任务完成后需要判断下一步时 |
| `ai-docs/parallel-rules.md` | 并行开发规则（8条严格规则、锁机制、进度隔离） | 多工人并行执行时 |

### 2.2 业务文档（`erp_ai_tasks_docs/` 内，只读引用，禁止修改）

| 层级 | 位置 | 职责 |
|------|------|------|
| AI执行手册 | `全局规范-AI开发执行手册.md` | 任务解读流程、规范加载策略、执行顺序、质量红线 |
| 任务文档规范 | `全局规范-末端任务文档编写规范.md` | 任务文档模板与质量标准 |
| 全局规范 | `全局规范-*.md`（37个） | 技术栈、代码规范、数据库、安全、国际化等 |
| 模块索引 | `P*/P*-XXX/模块索引.md` | 模块内子任务导航与叶子任务统计 |
| 层级规范 | `P*/P*-XXX/层级规范-P*-XXX.md` | 模块级架构约束、数据模型、依赖关系 |
| 模块开发指南 | `P*/P*-XXX/P*-XXX-*-模块开发指南.md` | 模块级详细开发指导 |
| 末端任务文档 | `P*/.../P*-XXX-...-XXX.md` | 具体任务的完整规格（Section 1-8） |

### 2.3 文档职责隔离

- **管控文档**只管：怎么做 → 做完没 → 下一个是什么
- **业务文档**只管：做什么 → 怎么做 → 做成什么样
- 两者严格分离，禁止在管控文档中写入业务内容

### 2.4 文档优先级（高 → 低，冲突时以高为准）

```
1. 用户明确指令（最高优先）
2. 本文件 claude.md（AI 执行规则）
3. tasks_active.md（当前开发位置）
4. 末端任务文档（最具体的任务规格 —— 实现细节以此为准）
5. 层级规范（模块级约束）
6. 全局规范-AI开发执行手册.md（执行流程）
7. 全局规范-*.md（通用技术规范 —— 通用标准以此为准）
```

> **关键原则**：当末端任务文档与全局规范在实现细节上冲突时，以**末端任务文档**为准（它更具体）。当涉及通用编码标准、命名规范时，以**全局规范**为准（它更权威）。

---

## 3. 新会话启动协议（强制执行）

**任何新会话的第一动作，必须严格按以下步骤执行：**

### Step 1：读取进度

```
读取 tasks_active.md
获取 → current_pointer（当前执行指针）
获取 → current_task_status（当前任务状态）
获取 → last_completed（最近完成的任务）
获取 → module_progress（模块级统计）
```

### Step 2：判断接续逻辑

```
IF current_task_status == "🔄 执行中":
    → 任务正在执行，可能中断了，继续执行（跳转 Step 3）
    
ELSE IF current_task_status == "⬜ 待执行":
    → 任务尚未开始，设为执行中并启动（跳转 Step 3）
    
ELSE IF current_pointer 为空:
    → 项目全部完成，通知用户，等待指令
    
ELSE IF 当前任务标记为 "❌ 阻塞":
    → 检查阻塞原因是否已解除
    → 已解除：恢复执行（跳转 Step 3）
    → 未解除：跳到下一个可执行任务
```

### Step 3：定位任务文档

按以下算法从任务编号定位到具体文件：

```
输入：任务编号，如 P0-001-001-001-001-001

Step A - 确定优先级根目录：
  P0-XXX → erp_ai_tasks_docs/P0 - 核心必做/
  P1-XXX → erp_ai_tasks_docs/P1 - 重要/
  P2-XXX → erp_ai_tasks_docs/P2 - 常规/

Step B - 确定一级模块目录：
  P0-001 → 在该根目录下查找以 "P0-001" 开头的目录
  → P0-001 - 后端项目框架搭建/

Step C - 逐级深入子目录：
  任务编号的每一段（-XXX）对应一层子目录
  P0-001-001 → P0-001-001 - 项目初始化与基础配置/
  P0-001-001-001 → P0-001-001-001 - Spring Boot项目创建/
  ...

Step D - 叶子节点识别：
  在最深层目录中查找以完整任务编号开头的 .md 文件
  该文件即为末端任务文档
  
  如果某一层目录内同时存在子目录和 .md 文件：
  - .md 文件 = 末端任务文档
  - 子目录 = 还有更深层任务
  按目录名字母/数字顺序，先处理子目录内的任务，再处理同级 .md
```

### Step 4：加载上下文

```
必读（按顺序，不得跳过）：
  1. 当前任务文档（Section 1-8 全部读完）
  2. 所属一级模块的 层级规范-P*-XXX.md
  3. 任务文档 Section 4（关联规范引用）中列出的全局规范文档

按需加载（根据任务类型，见 §4.2 加载策略表）
```

### Step 5：执行开发

按全局规范-AI开发执行手册.md §4 规定的执行顺序进行开发。

---

## 4. 任务读取与规范加载

### 4.1 末端任务文档读取顺序

每份末端任务文档包含 8 个 Section，**全部按顺序读取**：

| 顺序 | Section | 获取信息 |
|:---:|---------|---------|
| 1 | 一、任务标识 | 任务编号、所属模块、任务类型 |
| 2 | 二、任务目标 | 具体要做什么 |
| 3 | 三、前置依赖 | 需要什么前置条件和资源 |
| 4 | 四、关联规范引用 | 需要遵循哪些全局规范文档 |
| 5 | 五、详细开发规格 | 具体怎么做（字段、API、逻辑、交互） |
| 6 | 六、交付物清单 | 需要产出哪些文件 |
| 7 | 七、验收标准 | 如何判断做得对不对 |
| 8 | 八、易错警示 | 需要避免什么错误 |

### 4.2 任务类型判定与规范加载策略

| 关键词/路径特征 | 类型 | 必须加载的全局规范 | 按需加载 |
|---------------|------|------------------|---------|
| DDL / 建表 / 索引 | DDL | 数据库规范、项目架构与开发约束 | 安全开发规范 |
| Entity / DTO / VO | ENTITY | 后端代码规范、数据库规范 | 安全开发规范 |
| Mapper / XML映射 | MAPPER | 后端代码规范、数据库规范 | - |
| Service / 业务校验 | SERVICE | 后端代码规范、项目架构与开发约束 | 通用业务规则、缓存使用规范 |
| Controller / API | CONTROLLER | 后端代码规范、API接口规范 | 安全开发规范、权限体系规范 |
| .vue / 页面 / 组件 | FRONTEND | 前端代码规范、页面类型定义 | 表格交互规范、标签页与导航规范 |
| Dockerfile / CI | DEVOPS | 部署架构规范、项目架构与开发约束 | 安全开发规范 |
| 配置文件 / 配置项 | CONFIG | 项目架构与开发约束 | 系统参数配置规范 |

### 4.3 信息不足时的处理

按以下层级查找补充：

```
1. 本任务 Section 5 开头的"模块上下文"信息框
2. 所属模块的"模块开发指南"（同级目录）
3. 所属模块的"数据表字段定义"（同级目录，如存在）
4. Section 4 引用的全局规范文档
5. 模块索引文件
```

**兜底处理**：
- 在 tasks_active.md 当前任务备注中标注 `⚠️ 信息不足-需人工补充`
- 记录具体缺失项（哪些字段/API/规则缺失）
- 跳过该任务，继续下一个可完整执行的任务
- 在 tasks_completed.md 中记录跳过原因

**绝对禁止**：
- ❌ 查阅 `erp_ai_tasks_docs/` 以外的业务设计文档
- ❌ 假设"根据设计文档确定"并自行猜测填充
- ❌ 跳过任何 Section 不读

---

## 5. 开发执行规则

> 详细执行顺序和编码规范以 `全局规范-AI开发执行手册.md` §4 为权威。以下为摘要和补充。

### 5.1 执行顺序摘要

**后端（含完整 CRUD 的模块）**：
```
DDL建表 → Entity/DTO/VO → Mapper+XML → Service+Impl → Controller
每完成一层立即编译验证（mvn compile），出错立即修复
```

**前端**：
```
类型定义 → API封装 → 组合式函数 → 页面Vue文件 → 路由注册 → 菜单配置 → 国际化词条
```

**单模块内部**：
```
数据库 → 后端全栈 → 后端验证 → 前端路由 → 前端页面 → 前后端联调
```

### 5.2 代码规范核心约束

- 技术栈版本严格遵守 `全局规范-技术栈版本锁定.md`
- 标点符号全部半角（字符串内中文除外）
- 所有函数参数必须标注类型，禁止隐式 any
- 所有组件/函数/类型使用前必须 import
- 代码注释按 AI执行手册 §8.2 格式（Java用@author AI，Vue用注释块，SQL用分隔线）

### 5.3 编译验证频率

| 时机 | 后端 | 前端 |
|------|------|------|
| 每完成一个层级（Entity/Mapper/Service/Controller） | `mvn compile` | - |
| 每创建一个 Vue 文件 | - | `pnpm build` 检查类型 |
| 任务完成时 | `mvn compile` | `pnpm build` |
| 发现错误 | 立即修复后重新编译 | 立即修复后重新编译 |

---

## 6. 任务完成自检协议（强制门禁）

**每个末端任务确认完成前，必须执行以下自检。不通过不得标记完成。**

### 6.1 必检项（所有任务类型）

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 任务目标（Section 2）完全实现 | 逐条比对目标描述与实际交付物 |
| 2 | 交付物（Section 6）全部产出 | 确认每个文件存在且内容完整 |
| 3 | 验收标准（Section 7）全部通过 | 逐项执行验证方法，记录结果 |
| 4 | 详细规格（Section 5）一致 | 核对字段名、方法名、API路径、数据类型 |
| 5 | 易错警示（Section 8）已规避 | 逐条检查每个警示项 |
| 6 | 编译通过 | 后端 `mvn compile` 无 ERROR / 前端 `pnpm build` 无 Error |
| 7 | 代码规范合规 | 命名规范、注解完整、import 正确、无隐式 any |

### 6.2 条件检查项（根据任务类型按需执行）

| 任务类型 | 额外检查项 |
|---------|-----------|
| DDL | DDL 在 PostgreSQL 15+ 执行成功；索引创建正确；COMMENT 完整 |
| ENTITY | 字段类型与 DDL 一致；校验注解齐全；通用字段通过 BaseEntity 继承 |
| MAPPER | XML 与 Mapper 接口方法一一对应；复杂 SQL 可执行 |
| SERVICE | @Transactional 标注正确；业务校验覆盖；异常使用 BusinessException |
| CONTROLLER | RESTful 路径规范；@Operation 注解齐全；权限注解 @SaCheckPermission |
| FRONTEND | 页面可渲染；Element Plus 组件使用正确；$t() 国际化；v-permission 权限 |
| 涉及多表写操作 | @Transactional(rollbackFor = Exception.class) 已添加 |

### 6.3 自检不通过的处理

```
发现问题 → 定位根因 → 修复 → 重新编译 → 重跑受影响检查项 → 直到全部通过
连续 3 次修复同一问题仍不通过 → 标记任务 ❌ 阻塞，记录原因，跳到下一个任务
```

### 6.4 自检通过后的操作（严格按顺序）

```
Step 1: 标记任务 ✅ 已完成
Step 2: 更新 tasks_completed.md → 追加完成记录（含时间、摘要）
Step 3: 更新 tasks_active.md → 推进执行指针到下一个任务、更新模块统计
Step 4: git add -A && git commit（按 §7.2 格式提交）
Step 5: 正常退出 claude 会话（auto.ps1 循环会自动启动下一个任务）
```

---

## 7. Git 管理规范

### 7.1 分支策略

```
主分支：    develop（日常开发集成分支）
功能分支：  feature/P{级别}-{模块号}
            例：feature/P0-001（后端项目框架搭建）
            例：feature/P0-007（商品管理模块）

分支生命周期：
  1. 开始新模块时从 develop 创建 feature 分支
  2. 模块内所有任务在该分支上开发提交
  3. 模块全部完成后合并回 develop
  4. 合并后保留分支（不删除，便于回溯）
```

### 7.2 提交时机与格式

- **每个末端任务完成并自检通过后**，立即提交
- 禁止累积多个任务后批量提交
- 禁止在自检未通过时提交

**Commit message 格式**：

```
feat({模块号}): {任务名称}

- {交付物摘要1}
- {交付物摘要2}

Task: {完整任务编号}
Module: {模块名称}
```

示例：
```
feat(P0-001): 执行Spring Initializr生成项目

- 创建Spring Boot 3.4.x项目骨架
- 配置pom.xml核心依赖
- 配置主启动类

Task: P0-001-001-001-001-001
Module: P0-001 后端项目框架搭建
```

### 7.3 提交安全约束

- 不得提交 `.env`、`credentials.json` 等敏感文件
- 不得提交编译产物（`target/`、`dist/`、`node_modules/`）
- 不得执行 `git push --force` 到 develop 分支
- 不得执行 `git reset --hard` 丢失已完成工作
- 提交前确认 `.gitignore` 已正确配置

### 7.4 推送规则

```
1. 本地 commit 后推送到远程 feature 分支
2. 模块最后一个任务完成后，合并到 develop
3. 记录 commit SHA 到 tasks_completed.md
4. 如遇推送冲突：先 pull --rebase，解决冲突后再 push
```

---

## 8. 全自动连续开发（任务池 + 工人池）

> **详细规则见** `ai-docs/auto-continuous-dev.md`，以下为核心摘要。

**核心机制**：任务清单 + 依赖表 + 工人池。`tasks_catalog.md` 是全量叶子任务清单（带位置编号和游标），`tasks_dependency.md` 是预计算依赖关系，`tasks_active.md` 是活跃任务（工人认领池）。工人优先从 `tasks_active.md` 认领任务，任务不足时自动从 `tasks_catalog.md` 按游标位置补充。

**启动方式**：
- **单工人**：`auto.bat` → 1 个窗口，auto.ps1 循环执行任务（每个任务一个 claude 会话）
- **多工人**：`parallel.bat N` → N 个窗口同时认领任务（最多 100 个，由 config.ini 配置，每个窗口独立循环）
- 窗口数和路径从 `config.ini` 读取

**工人循环**：检查停止开关 → 从 `tasks_active.md` 查找⬜/🔄任务 → 不足时从 `tasks_catalog.md` 按游标补充（查 `tasks_dependency.md` 确认依赖） → 执行叶子任务 → commit → claude 退出 → auto.ps1 循环自动启动下一个会话 → 循环

**停止开关**（`config.ini`）：
- `single_stop=true` → 单人模式当前任务完成后 auto.ps1 循环退出，窗口关闭
- `multi_stop=true` → 多人模式所有工人当前任务完成后 auto.ps1 循环退出，窗口关闭
- 用于人工暂停开发，改回 `false` 后运行 `auto.bat` 即可继续

**零闲置保证**：auto.ps1 循环在 claude 退出后自动启动下一个会话，无需人工干预，始终保持窗口不空闲。

**窗口生命周期**：每个窗口由 auto.ps1 的 while 循环驱动，循环一次 = 一个 claude 会话 = 一个叶子任务。停止开关开启 → 循环 break → auto.ps1 退出 → auto.bat 退出 → CMD 窗口关闭。

---

## 9. AI 端安全开发规范

> AI 通过 `auto.bat` + `--dangerously-skip-permissions` 启动，拥有完全权限。必须在每次提交前自行完成验证，确保代码质量可控。

### 9.1 每个任务的操作流程

**任务开始前**，AI 必须：
```
1. 确认当前分支正确
2. 读取 tasks_active.md 获取当前任务编号
```

**任务完成后**，AI 必须：
```
1. 执行 §6 自检协议（全部检查项）
2. 编译验证（后端 mvn compile / 前端 pnpm build）
3. 确认编译通过、自检全部 PASS
4. 只有全部通过后才执行 git add -A && git commit
5. 如有问题 → 修复 → 重新编译验证 → 直到全部通过
6. commit 成功后正常退出 claude 会话（auto.ps1 循环会自动启动下一个任务）
```

### 9.2 AI 自检清单（提交前必做）

```
1. tasks_completed.md 已追加完成记录
2. tasks_active.md 指针已推进到下一个任务
3. 进度文件双向一致（tasks_active.md 和 tasks_completed.md 匹配）
4. 编译通过（后端 mvn compile / 前端 pnpm build）
5. 受保护文件未被修改（全局规范/层级规范/CLAUDE.md/ai-docs子文档/模块索引/模块开发指南）
6. 无异常文件删除
7. 变更文件数在合理范围（≤30）
8. 源代码文件存在且非空（非 stub）
9. 变更涉及预期的模块目录（src/或配置文件）
10. 无硬编码密码/密钥（password=、secret=、apiKey=）
```

### 9.3 受保护文件

以下文件**禁止 AI 修改**，只能在任务文档中引用：

```
全局规范-*.md              — 全局技术规范（只读引用）
层级规范-*.md              — 模块级约束（只读引用）
CLAUDE.md                  — AI 执行规则（只读，修改需用户明确确认）
ai-docs/auto-continuous-dev.md — 全自动连续开发规则（只读，修改需用户明确确认）
ai-docs/parallel-rules.md      — 并行开发规则（只读，修改需用户明确确认）
模块索引.md                — 模块导航（只读引用）
模块开发指南.md            — 模块开发指导（只读引用）
```

---

## 10. 并行开发规则

> **详细规则见** `ai-docs/parallel-rules.md`，依赖关系和并行分组见 `tasks_dependency.md`。

**调度模型**：任务清单 + 工人池。`tasks_catalog.md` 是全量任务清单，`tasks_dependency.md` 是预计算依赖关系，工人通过 `.catalog.lock` 串行化认领操作。

**两个层级**：
- **任务内并行**：单任务内部使用 Agent 工具拆分独立子步骤，主会话统一编译验证和提交
- **模块间并行**：多个工人窗口同时从 `tasks_catalog.md` 认领不同模块的任务，独立完成

**8 条严格规则**（违反任一条即执行失败）：
1. 工人窗口上限由 `config.ini` 的 `max_workers` 决定（`parallel.bat` 参数最大 100）
2. 文件/目录绝对隔离（两个窗口不能改同一文件/目录）
3. 禁止同时修改共享文件（.vue/.java/路由/配置/XML）
4. 无依赖才可并行（`tasks_dependency.md` 依赖总表控制）
5. 执行顺序遵循层级（基础→Entity→Service→Controller→前端→配置）
6. `tasks_catalog.md` 读写和 Git 提交串行化（通过锁机制）
7. 目录隔离规范（前端/后端/配置/数据库各守各的路径）
8. 多工人通过 `tasks_active.md` 中的🔄标记+窗口ID 防止重复认领

**启动方式**：`parallel.bat N` → 启动 N 个 `auto.bat` 工人窗口 → 各窗口自动从 `tasks_catalog.md` 认领任务

---

## 11. 进度文档管理

### 11.1 tasks_active.md 维护规则

```
写入时机：
  - 叶子任务状态变更（⬜ → 🔄 → ✅）
  - 执行指针推进（当前窗口/模块/任务）
  - 从 tasks_catalog.md 按 fetch_cursor 补充叶子任务
  - 模块完成时清理已完成叶子 + 归档到 tasks_completed.md
  - 发现阻塞
  - 更新 fetch_cursor

内容范围（仅限）：
  - 执行头信息（当前窗口、当前模块、当前任务、分支）
  - 取任务游标（fetch_cursor）
  - 活跃叶子任务表（⬜/🔄 状态）
  - 统计数据（活跃模块数、待认领数、执行中数）

禁止写入：业务需求、代码、字段定义、验收标准
行数控制：< 500 行（活跃叶子超出时及时清理已完成的）
```

### 11.2 tasks_completed.md 维护规则

```
写入时机：每个任务自检通过后，追加到末尾

每条记录包含：
  - 任务编号、任务名称
  - 完成时间
  - 状态（✅ / ⏭️）
  - 一句话摘要
  - Git commit SHA
  - 跳过原因（仅跳过时）

组织方式：
  - 按模块分节，仅在模块有完成记录时才创建该模块的节
  - 初始状态只包含表头和统计汇总
  - 随着任务完成逐步增长

特性：只追加不读取，不消耗工人 token
禁止写入：代码实现、业务逻辑、技术细节
```

---

## 12. 异常处理

| 场景 | 处理方式 |
|------|---------|
| 编译失败 | 分析错误 → 定位文件行号 → 修复 → 重新编译 → 通过后继续 |
| 任务阻塞 | 标记 ❌ → 记录原因 → 跳到下一个可执行任务 → 阻塞解除后回来 |
| 前置依赖缺失 | 暂停当前 → 先执行缺失的前置任务 → 完成后回到当前 |
| 信息不足 | 按 §4.3 层级查找 → 仍不足则标记 ⚠️ 跳过 → 记录缺失项 |
| 版本冲突 | 以 `全局规范-技术栈版本锁定.md` 为准，不得更改版本号 |
| Git 冲突 | `git pull --rebase` → 解决冲突 → `git push` |
| 连续 3 次修复同一问题仍失败 | 标记 ❌ 阻塞 → 记录详细错误 → 跳到下一个任务 |

---

## 13. 禁止行为清单

| 序号 | 禁止行为 | 原因 |
|:---:|---------|------|
| 1 | 跳过自检直接标记完成 | 质量门禁不可绕过 |
| 2 | 假设或猜测字段、API、业务规则 | 必须从任务文档获取 |
| 3 | 查阅 `erp_ai_tasks_docs/` 以外的业务文档 | AI执行手册禁止 |
| 4 | 修改全局规范或层级规范文档 | 规范文档只读引用 |
| 5 | 留下未完成的任务进入下一个 | 单任务原子性 |
| 6 | 跳过 Section 不读 | 8个Section必须全读 |
| 7 | 使用非锁定版本的技术栈 | 版本锁定为刚性约束 |
| 8 | 在管控文档中写入业务内容 | 文档职责隔离 |
| 9 | 累积多个任务后批量 Git 提交 | 每任务完成后立即提交 |
| 10 | 并行Agent修改同一文件 | 冲突防护 |
| 11 | git push --force 到 develop | 分支安全 |
| 12 | 提交敏感文件或编译产物 | Git安全 |
| 13 | 未更新 progress 文件就结束会话 | 会导致下一个会话无法接续 |

---

## 14. 快速指令对照表

| 用户输入 | AI 执行动作 |
|---------|-----------|
| `继续开发` / `继续` | 运行 `auto.bat`，工人从 `tasks_active.md` 认领任务并执行 |
| `查看进度` / `进度` | 显示 `tasks_active.md` + `tasks_completed.md` 中的状态统计 |
| `跳过任务` / `跳过` | 标记当前任务 ⏭️，推进到下一个 |
| `并行开发 N` / `并行 N` | 运行 `parallel.bat N`，启动 N 个工人窗口并行认领 |
| `查看任务池` | 显示 `tasks_catalog.md`（fetch_cursor 位置）+ `tasks_active.md` 中的任务状态 |
| `暂停开发` / `暂停` | 将 `config.ini` 中 `single_stop` 和 `multi_stop` 设为 `true` |
| `恢复开发` / `恢复` | 将 `config.ini` 中停止开关改回 `false`，运行 `auto.bat` 继续 |
| `任务内并行` | 对当前任务启用 Agent 工具拆分（参考 `ai-docs/parallel-rules.md` §1） |
| `重新执行` / `重试` | 回滚当前任务代码变更，重新执行 |
| `回退任务` / `回退` | 回退到上一个已完成任务重做 |
| `模块切换` | 手动切换到指定模块（合并旧分支、创建新分支） |

---

> **本文件由 AI 全自动开发系统维护，任何修改需经用户明确确认后生效。**

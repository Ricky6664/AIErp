# 发现问题清单 — P0-004-006-004-001-002

## 基本信息

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-004-006-004-001-002 |
| 检查日期 | 2026-06-04 |
| 被检文件 | `erp-ai-web/src/utils/request/tokenRefresh.ts`, `erp-ai-web/src/utils/request.ts` |

## 问题汇总

| # | 严重度 | 问题描述 | 状态 |
|:-:|:------:|---------|:----:|
| 1 | 🟡 低 | 错误码不一致 | 已记录跟踪 |

## 问题详情

### 问题 1: Token 过期错误码不一致

**严重度**: 🟡 低（不影响功能，建议后续统一）

**描述**:
任务规格文档（P0-004-006-004-001-001 Section 5.2）中列出的 Token 过期错误码为 `TOKEN_EXPIRED=401001`，但实际实现中使用的是 `code === 20001`：

```typescript
// erp-ai-web/src/utils/request.ts:91
if (code === 20001) {
  return handleTokenRefresh(response.config)
}
```

同时，HTTP 错误拦截器中也捕获了 HTTP 401 状态码并触发刷新：

```typescript
// erp-ai-web/src/utils/request.ts:142
case 401:
  return handleTokenRefresh(config!)
```

**影响**: 不阻塞功能，但存在规格与实际不完全一致的情况。后端可能使用不同的错误码策略（20001 作为业务层面的 token 过期，401 作为 HTTP 层面的未授权）。

**建议**: 与后端确认统一错误码后，更新规格文档或调整为 401001。

## 验证结论

Token 刷新核心逻辑实现完整且正确：
- 主流程正常（刷新 → 存储 → 重放）
- 并发控制正确（isRefreshing + pendingQueue）
- 降级处理合理（清除状态 → 跳转登录）
- 防无限循环有效（isRefreshRequest 检测）
- 边界条件处理得当

**无阻塞性问题，所有核心验证通过。**

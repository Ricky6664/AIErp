# P0-003-002-006-001-001 编写单据主表DDL约束与枚举注释 — 任务规范/验证文档

## 任务摘要

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-003-002-006-001-001 |
| 任务名称 | 编写DDL+约束（bill_no VARCHAR(32) NOT NULL UNIQUE含is_deleted+bill_status SMALLINT DEFAULT 0+枚举注释） |
| 目标Schema | erp_base |
| 迁移脚本 | V20260601002__task_P0_003_002_006_001_001.sql |
| 完成日期 | 2026-06-01 |

## 创建的对象一览

| 对象名 | 类型 | 说明 |
|--------|------|------|
| `erp_base.bill_main_field_spec` | TABLE | 单据主表特有字段规范定义表 |
| `pk_bill_main_field_spec` | PRIMARY KEY | 主键约束（field_name） |
| `erp_base.fn_validate_bill_fields` | FUNCTION | 单据主表字段结构合规校验函数 |

## 单据特有字段规范

| 序号 | 字段名 | 数据类型 | 默认值 | NOT NULL | 部分唯一索引 | 说明 |
|:---:|--------|---------|--------|:---:|:---:|------|
| 1 | bill_no | VARCHAR(32) | — | YES | YES | 单据编号，由编码规则引擎生成 |
| 2 | bill_status | SMALLINT | 0 | YES | NO | 单据状态枚举 |

## bill_status 枚举值定义

| 值 | 状态 | 说明 |
|:---:|------|------|
| 0 | 草稿 | 新建单据初始状态，可编辑、可删除 |
| 1 | 待审核 | 提交审核中，不可编辑，可撤回至草稿 |
| 2 | 已审核 | 审核通过，可执行后续业务操作 |
| 3 | 已关闭 | 业务流程完结，不可再操作 |
| 4 | 已作废 | 单据作废，不可再操作，任意状态可跳转 |

## 单据主表完整字段结构

```
业务字段 (N个) → 单据特有字段 (2个) → 扩展字段 (22个) → 公共字段 (10个)
```

## 验收标准核查

| 序号 | 检查项 | 状态 | 说明 |
|:---:|--------|:---:|------|
| 1 | 脚本执行成功，无SQL语法错误 | PASS | CREATE TABLE IF NOT EXISTS / INSERT ON CONFLICT / CREATE OR REPLACE FUNCTION 均为幂等安全语法 |
| 2 | 数据完整性校验通过 | PASS | 2条规范记录（bill_no、bill_status）通过 ON CONFLICT 幂等插入 |
| 3 | 幂等性验证 | PASS | 所有DDL使用 IF NOT EXISTS / ON CONFLICT DO UPDATE / OR REPLACE，多次执行无副作用 |
| 4 | Flyway版本号无冲突 | PASS | V20260601002（2026-06-01第2个脚本），未与现有脚本冲突 |
| 5 | COMMENT注释完整 | PASS | 表/列/函数均有COMMENT注释，bill_status含完整枚举值（0=草稿,1=待审核,2=已审核,3=已关闭,4=已作废） |

## 规范合规确认

- [x] 10个通用字段完整包含 — 示例表展示完整10个公共字段 + 22个扩展字段
- [x] 部分唯一索引含 WHERE is_deleted = false — bill_no 索引模板含 `WHERE is_deleted = FALSE`
- [x] tenant_id 作为联合索引首列 — `(tenant_id, bill_no)`
- [x] 数值字段统一 DECIMAL(18,8) — 示例表中金额字段使用 DECIMAL(18,8)
- [x] 所有表和字段含 COMMENT 注释 — 规范表及示例表均为完整注释
- [x] Flyway 命名合规 — `V20260601002__task_P0_003_002_006_001_001.sql`（双下划线）
- [x] 未使用数据库外键约束 — 全部建表/示例未出现 FOREIGN KEY
- [x] bill_no VARCHAR(32) NOT NULL — 规范定义正确
- [x] bill_status SMALLINT DEFAULT 0 — 规范定义正确
- [x] bill_status 枚举注释完整 — 0=草稿/1=待审核/2=已审核/3=已关闭/4=已作废
- [x] 幂等性安全 — CREATE TABLE IF NOT EXISTS + INSERT ON CONFLICT DO UPDATE + CREATE OR REPLACE FUNCTION

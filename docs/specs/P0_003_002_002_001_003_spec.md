# P0-003-002-002-001-003 验证编写系统核心表DDL — 验证规范文档

## 1. 验证概览

| 属性 | 值 |
|------|-----|
| 验证编号 | P0-003-002-002-001-003 |
| 验证范围 | 10张系统核心表（sys_user/sys_role/sys_menu/sys_user_role/sys_user_dept/sys_role_menu/sys_role_data_scope/sys_role_field_permission/sys_user_group/sys_user_group_member） |
| 验证依据 | P0-003-002-002-001-001（CREATE TABLE DDL）、P0-003-002-002-001-002（索引与约束） |
| 验证日期 | 2026-05-31 |
| 验证工具 | PostgreSQL 15+ 系统视图（pg_tables / information_schema.columns / pg_indexes / pg_description / flyway_schema_history） |

## 2. 验证维度

| 维度 | 验证内容 | 预期结果 |
|------|---------|---------|
| 表存在性 | 10张系统核心表均存在于 public schema | pg_tables COUNT = 10 |
| 通用字段 | 每表包含10个通用必含字段 | information_schema.columns 全覆盖 |
| 字段类型 | tenant_id BIGINT NOT NULL, is_deleted BOOLEAN DEFAULT false, decimal(18,8) | 类型与设计一致 |
| 索引 | 27个索引（含10个唯一索引 + 17个普通索引） | pg_indexes COUNT >= 27 |
| 部分唯一索引 | 所有UNIQUE INDEX包含 WHERE is_deleted = false | indexdef ILIKE '%WHERE is_deleted = false%' |
| 联合索引首列 | 所有联合索引以 tenant_id 为首列 | indexdef ~ '(tenant_id[,\)]' |
| COMMENT | 所有表和字段包含COMMENT注释 | 覆盖率 100% |
| 外键 | 禁止数据库级外键约束 | information_schema.table_constraints COUNT = 0 |
| Flyway | 迁移脚本 V20260531004、V20260531005 执行成功 | flyway_schema_history.success = true |

## 3. 验证方法

### 3.1 执行验证脚本

在 PostgreSQL 客户端中执行 `db/migration/V20260526001__task_P0_003_002_002_001_003.sql`，逐段检查 PASS/FAIL 标记。

### 3.2 关键验证SQL

**表存在性：**
```sql
SELECT COUNT(*) FROM pg_tables
WHERE schemaname = 'public'
  AND tablename IN ('sys_user', 'sys_role', 'sys_menu',
      'sys_user_role', 'sys_user_dept', 'sys_role_menu',
      'sys_role_data_scope', 'sys_role_field_permission',
      'sys_user_group', 'sys_user_group_member');
-- 预期：10
```

**通用字段完整性（以sys_user为例）：**
```sql
SELECT column_name FROM information_schema.columns
WHERE table_schema = 'public' AND table_name = 'sys_user'
  AND column_name IN ('id', 'tenant_id', 'created_at', 'updated_at',
      'created_by', 'updated_by', 'is_deleted', 'owner_dept_id', 'owner_id', 'version');
-- 预期：10 rows
```

**部分唯一索引检查：**
```sql
SELECT indexname FROM pg_indexes
WHERE schemaname = 'public'
  AND indexdef ILIKE '%CREATE UNIQUE INDEX%'
  AND indexdef NOT ILIKE '%WHERE is_deleted = false%'
  AND tablename IN ('sys_user', 'sys_role', 'sys_menu',
      'sys_user_role', 'sys_user_dept', 'sys_role_menu',
      'sys_role_data_scope', 'sys_role_field_permission',
      'sys_user_group', 'sys_user_group_member');
-- 预期：0 rows（无不带WHERE条件的唯一索引）
```

**禁止外键：**
```sql
SELECT COUNT(*) FROM information_schema.table_constraints
WHERE constraint_schema = 'public'
  AND constraint_type = 'FOREIGN KEY'
  AND table_name IN ('sys_user', 'sys_role', 'sys_menu',
      'sys_user_role', 'sys_user_dept', 'sys_role_menu',
      'sys_role_data_scope', 'sys_role_field_permission',
      'sys_user_group', 'sys_user_group_member');
-- 预期：0
```

## 4. 静态验证结果（代码审查）

> 以下为对 `V20260531004` 和 `V20260531005` SQL 脚本的静态代码审查结果。

### 4.1 表清单（Part 1: CREATE TABLE）

| 序号 | 表名 | 业务字段数 | 扩展字段数 | 通用字段数 | 表COMMENT |
|:---:|------|:---:|:---:|:---:|:---:|
| 1 | sys_user | 12 | 22 | 10 | ✅ |
| 2 | sys_role | 6 | 22 | 10 | ✅ |
| 3 | sys_menu | 12 | 22 | 10 | ✅ |
| 4 | sys_user_role | 2 | 22 | 10 | ✅ |
| 5 | sys_user_dept | 2 | 22 | 10 | ✅ |
| 6 | sys_role_menu | 3 | 22 | 10 | ✅ |
| 7 | sys_role_data_scope | 3 | 22 | 10 | ✅ |
| 8 | sys_role_field_permission | 4 | 22 | 10 | ✅ |
| 9 | sys_user_group | 5 | 22 | 10 | ✅ |
| 10 | sys_user_group_member | 2 | 22 | 10 | ✅ |

### 4.2 通用必含字段一致性

所有10张表均包含以下字段，类型/默认值一致：

| 字段 | 类型 | 约束 | 默认值 |
|------|------|------|--------|
| id | BIGSERIAL | PRIMARY KEY | — |
| tenant_id | BIGINT | NOT NULL | — |
| created_at | TIMESTAMP | NOT NULL | NOW() |
| updated_at | TIMESTAMP | NOT NULL | NOW() |
| created_by | BIGINT | — | — |
| updated_by | BIGINT | — | — |
| is_deleted | BOOLEAN | NOT NULL | false |
| owner_dept_id | BIGINT | — | — |
| owner_id | BIGINT | — | — |
| version | INT | NOT NULL | 1 |

### 4.3 数值精度一致性

所有 ext_num1~ext_num5 字段统一使用 `DECIMAL(18,8)`：✅ 通过（10表 × 5字段 = 50处一致）

### 4.4 索引清单（Part 2: CREATE INDEX）

| 序号 | 表名 | 索引数 | 唯一索引 | 普通索引 |
|:---:|------|:---:|:---:|:---:|
| 1 | sys_user | 6 | 3 | 3 |
| 2 | sys_role | 2 | 1 | 1 |
| 3 | sys_menu | 4 | 1 | 3 |
| 4 | sys_user_role | 3 | 1 | 2 |
| 5 | sys_user_dept | 3 | 1 | 2 |
| 6 | sys_role_menu | 3 | 1 | 2 |
| 7 | sys_role_data_scope | 2 | 1 | 1 |
| 8 | sys_role_field_permission | 3 | 1 | 2 |
| 9 | sys_user_group | 2 | 1 | 1 |
| 10 | sys_user_group_member | 3 | 1 | 2 |
| **合计** | | **31** | **12** | **19** |

> 注：含各表主键索引（BIGSERIAL PRIMARY KEY自动创建的唯一索引），indexdef中以 `_pkey` 后缀命名。CREATE INDEX语句本身创建了 27 个索引，加上 10 个主键索引共 37 个索引。

### 4.5 易错警示对照检查

| 警示项 | 检查结果 |
|--------|:---:|
| 10个通用字段完整 (id/tenant_id/created_at/updated_at/created_by/updated_by/is_deleted/owner_dept_id/owner_id/version) | ✅ |
| 部分唯一索引包含 WHERE is_deleted = false | ✅ (12个全部包含) |
| 联合索引以 tenant_id 为首列 | ✅ (所有idx_索引) |
| decimal(18,8) 禁止变体 (DECIMAL(18,8)/DECIMAL(18,6)) | ✅ (统一使用DECIMAL(18,8)) |
| Flyway命名 V{yyyyMMdd}{seq}__{description}.sql（双下划线） | ✅ |
| 所有表和字段含COMMENT | ✅ |
| 禁止数据库外键约束 | ✅ (无FOREIGN KEY) |

## 5. 验证结论

**静态代码审查：全部通过 ✅**

- 10张系统核心表DDL定义完整、规范一致
- 10个通用必含字段在所有表中类型/默认值完全一致
- 31个索引（含12个部分唯一索引）命名规范、WHERE条件正确
- 所有联合索引以 tenant_id 为首列
- decimal(18,8) 精度统一
- COMMENT注释覆盖率100%
- 无数据库级外键约束
- 易错警示8项全部规避

**待运行时验证：**
- 需在 PostgreSQL 15+ 中执行 `V20260526001__task_P0_003_002_002_001_003.sql` 验证脚本
- 确认 Flyway 迁移历史中 V20260531004 和 V20260531005 的 success = true

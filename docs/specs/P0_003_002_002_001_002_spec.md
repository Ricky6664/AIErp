# P0-003-002-002-001-002 系统核心表索引与约束 Spec

## 任务概述

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-003-002-002-001-002 |
| 任务名称 | 编写系统核心表索引与约束 |
| Flyway脚本 | V20260531005__task_P0_003_002_002_001_002.sql |

## 涉及表

| 序号 | 表名 | 唯一索引 | 查询索引 |
|:---:|------|---------|---------|
| 1 | sys_user | 3 | 3 |
| 2 | sys_role | 1 | 1 |
| 3 | sys_menu | 1 | 3 |
| 4 | sys_user_role | 1 | 2 |
| 5 | sys_user_dept | 1 | 2 |
| 6 | sys_role_menu | 1 | 2 |
| 7 | sys_role_data_scope | 1 | 1 |
| 8 | sys_role_field_permission | 1 | 2 |
| 9 | sys_user_group | 1 | 1 |
| 10 | sys_user_group_member | 1 | 2 |

## 索引详情

### sys_user
- `uk_sys_user_username_active` — UNIQUE (tenant_id, username) WHERE is_deleted=false
- `uk_sys_user_mobile_active` — UNIQUE (tenant_id, mobile) WHERE is_deleted=false AND mobile IS NOT NULL
- `uk_sys_user_email_active` — UNIQUE (tenant_id, email) WHERE is_deleted=false AND email IS NOT NULL
- `idx_sys_user_tenant_employee` — (tenant_id, employee_id)
- `idx_sys_user_tenant_status` — (tenant_id, status)
- `idx_sys_user_tenant_id` — (tenant_id)

### sys_role
- `uk_sys_role_code_active` — UNIQUE (tenant_id, role_code) WHERE is_deleted=false
- `idx_sys_role_tenant_id` — (tenant_id)

### sys_menu
- `uk_sys_menu_permission_active` — UNIQUE (tenant_id, permission_code) WHERE is_deleted=false AND permission_code IS NOT NULL
- `idx_sys_menu_tenant_parent` — (tenant_id, parent_id)
- `idx_sys_menu_tenant_type` — (tenant_id, menu_type)
- `idx_sys_menu_tenant_id` — (tenant_id)

### sys_user_role
- `uk_sys_user_role_user_role_active` — UNIQUE (tenant_id, user_id, role_id) WHERE is_deleted=false
- `idx_sys_user_role_tenant_user` — (tenant_id, user_id)
- `idx_sys_user_role_tenant_role` — (tenant_id, role_id)

### sys_user_dept
- `uk_sys_user_dept_user_dept_active` — UNIQUE (tenant_id, user_id, dept_id) WHERE is_deleted=false
- `idx_sys_user_dept_tenant_user` — (tenant_id, user_id)
- `idx_sys_user_dept_tenant_dept` — (tenant_id, dept_id)

### sys_role_menu
- `uk_sys_role_menu_role_menu_active` — UNIQUE (tenant_id, role_id, menu_id) WHERE is_deleted=false
- `idx_sys_role_menu_tenant_role` — (tenant_id, role_id)
- `idx_sys_role_menu_tenant_menu` — (tenant_id, menu_id)

### sys_role_data_scope
- `uk_sys_role_data_scope_role_type_active` — UNIQUE (tenant_id, role_id, scope_type) WHERE is_deleted=false
- `idx_sys_role_data_scope_tenant_role` — (tenant_id, role_id)

### sys_role_field_permission
- `uk_sys_role_field_perm_active` — UNIQUE (tenant_id, role_id, table_name, field_name) WHERE is_deleted=false
- `idx_sys_role_field_perm_tenant_role` — (tenant_id, role_id)
- `idx_sys_role_field_perm_tenant_table` — (tenant_id, table_name)

### sys_user_group
- `uk_sys_user_group_code_active` — UNIQUE (tenant_id, group_code) WHERE is_deleted=false
- `idx_sys_user_group_tenant_id` — (tenant_id)

### sys_user_group_member
- `uk_sys_user_group_member_active` — UNIQUE (tenant_id, group_id, user_id) WHERE is_deleted=false
- `idx_sys_user_group_member_tenant_group` — (tenant_id, group_id)
- `idx_sys_user_group_member_tenant_user` — (tenant_id, user_id)

## 验收确认

| 序号 | 检查项 | 结果 |
|:---:|--------|:---:|
| 1 | 所有唯一索引含WHERE is_deleted=false | PASS |
| 2 | tenant_id作为联合索引首列 | PASS |
| 3 | 索引命名规范(uk_/idx_前缀) | PASS |
| 4 | 共12个UK + 22个IDX = 34个索引 | PASS |
| 5 | Flyway版本号V20260531005无冲突 | PASS |

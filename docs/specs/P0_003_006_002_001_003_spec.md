# P0-003-006-002-001-003 SRM标签定义表DDL验证规范

## 1. 验证概要

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-003-006-002-001-003 |
| 验证对象 | srm_tag_definition SRM标签定义表DDL |
| 前序迁移 | V20260602001 (CREATE TABLE), V20260602002 (indexes/constraints) |
| 验证脚本 | V20260602003__verify_srm_tag_definition.sql |

## 2. 验证项清单

### 2.1 表存在性
- 表 `srm_tag_definition` 存在于 `public` schema

### 2.2 字段完整性
- 总字段数：37（6业务 + 10 ext_str + 5 ext_num + 3 ext_date + 3 ext_bool + 1 ext_json + 10通用）
- 10个通用字段全部存在：id, tenant_id, created_by, created_at, updated_by, updated_at, is_deleted, owner_dept_id, owner_id, version
- 6个业务字段：tag_name, tag_group, tag_color, sort_no, enable_flag, ext_json

### 2.3 约束验证
- 主键：pk_srm_tag_definition（由 srm_tag_definition_pkey 重命名而来）
- 部分唯一索引：uk_srm_tag_definition_tag_name WHERE is_deleted = false
- 无外键约束（应用层维护关联）

### 2.4 索引验证
- 总索引数：12（1 PK + 1 部分唯一 + 10 普通索引）
- 多租户索引以 tenant_id 为首列（至少2个）
- 普通索引覆盖：tenant_id+tag_name, tenant_id+is_deleted, tag_group, enable_flag, sort_no, created_by, updated_by, owner_dept_id, owner_id, tenant_id+created_at

### 2.5 数值精度
- ext_num1~5 均为 decimal(18,8)

### 2.6 NOT NULL约束
- NOT NULL字段：id, tenant_id, tag_name, created_at, updated_at, is_deleted, version

### 2.7 COMMENT注释
- 表级 COMMENT 存在
- 所有字段 COMMENT 完整（无 MISSING）

### 2.8 Flyway迁移记录
- 3个迁移脚本（V20260602001, V20260602002, V20260602003）均 success=true

## 3. 预期结果

| 检查项 | 期望值 | 验证SQL段 |
|--------|:------:|:---------:|
| TABLE_EXISTS | PASS | §1 |
| FIELD_COUNT | 37 | §4 |
| 10通用字段缺失 | 0行 | §3 |
| num精度 | 全部PASS | §5 |
| PK存在 | PASS | §6 |
| PK命名 | pk_srm_tag_definition | §7 |
| 部分唯一索引WHERE | PASS | §8 |
| INDEX_COUNT | 12 | §10 |
| TENANT_INDEX | PASS (>=2) | §11 |
| TABLE_COMMENT | PASS | §12 |
| COMMENT_MISSING | 0个MISSING | §12 |
| NOT NULL | 全部PASS | §13 |
| IS_DELETED默认false | PASS | §14 |
| ENABLE_FLAG默认true | PASS | §14 |
| FK_CHECK (无外键) | PASS | §16 |
| 列名一致性 | 全部PASS | §17 |

## 4. 易错点提醒

- `is_deleted` 为 BOOLEAN 类型，部分唯一索引必须用 `WHERE is_deleted = false` 而非 `WHERE is_deleted = 0`
- 主键 inline 定义为 `srm_tag_definition_pkey`，索引迁移将其重命名为 `pk_srm_tag_definition`
- ext_json 为 JSONB 类型，飞行记录中可能显示为 jsonb

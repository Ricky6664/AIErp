/**
 * 业务模块容器包.
 * <p>
 * 按业务域组织所有业务模块, 每个子包代表一个独立的业务模块.
 * 每个业务模块内部包含 controller、service、mapper、entity、dto、vo 子包.
 * 业务模块列表: system(系统管理)、auth(认证授权)、user(用户管理)、
 * permission(权限配置)、crm(客户管理)、srm(供应商管理)、product(商品管理)等.
 * </p>
 *
 * @author AI
 */
package com.erp.module;

# P2-008-002-009-001-001 搜索表单布局

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-008-002-009-001-001 |
| 任务名称 | 搜索表单布局 |
| 所属模块 | P2-008 |
| 优先级 | P2 |
| 任务类型 | 前端搜索表单 |

## 二、任务目标

实现新闻列表页搜索表单布局：搜索字段包括新闻标题(title)、分类(category)、发布状态(status)、发布日期范围(publishDateRange)共4个字段；el-form+el-row/el-col响应式栅格(xl=6/lg=8/md=12/sm=24)；labelWidth统一100px；展开/收起功能(默认显示全部4个常用条件)

## 三、前置依赖

### 3.1 前置任务

- P2-008-002-009-001 搜索条件区+数据表格区+分类筛选+置顶标记+联调验证（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：oa_news(新闻表)
> - 涉及API：GET /api/oa/news/list

### 5.1 组件结构

```vue
<template>
  <el-form :model="searchForm" label-width="100px" class="news-search-form">
    <el-row :gutter="20">
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item label="新闻标题">
          <el-input v-model="searchForm.title" placeholder="请输入新闻标题关键字"
                    clearable @keyup.enter="handleSearch"/>
        </el-form-item>
      </el-col>
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item label="新闻分类">
          <el-select v-model="searchForm.category" placeholder="全部分类" clearable>
            <el-option label="公司新闻" value="company"/>
            <el-option label="行业动态" value="industry"/>
            <el-option label="通知公告" value="notice"/>
          </el-select>
        </el-form-item>
      </el-col>
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item label="发布状态">
          <el-select v-model="searchForm.status" placeholder="全部状态" clearable>
            <el-option label="草稿" value="draft"/>
            <el-option label="已发布" value="published"/>
            <el-option label="已下线" value="offline"/>
          </el-select>
        </el-form-item>
      </el-col>
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item label="发布日期">
          <el-date-picker v-model="searchForm.publishDateRange" type="daterange"
                          range-separator="至" start-placeholder="开始日期" end-placeholder="结束日期"
                          value-format="YYYY-MM-DD"/>
        </el-form-item>
      </el-col>
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item>
          <el-button type="primary" @click="handleSearch">搜索</el-button>
          <el-button @click="handleReset">重置</el-button>
          <el-link @click="expand = !expand">{{ expand ? '收起' : '展开' }}</el-link>
        </el-form-item>
      </el-col>
    </el-row>
  </el-form>
</template>
```

### 5.2 字段布局说明

| 序号 | 字段名 | 组件 | 栅格占比 | 是否常用 |
|:---:|--------|------|---------|---------|
| 1 | title | el-input | xl=6/lg=8/md=12/sm=24 | 是(默认显示) |
| 2 | category | el-select | xl=6/lg=8/md=12/sm=24 | 是(默认显示) |
| 3 | status | el-select | xl=6/lg=8/md=12/sm=24 | 是(默认显示) |
| 4 | publishDateRange | el-date-picker(daterange) | xl=6/lg=8/md=12/sm=24 | 是(默认显示) |
| 5 | 操作按钮组 | el-button+el-link | xl=6/lg=8/md=12/sm=24 | 始终显示 |

### 5.3 新闻分类选项(字典:news_category)

| 值 | 标签 |
|---|------|
| company | 公司新闻 |
| industry | 行业动态 |
| notice | 通知公告 |

### 5.4 新闻状态选项(字典:news_status)

| 值 | 标签 |
|---|------|
| draft | 草稿 |
| published | 已发布 |
| offline | 已下线 |

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-web/src/views/oa/news/components/NewsSearchForm.vue | 新闻搜索表单组件 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | el-form+栅格布局正确，4个搜索字段+1个按钮组 | 浏览器查看 |
| 2 | title el-input渲染正确 | 输入测试 |
| 3 | category el-select显示3种分类 | 点击下拉框 |
| 4 | status el-select显示3种状态 | 点击下拉框 |
| 5 | publishDateRange daterange模式正确 | 选择日期 |
| 6 | 响应式适配不同屏幕宽度 | 调整窗口 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 新闻分类值company/industry/notice需与后端枚举一致

> ⚠️ 新闻状态值draft/published/offline需与后端枚举一致(注意与公告状态不同)

> ⚠️ 响应式断点需要在不同屏幕宽度下逐一测试

> ⚠️ [OA模块] 新闻与公告是独立模块，状态枚举值不完全相同

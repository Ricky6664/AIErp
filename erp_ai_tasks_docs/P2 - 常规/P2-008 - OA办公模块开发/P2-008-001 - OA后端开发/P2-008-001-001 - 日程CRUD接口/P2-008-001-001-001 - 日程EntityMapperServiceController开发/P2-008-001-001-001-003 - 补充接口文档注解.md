# P2-008-001-001-001-003 补充接口文档注解

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-008-001-001-001-003 |
| 任务名称 | 补充接口文档注解 |
| 所属模块 | P2-008 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

为 OaScheduleController 及日程相关 DTO/VO 补充 Knife4j 接口文档注解：类 @Tag(name="OA办公-日程管理")；每个方法 @Operation(summary+description)；路径/查询参数 @Parameter；DTO/VO 字段 @Schema(description+example+required)，example 值为日程业务真实样例

## 三、前置依赖

### 3.1 前置任务

- P2-008-001-001-001 日程Entity/Mapper/Service/Controller开发（父任务）
- P2-008-001-001-001-002 实现接口方法（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-API接口规范 | API接口设计与RESTful规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 数据表：oa_schedule（日程表）
> - API基础路径：/api/oa/schedule
> - Controller：OaScheduleController
> - 创建DTO：OaScheduleCreateDTO

### 5.1 Controller 类注解

```java
@Slf4j
@RestController
@RequestMapping("/api/oa/schedule")
@Tag(name = "OA办公-日程管理", description = "日程管理相关接口，含CRUD及日程专属操作")
public class OaScheduleController {
    // ...
}
```

### 5.2 标准 CRUD 方法注解示例

```java
@PostMapping("/create")
@Operation(summary = "创建日程", description = "创建新的日程记录，返回创建后的完整VO")
public Result<OaScheduleVO> create(
        @RequestBody @Valid @Parameter(description = "日程创建参数") OaScheduleCreateDTO dto) {
    // ...
}

@PutMapping("/update")
@Operation(summary = "修改日程", description = "修改日程记录，需携带id字段，使用乐观锁")
public Result<Boolean> update(
        @RequestBody @Valid @Parameter(description = "日程修改参数") OaScheduleUpdateDTO dto) {
    // ...
}

@DeleteMapping("/delete/{id}")
@Operation(summary = "删除日程", description = "逻辑删除日程记录，设置 is_deleted=1")
public Result<Boolean> delete(
        @PathVariable @Parameter(description = "日程ID") Long id) {
    // ...
}

@GetMapping("/page")
@Operation(summary = "分页查询日程", description = "按条件分页查询日程列表，支持多字段筛选")
public Result<Page<OaScheduleListVO>> page(OaScheduleQueryDTO query) {
    // ...
}

@GetMapping("/detail/{id}")
@Operation(summary = "日程详情", description = "根据ID查询日程完整信息")
public Result<OaScheduleVO> detail(
        @PathVariable @Parameter(description = "日程ID") Long id) {
    // ...
}
```

### 5.3 日程专属端点注解示例

```java
    @GetMapping("/calendar")
    @Operation(summary = "日程月视图", description = "按年月查询日程，返回按日期分组的日程列表，用于日历展示")
    public Result<Map<String, List<OaScheduleVO>>> calendar(
            @RequestParam @Parameter(description = "年份，如2026") Integer year,
            @RequestParam @Parameter(description = "月份，如5") Integer month) {
        // ...
    }
```

### 5.4 CreateDTO @Schema 注解示例

```java
@Data
public class OaScheduleCreateDTO {
    @Schema(description = "日程标题", example = "P2-008模块评审会议", required = true)
    @NotBlank private String title;

    @Schema(description = "日程日期", example = "2026-05-26", required = true)
    @NotNull private LocalDate scheduleDate;

    @Schema(description = "开始时间", example = "2026-05-26T09:00:00", required = true)
    @NotNull private LocalDateTime startTime;

    @Schema(description = "结束时间", example = "2026-05-26T10:30:00", required = true)
    @NotNull private LocalDateTime endTime;

    @Schema(description = "地点", example = "3号会议室")
    private String location;

    @Schema(description = "提醒标志：0不提醒/1提前5分钟/2提前15分钟/3提前30分钟/4提前1小时", example = "2")
    private Integer remindFlag;

    @Schema(description = "自定义提醒分钟数", example = "15")
    private Integer remindMinutes;

    @Schema(description = "日历分类颜色", example = "#1890FF")
    private String color;
}
```

### 5.5 必加注解清单
- 类 `@Tag(name, description)` -- name 格式：`OA办公-日程管理`
- 方法 `@Operation(summary, description)` -- summary 简明，description 含业务规则
- 参数 `@Parameter(description)` -- 路径参数和查询参数
- 字段 `@Schema(description, example, required)` -- example 使用业务真实样例值，禁止中文

### 5.6 验证
- 访问 /doc.html 查看 Knife4j 分组 `OA办公-日程管理`
- 所有端点（CRUD + 专属）已完整展示
- 在线 Try it out 可用，请求参数 example 值已预填

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/oa/controller/OaScheduleController.java | Controller 注解完善 |
| 2 | src/main/java/com/erp/oa/dto/OaScheduleCreateDTO.java | CreateDTO @Schema 注解 |
| 3 | src/main/java/com/erp/oa/dto/OaScheduleUpdateDTO.java | UpdateDTO @Schema 注解 |
| 4 | src/main/java/com/erp/oa/vo/OaScheduleVO.java | VO @Schema 注解 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | @Tag(name="OA办公-日程管理") 正确展示 | /doc.html 分组列表 |
| 2 | 所有方法 @Operation(summary, description) 完整 | /doc.html 接口列表 |
| 3 | 所有 DTO 字段 @Schema(description, example) 完整 | /doc.html 模型查看 |
| 4 | example 值为日程业务真实样例（非通用占位符） | 在线 Try it out 预填值 |
| 5 | 在线调试 Try it out 全部可用 | 逐个接口测试 |
| 6 | @Parameter 注解在路径/查询参数上完整 | 代码审查 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ @Schema example 禁止使用中文，使用英文/数字/拼音

> ⚠️ @Tag name 格式必须为 `OA办公-日程管理`，禁止使用泛型名称

> ⚠️ example 值需符合字段类型（日期用 ISO 格式、数字用数字字符串）

> ⚠️ [日程] @Schema example 日期时间格式需符合 ISO 8601

> ⚠️ [日程] remind_flag 的 @Schema description 需列出所有枚举含义

# P2-008-001-004-001-003 补充接口文档注解

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-008-001-004-001-003 |
| 任务名称 | 补充接口文档注解 |
| 所属模块 | P2-008 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

为 OaMeetingRoomController 及会议室相关 DTO/VO 补充 Knife4j 接口文档注解：类 @Tag(name="OA办公-会议室管理")；每个方法 @Operation(summary+description)；路径/查询参数 @Parameter；DTO/VO 字段 @Schema(description+example+required)，example 值为会议室业务真实样例

## 三、前置依赖

### 3.1 前置任务

- P2-008-001-004-001 会议室Entity/Mapper/Service/Controller开发（父任务）
- P2-008-001-004-001-002 实现接口方法（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-API接口规范 | API接口设计与RESTful规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 数据表：oa_meeting_room（会议室表）
> - API基础路径：/api/oa/meeting-room
> - Controller：OaMeetingRoomController
> - 创建DTO：OaMeetingRoomCreateDTO

### 5.1 Controller 类注解

```java
@Slf4j
@RestController
@RequestMapping("/api/oa/meeting-room")
@Tag(name = "OA办公-会议室管理", description = "会议室管理相关接口，含CRUD及会议室专属操作")
public class OaMeetingRoomController {
    // ...
}
```

### 5.2 标准 CRUD 方法注解示例

```java
@PostMapping("/create")
@Operation(summary = "创建会议室", description = "创建新的会议室记录，返回创建后的完整VO")
public Result<OaMeetingRoomVO> create(
        @RequestBody @Valid @Parameter(description = "会议室创建参数") OaMeetingRoomCreateDTO dto) {
    // ...
}

@PutMapping("/update")
@Operation(summary = "修改会议室", description = "修改会议室记录，需携带id字段，使用乐观锁")
public Result<Boolean> update(
        @RequestBody @Valid @Parameter(description = "会议室修改参数") OaMeetingRoomUpdateDTO dto) {
    // ...
}

@DeleteMapping("/delete/{id}")
@Operation(summary = "删除会议室", description = "逻辑删除会议室记录，设置 is_deleted=1")
public Result<Boolean> delete(
        @PathVariable @Parameter(description = "会议室ID") Long id) {
    // ...
}

@GetMapping("/page")
@Operation(summary = "分页查询会议室", description = "按条件分页查询会议室列表，支持多字段筛选")
public Result<Page<OaMeetingRoomListVO>> page(OaMeetingRoomQueryDTO query) {
    // ...
}

@GetMapping("/detail/{id}")
@Operation(summary = "会议室详情", description = "根据ID查询会议室完整信息")
public Result<OaMeetingRoomVO> detail(
        @PathVariable @Parameter(description = "会议室ID") Long id) {
    // ...
}
```

### 5.3 会议室专属端点注解示例

```java
    @GetMapping("/available")
    @Operation(summary = "查询会议室可用时段", description = "按日期查询各会议室未被预订的时间段，用于会议安排")
    public Result<List<OaMeetingRoomSlotVO>> available(
            @RequestParam @Parameter(description = "查询日期，格式 yyyy-MM-dd") String date) {
        // ...
    }
```

### 5.4 CreateDTO @Schema 注解示例

```java
@Data
public class OaMeetingRoomCreateDTO {
    @Schema(description = "会议室名称", example = "3号会议室", required = true)
    @NotBlank private String roomName;

    @Schema(description = "楼层", example = "3")
    private Integer floor;

    @Schema(description = "容纳人数", example = "20", required = true)
    @NotNull @Min(1) private Integer capacity;

    @Schema(description = "设备列表JSON", example = "[\"projector\",\"whiteboard\",\"video-conference\"]")
    private List<String> equipment;

    @Schema(description = "启用状态：0停用/1启用", example = "1")
    private Integer enableFlag;
}
```

### 5.5 必加注解清单
- 类 `@Tag(name, description)` -- name 格式：`OA办公-会议室管理`
- 方法 `@Operation(summary, description)` -- summary 简明，description 含业务规则
- 参数 `@Parameter(description)` -- 路径参数和查询参数
- 字段 `@Schema(description, example, required)` -- example 使用业务真实样例值，禁止中文

### 5.6 验证
- 访问 /doc.html 查看 Knife4j 分组 `OA办公-会议室管理`
- 所有端点（CRUD + 专属）已完整展示
- 在线 Try it out 可用，请求参数 example 值已预填

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/oa/controller/OaMeetingRoomController.java | Controller 注解完善 |
| 2 | src/main/java/com/erp/oa/dto/OaMeetingRoomCreateDTO.java | CreateDTO @Schema 注解 |
| 3 | src/main/java/com/erp/oa/dto/OaMeetingRoomUpdateDTO.java | UpdateDTO @Schema 注解 |
| 4 | src/main/java/com/erp/oa/vo/OaMeetingRoomVO.java | VO @Schema 注解 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | @Tag(name="OA办公-会议室管理") 正确展示 | /doc.html 分组列表 |
| 2 | 所有方法 @Operation(summary, description) 完整 | /doc.html 接口列表 |
| 3 | 所有 DTO 字段 @Schema(description, example) 完整 | /doc.html 模型查看 |
| 4 | example 值为会议室业务真实样例（非通用占位符） | 在线 Try it out 预填值 |
| 5 | 在线调试 Try it out 全部可用 | 逐个接口测试 |
| 6 | @Parameter 注解在路径/查询参数上完整 | 代码审查 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ @Schema example 禁止使用中文，使用英文/数字/拼音

> ⚠️ @Tag name 格式必须为 `OA办公-会议室管理`，禁止使用泛型名称

> ⚠️ example 值需符合字段类型（日期用 ISO 格式、数字用数字字符串）

> ⚠️ [会议室] equipment 为 JSON 数组，@Schema example 使用 JSON 字符串

> ⚠️ [会议室] room_code 为自动生成，CreateDTO 中不包含此字段

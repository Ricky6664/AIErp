# P2-009-002-009-001-001 编写DDL+Entity/Mapper

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-009-002-009-001-001 |
| 任务名称 | 编写DDL+Entity/Mapper |
| 所属模块 | P2-009 |
| 优先级 | P2 |
| 任务类型 | DDL/数据库建表 |

## 二、任务目标

编写DDL+Entity+Mapper：Flyway迁移脚本+Entity(@TableName+@TableId+@TableField+公共字段)+Mapper接口(extends BaseMapperX)+Mapper.xml(resultMap+SQL)

## 三、前置依赖

### 3.1 前置任务

- P2-009-002-009-001 后端开发（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-数据库规范 | 数据库字段映射与模型规范约束 |

## 五、详细开发规格

> **📦 本任务模块上下文**（来源：P2-009模块开发指南）
> - 本模块涉及数据表：参见模块开发指南
> - 本模块涉及API：/api/ai/chat, /api/ai/qa, /api/ai/data-analysis, /api/ai/smart-input, /api/ai/smart-query
> - 本模块业务规则：本模块无模块级专属约束，遵循全局规范。AI对话采用流式SSE响应，需处理上下文窗口裁剪与Token计数；智能问答基于RAG检索增强生成，需文档向量化与语义检索；数据智能分析需严格SQL安全校验（仅允许SELECT，禁止DROP/DELETE/UPDATE）；API Key等敏感配置需加密存储并支持运行时热更新。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 Step 1 - DDL建表(Flyway)
```sql
-- V2026052601__create_ai_d_d_l.sql
CREATE TABLE ai_d_d_l (
    id BIGINT PRIMARY KEY,
    code VARCHAR(50) NOT NULL,
    name VARCHAR(100) NOT NULL,
    status INT DEFAULT 0,
    amount decimal(18,8),
    biz_date DATE,
    dept_id BIGINT,
    create_by BIGINT,
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    update_by BIGINT,
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    tenant_id BIGINT NOT NULL,
    is_deleted INT DEFAULT 0,
    version INT DEFAULT 0
);
CREATE UNIQUE INDEX uk_d_d_l_code ON ai_d_d_l(code, tenant_id) WHERE is_deleted = 0;
CREATE INDEX idx_d_d_l_dept ON ai_d_d_l(dept_id);
```

### 5.2 Step 2 - Entity实体类
参照"编写Entity类"任务规格

### 5.3 Step 3 - Mapper接口+XML
参照"编写Mapper接口"+"编写Mapper.xml"任务规格

### 5.4 验证
- flyway migrate无报错
- Entity字段与表一致
- Mapper SQL正确
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/resources/db/migration/V2026052601__create_ai_d_d_l.sql | Flyway DDL |
| 2 | src/main/java/com/erp/ai/entity/DDLEntity.java | Entity |
| 3 | src/main/java/com/erp/ai/mapper/DDLMapper.java | Mapper接口 |
| 4 | src/main/resources/mapper/ai/DDLMapper.xml | Mapper XML |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | Flyway迁移成功 | flyway migrate |
| 2 | Entity与DDL一致 | 逐字段 |
| 3 | Mapper SQL正确 | 单元测试 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ Flyway版本号唯一

> ⚠️ 回滚脚本注释

> ⚠️ 唯一索引含is_deleted

> ⚠️ [AI模块] API Key AES加密存储

> ⚠️ [AI模块] Token计数精确避免超限

> ⚠️ [AI模块] AI服务降级策略

> ⚠️ [AI模块] 流式响应中断优雅处理

> ⚠️ [AI模块] SQL安全仅允许SELECT

# P2-009-002-008-002-002 实现测试连接

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-009-002-008-002-002 |
| 任务名称 | 实现测试连接 |
| 所属模块 | P2-009 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现测试连接：调用POST /api/ai/config/test→验证API Key+网络→显示成功/失败+响应时间

## 三、前置依赖

### 3.1 前置任务

- P2-009-002-008-002 前端开发（父任务）
- P2-009-002-008-002-001 实现配置表单（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：ai_config(AI配置表), ai_prompt_template(提示词模板表), ai_knowledge_base(知识库表), ai_knowledge_document(知识库文档表), ai_conversation_session(会话表), ai_conversation_message(消息表)
> - 涉及API：/api/ai/chat/*, /api/ai/config/*, /api/ai/knowledge/*, /api/ai/prompt/*, /api/ai/analysis/*
### 5.1 测试连接
```typescript
const handleTest = async () => {
  testing.value = true
  try {
    const res = await api.post('/ai/config/test', form)
    if (res.code === 200) {
      ElMessage.success(`连接成功，延迟${res.data.latency}ms`)
    } else {
      ElMessage.error(res.message)
    }
  } finally {
    testing.value = false
  }
}
```
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/ai/config/index.vue | 测试连接 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 测试连接正常 | 点击 |
| 2 | 失败提示 | 无效Key |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ @TableField勿遗漏

> ⚠️ 事务边界准确避免大事务

> ⚠️ 异常统一BusinessException

> ⚠️ [AI模块] API Key AES加密存储

> ⚠️ [AI模块] Token计数精确避免超限

> ⚠️ [AI模块] AI服务降级策略

> ⚠️ [AI模块] 流式响应中断优雅处理

> ⚠️ [AI模块] SQL安全仅允许SELECT

# P2-003-002-002-001-001 新增流程联调

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-003-002-002-001-001 |
| 任务名称 | 新增流程联调 |
| 所属模块 | P2-003 |
| 优先级 | P2 |
| 任务类型 | 前端联调验证 |

## 二、任务目标

新增流程联调：打开表单→填写→POST /create→成功关闭+刷新父列表；错误定位字段

## 三、前置依赖

### 3.1 前置任务

- P2-003-002-002-001 调汇参数配置+调汇执行+调汇结果展示+联调验证（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：fin_accounting_period(会计期间表), fin_period_close(结账记录表), fin_exchange_gain_loss(期末调汇表), fin_exchange_gain_loss_detail(调汇明细), fin_period_carry_forward(期末结转表)
> - 涉及API：/api/period/close/*, /api/period/unclose/*, /api/period/exchange/*, /api/period/carry-forward/*
### 5.1 新增流程
1. 父页面[新增]→打开弹窗(mode='create')
2. 表单初始化空+默认值
3. 填写→校验+联动
4. [保存]→formRef.validate()→api.create(form)
5. 成功→ElMessage.success+close()+emit('refresh')
6. 失败→定位错误字段

### 5.2 错误处理
- 字段校验：滚动到第一个错误
- 400：字段错误
- 409：编号已存在
- 500：系统异常
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/period/components/EditForm.vue | 新增流程 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 新增弹窗打开 | 点击 |
| 2 | 校验生效 | 空提交 |
| 3 | 成功关闭+刷新 | 完整流程 |
| 4 | 错误定位字段 | 触发错误 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 按钮loading防重复

> ⚠️ 错误滚动到第一个字段

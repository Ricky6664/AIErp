# P2-011-002-001-001-002 搜索字段组件渲染

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-011-002-001-001-002 |
| 任务名称 | 搜索字段组件渲染 |
| 所属模块 | P2-011 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现搜索字段组件渲染：el-form+el-row/el-col栅格布局，labelWidth统一100px，搜索字段按业务重要性从左到右排列，超过3个字段提供展开收起功能，搜索/重置按钮右对齐

## 三、前置依赖

### 3.1 前置任务

- P2-011-002-001-001 搜索条件区+数据表格区+分类筛选+联调验证（父任务）
- P2-011-002-001-001-001 搜索表单布局（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：report(报表主表), report_category(报表分类表), report_catalog(报表目录表), report_favorite(报表收藏表), report_screen_config(数据大屏配置表)
> - 涉及API：/api/report/*, /api/report/category/*, /api/report/data/*, /api/report/designer/*, /api/report/screen/*

### 5.1 字段组件映射

| 字段类型 | 组件 | 默认值 | 说明 |
|---------|------|--------|------|
| 文本 | el-input | "" | 支持placeholder |
| 字典 | DictSelect | null | 自动加载字典项 |
| 日期 | el-date-picker | null | type="daterange" |
| 级联 | el-cascader | [] | 多级选择 |

### 5.2 字典加载

- DictSelect组件通过dict_type属性自动调用/api/sys/dict/items/{dict_type}
- 选项缓存到Pinia store，避免重复请求

### 5.3 验证

1. 各字段组件渲染正确
2. 字典选项加载正确
3. 默认值生效

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/report/搜索字段组件渲染.vue | Vue页面组件 |
| 2 | src/api/report/index.ts | API调用层 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 路由注册正确，页面可访问 | 浏览器访问 |
| 2 | 组件渲染无报错 | Chrome DevTools |
| 3 | 数据绑定正确（v-model/:prop） | 代码review |
| 4 | 交互逻辑（搜索/分页/CRUD）正常 | 手动操作验证 |
| 5 | 与后端接口联调通过 | Knife4j+前端联调 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 权限控制必须使用v-permission指令，禁止手动判断角色

> ⚠️ 删除/批量操作必须二次确认（ElMessageBox.confirm）

> ⚠️ 表格操作列fixed="right"防止横向滚动被遮挡

> ⚠️ 表单离开未保存提示——beforeRouteLeave守卫拦截

> ⚠️ 接口请求必须携带loading状态，防止重复提交

> ⚠️ [报表中心模块] SQL查询必须经过白名单校验，禁止INSERT/UPDATE/DELETE/DROP/ALTER/TRUNCATE等写操作

> ⚠️ [报表中心模块] 所有动态SQL必须自动注入tenant_id和is_deleted条件，确保多租户隔离和逻辑删除

> ⚠️ [报表中心模块] 报表查询必须设置30秒超时限制，超时自动终止并返回友好提示

# P2-001-002-004-001-002 主表字段组件渲染

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-001-002-004-001-002 |
| 任务名称 | 主表字段组件渲染 |
| 所属模块 | P2-001 |
| 优先级 | P2 |
| 任务类型 | 前端表单 |

## 二、任务目标

主表字段组件渲染：el-input/el-input-number(precision=2)/el-select(字典)/业务选择器/el-upload/el-date-picker(value-format)

## 三、前置依赖

### 3.1 前置任务

- P2-001-002-004-001 主表字段+从表明细区（费用项目选择+金额+发票附件）+预算校验提示+联调验证（父任务）
- P2-001-002-004-001-001 主表表单布局（前序兄弟任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：expense_budget(费用预算单), expense_budget_detail(预算明细), expense_application(费用申请单), expense_application_detail(申请明细), expense_reimbursement(费用报销单), expense_reimbursement_detail(报销明细)
> - 涉及API：/api/expense/claim/*, /api/expense/application/*, /api/expense/budget/*, /api/expense/budget/execution/*
### 5.1 字段组件
- 文本：el-input(max length)
- 数值：el-input-number(precision精度)
- 下拉：el-select(远程搜索可选)
- 日期：el-date-picker(value-format统一)
- 业务选择器：自定义弹窗
- 附件：el-upload(限制类型/大小)

### 5.2 示例
```vue
<el-form-item label="金额" prop="amount">
  <el-input-number v-model="form.amount" :min="0" :precision="2"/>
</el-form-item>
<el-form-item label="部门" prop="deptId">
  <DeptSelector v-model="form.deptId" @change="handleDeptChange"/>
</el-form-item>
```
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/expense/components/EditForm.vue | 表单字段 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 各组件渲染 | 浏览器 |
| 2 | 数值精度 | 输入测试 |
| 3 | 选择器正常 | 点击 |
| 4 | 附件上传 | 上传测试 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ el-input-number precision

> ⚠️ el-date-picker value-format

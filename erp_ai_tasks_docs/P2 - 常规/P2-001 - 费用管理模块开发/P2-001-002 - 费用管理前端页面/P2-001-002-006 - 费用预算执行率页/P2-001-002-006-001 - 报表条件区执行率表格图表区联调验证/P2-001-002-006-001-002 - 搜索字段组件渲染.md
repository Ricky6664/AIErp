# P2-001-002-006-001-002 搜索字段组件渲染

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-001-002-006-001-002 |
| 任务名称 | 搜索字段组件渲染 |
| 所属模块 | P2-001 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

搜索字段组件渲染：el-input(编号/名称)+el-select(状态/类型+字典加载)+el-date-picker(日期范围)+DeptSelector/UserSelector(业务选择器弹窗)

## 三、前置依赖

### 3.1 前置任务

- P2-001-002-006-001 报表条件区+执行率表格+图表区+联调验证（父任务）
- P2-001-002-006-001-001 搜索表单布局（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：expense_budget(费用预算单), expense_budget_detail(预算明细), expense_application(费用申请单), expense_application_detail(申请明细), expense_reimbursement(费用报销单), expense_reimbursement_detail(报销明细)
> - 涉及API：/api/expense/claim/*, /api/expense/application/*, /api/expense/budget/*, /api/expense/budget/execution/*
### 5.1 字段组件映射

| 字段类型 | 组件 | 属性 |
|---------|------|------|
| 编号/名称 | el-input | placeholder, clearable |
| 状态/类型 | el-select | 字典加载, clearable |
| 日期范围 | el-date-picker | type=daterange, value-format |
| 部门 | DeptSelector | 业务选择器弹窗 |
| 人员 | UserSelector | 业务选择器弹窗 |

### 5.2 字典加载
```typescript
const statusOptions = ref([])
onMounted(async () => {
  statusOptions.value = await dictApi.getDictItems('expense_status')
})
```
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/expense/components/SearchForm.vue | 搜索字段渲染 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 各组件渲染正确 | 浏览器 |
| 2 | 字典加载完整 | F12 Network |
| 3 | 业务选择器正常 | 点击 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 字典用dictApi统一接口

> ⚠️ el-select clearable

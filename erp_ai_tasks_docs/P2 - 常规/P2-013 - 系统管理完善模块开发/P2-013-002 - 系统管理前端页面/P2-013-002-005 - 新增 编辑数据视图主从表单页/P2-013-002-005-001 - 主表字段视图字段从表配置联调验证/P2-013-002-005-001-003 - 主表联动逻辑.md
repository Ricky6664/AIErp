# P2-013-002-005-001-003 主表联动逻辑

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-013-002-005-001-003 |
| 任务名称 | 主表联动逻辑 |
| 所属模块 | P2-013 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现主表联动逻辑：基于Vue3+Element Plus+TypeScript开发，调用/api/sys/code-rule接口，实现完整交互（搜索/分页/CRUD），响应式布局适配不同屏幕

## 三、前置依赖

### 3.1 前置任务

- P2-013-002-005-001 主表字段+视图字段从表配置+联调验证（父任务）
- P2-013-002-005-001-002 主表字段组件渲染（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：sys_code_rule(编码规则表), sys_code_rule_detail(编码段配置), sys_data_view(数据视图表), sys_data_view_field(视图字段), sys_operation_log(操作日志), sys_login_log(登录日志), sys_notice(系统公告), sys_doc_config(单据配置)
> - 涉及API：/api/sys/code-rule/*, /api/sys/data-view/*, /api/sys/log/*, /api/sys/notice/*, /api/sys/doc-config/*

### 5.1 页面结构

```
主表联动逻辑.vue
├── 搜索区 (SearchForm)
├── 操作按钮区 (v-permission控制)
├── 数据表格 (el-table)
├── 分页 (el-pagination)
└── 弹窗表单 (el-dialog + Form)
```

### 5.2 数据交互

- GET /api/sys/code-rule/page 分页查询
- POST /api/sys/code-rule/create 新增
- PUT /api/sys/code-rule/update/{id} 修改
- DELETE /api/sys/code-rule/delete/{id} 删除

### 5.3 关键逻辑

1. 搜索条件重置自动刷新列表
2. 表单提交后关闭弹窗刷新列表
3. 删除操作二次确认

### 5.4 验证

1. CRUD全流程正常
2. 搜索分页联动正确
3. 权限控制生效

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/system/主表联动逻辑.vue | Vue页面组件 |
| 2 | src/api/system/index.ts | API调用层 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 路由注册正确，页面可访问 | 浏览器访问 |
| 2 | 组件渲染无报错 | Chrome DevTools |
| 3 | 数据绑定正确（v-model/:prop） | 代码review |
| 4 | 交互逻辑（搜索/分页/CRUD）正常 | 手动操作验证 |
| 5 | 与后端接口联调通过 | Knife4j+前端联调 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 权限控制必须使用v-permission指令，禁止手动判断角色

> ⚠️ 删除/批量操作必须二次确认（ElMessageBox.confirm）

> ⚠️ 表格操作列fixed="right"防止横向滚动被遮挡

> ⚠️ 表单离开未保存提示——beforeRouteLeave守卫拦截

> ⚠️ 接口请求必须携带loading状态，防止重复提交

> ⚠️ [系统管理模块] 系统参数优先级链：租户级参数 > 全局默认参数，查询时先查租户级再回退全局

> ⚠️ [系统管理模块] 参数变更后仅对新操作生效，不影响已有业务单据，需在参数值中记录生效时间

> ⚠️ [系统管理模块] 字典数据缓存至Redis（TTL 24h），字典项增删改时必须清除对应dict_type的缓存

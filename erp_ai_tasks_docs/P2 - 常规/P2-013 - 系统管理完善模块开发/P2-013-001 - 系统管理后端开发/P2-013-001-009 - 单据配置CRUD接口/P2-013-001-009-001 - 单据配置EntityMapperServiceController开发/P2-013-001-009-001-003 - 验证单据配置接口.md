# P2-013-001-009-001-003 验证单据配置接口

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-013-001-009-001-003 |
| 任务名称 | 验证单据配置接口 |
| 所属模块 | P2-013 |
| 优先级 | P2 |
| 任务类型 | 配置/基础设施 |

## 二、任务目标

编写验证单据配置接口：配置项定义（Key命名规范sys.feature.name）+默认值+环境差异（dev/test/prod）+配置说明注释，敏感配置加密存储

## 三、前置依赖

### 3.1 前置任务

- P2-013-001-009-001 单据配置Entity/Mapper/Service/Controller开发（父任务）
- P2-013-001-009-001-002 编写Service+Controller（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-配置管理规范 | 配置Key命名与环境差异规范 |
## 五、详细开发规格

> **📦 本任务模块上下文**（来源：P2-013模块开发指南）
> - 本模块涉及数据表：参见模块开发指南
> - 本模块涉及API：/api/system/dashboard, /api/system/dict, /api/system/code-rule, /api/system/data-view, /api/system/operation-log
> - 本模块业务规则：本模块部分功能已在V10.0版本中迁移至P0阶段：; 数据字典管理：后端CRUD已迁移至P0-005-016-001，前端页面已迁移至P0-005-016-002。P2阶段仅保留数据字典增强功能（字典导入模板管理、字典数据批量导入导出增强、字典引用查询）。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 配置项清单

| 配置Key | 默认值 | 说明 | 环境差异 |
|---------|--------|------|---------|
| sys.feature.enabled | true | 功能开关 | dev/test=true, prod按需 |
| sys.cache.ttl | 3600 | 缓存TTL(秒) | 各环境一致 |
| sys.api.timeout | 30000 | 接口超时(ms) | prod=30000 |

### 5.2 配置类定义

```java
@Configuration
@ConfigurationProperties(prefix = "sys")
@Data
public class SysProperties {
    private Feature feature;
    private Cache cache;
    // ...
}
```

### 5.3 验证

1. 配置Key命名符合 {module}.{feature}.{item} 规范
2. 各环境配置文件(dev/test/prod)完整
3. 敏感配置加密存储

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/sys/config/SysProperties.java | 配置类 |
| 2 | src/main/resources/application-{env}.yml | 配置文件(dev/test/prod) |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 配置Key符合{module}.{feature}.{item}命名 | 代码review |
| 2 | dev/test/prod三套配置文件完整 | 代码review |
| 3 | @ConfigurationProperties绑定正确 | 应用启动检查 |
| 4 | 敏感配置加密存储 | 代码review |
| 5 | 配置变更即时生效 | 修改配置验证 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id条件

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE而非DELETE

> ⚠️ 敏感配置（数据库密码/API密钥）必须加密存储，禁止明文

> ⚠️ 配置变更需考虑对已有业务的影响——新配置仅对新操作生效

> ⚠️ [系统管理模块] 系统参数优先级链：租户级参数 > 全局默认参数，查询时先查租户级再回退全局

> ⚠️ [系统管理模块] 参数变更后仅对新操作生效，不影响已有业务单据，需在参数值中记录生效时间

> ⚠️ [系统管理模块] 字典数据缓存至Redis（TTL 24h），字典项增删改时必须清除对应dict_type的缓存

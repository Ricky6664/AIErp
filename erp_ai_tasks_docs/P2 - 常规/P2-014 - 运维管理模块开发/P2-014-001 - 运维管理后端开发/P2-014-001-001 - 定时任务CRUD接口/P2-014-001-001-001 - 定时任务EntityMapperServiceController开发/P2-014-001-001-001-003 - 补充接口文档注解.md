# P2-014-001-001-001-003 补充接口文档注解

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-014-001-001-001-003 |
| 任务名称 | 补充接口文档注解 |
| 所属模块 | P2-014 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

补充OpsController接口文档注解：类级@Tag(name="运维管理")，方法级@Operation(summary+description)，参数@Parameter，DTO/VO字段@Schema(description+example)，确保Knife4j文档页完整展示且在线调试可用

## 三、前置依赖

### 3.1 前置任务

- P2-014-001-001-001 定时任务Entity/Mapper/Service/Controller开发（父任务）
- P2-014-001-001-001-002 实现接口方法（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-API接口规范 | API接口设计与RESTful规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：ops_scheduled_task(定时任务表), ops_task_execution_log(执行日志), ops_data_backup(数据备份表), ops_alert_rule(告警规则表), ops_alert_log(告警日志), ops_server_monitor(服务器监控)
> - 涉及API：/api/ops/task/*, /api/ops/backup/*, /api/ops/alert/*, /api/ops/monitor/*, /api/ops/online-user/*

### 5.1 类级注解

```java
@Tag(name = "运维管理")
```

### 5.2 方法级注解

```java
@PostMapping("/create")
@Operation(summary = "新增运维管理", description = "新增运维管理记录")
@SaCheckPermission("ops:add")
```

### 5.3 DTO/VO字段注解

```java
@Schema(description = "名称", example = "示例名称", required = true)
@NotBlank(message = "名称不能为空")
private String name;
```

### 5.4 验证

1. Knife4j文档页所有接口均有描述
2. DTO/VO字段@Schema完整
3. 在线调试可正常调用

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/ops/controller/OpsController.java | Controller(补充注解) |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 类级@Tag注解存在且name准确 | 代码review |
| 2 | 每个方法@Operation(summary+description) | 代码review |
| 3 | DTO/VO字段@Schema(description+example) | 代码review |
| 4 | Knife4j文档页完整展示所有接口 | 访问Knife4j页面 |
| 5 | 在线调试可正常调用接口 | Knife4j在线调试 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id条件

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE而非DELETE

> ⚠️ 接口路径必须符合 `/api/{module}/{resource}` 规范，复数形式

> ⚠️ @RequestBody必须配合@Valid使用，否则DTO校验不生效

> ⚠️ GET请求复杂查询参数使用@ModelAttribute而非@RequestParam

> ⚠️ [运维管理模块] 定时任务CRON表达式必须做合法性校验，防止无效表达式导致调度器异常

> ⚠️ [运维管理模块] 任务执行日志需设置保留天数，超期日志自动归档或清理防止日志表膨胀

> ⚠️ [运维管理模块] 监控数据采集使用异步线程池，采集失败时不得影响主业务

# P2-014-002-005-001-001 新增流程联调

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-014-002-005-001-001 |
| 任务名称 | 新增流程联调 |
| 所属模块 | P2-014 |
| 优先级 | P2 |
| 任务类型 | 前端联调验证 |

## 二、任务目标

实现新增流程联调：前后端接口联调验证，覆盖CRUD全流程+搜索分页+异常场景，检查请求参数/响应数据/错误码/Loading状态，确保与Knife4j文档一致

## 三、前置依赖

### 3.1 前置任务

- P2-014-002-005-001 CPU/内存/磁盘实时曲线+JVM状态+数据库连接池+告警状态+联调验证（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：ops_scheduled_task(定时任务表), ops_task_execution_log(执行日志), ops_data_backup(数据备份表), ops_alert_rule(告警规则表), ops_alert_log(告警日志), ops_server_monitor(服务器监控)
> - 涉及API：/api/ops/task/*, /api/ops/backup/*, /api/ops/alert/*, /api/ops/monitor/*, /api/ops/online-user/*

### 5.1 联调用例清单

| 序号 | 场景 | 操作 | 预期结果 |
|:---:|------|------|---------|
| 1 | 列表加载 | 进入页面 | 调用GET /api/ops/task/page返回分页数据并渲染 |
| 2 | 搜索过滤 | 输入关键字点搜索 | 请求参数含keyword，列表刷新 |
| 3 | 新增提交 | 填写表单提交 | 调用POST /create返回新ID |
| 4 | 参数校验失败 | 提交空必填 | 表单显示校验错误提示 |
| 5 | 接口异常 | 模拟500 | 全局提示"系统异常" |

### 5.2 请求/响应检查

- 请求参数与Knife4j文档一致
- 响应code=200时展示data，非200时展示message
- Loading状态在请求期间显示

### 5.3 验证

1. 全部联调用例通过
2. 异常场景处理得当
3. 控制台无报错

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/ops/新增流程联调.vue | Vue页面组件 |
| 2 | src/api/ops/index.ts | API调用层 |
| 3 | tests/e2e/{vue_dir}.spec.ts | 联调测试记录 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | CRUD全流程联调通过 | 手动操作验证 |
| 2 | 搜索/分页/排序联动正确 | 手动操作验证 |
| 3 | 异常场景（401/403/500）提示友好 | 模拟异常 |
| 4 | 控制台无报错 | Chrome DevTools |
| 5 | Loading状态正确展示 | 慢网络模拟 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 权限控制必须使用v-permission指令，禁止手动判断角色

> ⚠️ 删除/批量操作必须二次确认（ElMessageBox.confirm）

> ⚠️ 表格操作列fixed="right"防止横向滚动被遮挡

> ⚠️ 表单离开未保存提示——beforeRouteLeave守卫拦截

> ⚠️ 接口请求必须携带loading状态，防止重复提交

> ⚠️ [运维管理模块] 定时任务CRON表达式必须做合法性校验，防止无效表达式导致调度器异常

> ⚠️ [运维管理模块] 任务执行日志需设置保留天数，超期日志自动归档或清理防止日志表膨胀

> ⚠️ [运维管理模块] 监控数据采集使用异步线程池，采集失败时不得影响主业务

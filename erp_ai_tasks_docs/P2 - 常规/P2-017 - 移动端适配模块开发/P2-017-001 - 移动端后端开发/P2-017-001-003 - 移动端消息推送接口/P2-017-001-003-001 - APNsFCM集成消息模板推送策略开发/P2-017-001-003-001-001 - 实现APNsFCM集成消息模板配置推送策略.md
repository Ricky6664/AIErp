# P2-017-001-003-001-001 实现APNs/FCM集成+消息模板配置+推送策略

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-017-001-003-001-001 |
| 任务名称 | 实现APNs/FCM集成+消息模板配置+推送策略 |
| 所属模块 | P2-017 |
| 优先级 | P2 |
| 任务类型 | 配置/基础设施 |

## 二、任务目标

编写实现APNs/FCM集成+消息模板配置+推送策略：配置项定义（Key命名规范mobile.feature.name）+默认值+环境差异（dev/test/prod）+配置说明注释，敏感配置加密存储

## 三、前置依赖

### 3.1 前置任务

- P2-017-001-003-001 APNs/FCM集成+消息模板+推送策略开发（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-配置管理规范 | 配置Key命名与环境差异规范 |
## 五、详细开发规格

> **📦 本任务模块上下文**（来源：P2-017模块开发指南）
> - 本模块涉及数据表：参见模块开发指南
> - 本模块涉及API：/api/mobile/menu, /api/mobile/approval, /api/mobile/push, /api/mobile/business, /api/mobile/report
> - 本模块业务规则：本模块包含移动端专属约束，基于UniApp跨平台框架（iOS/Android/H5）开发：; 跨平台适配：移动端页面基于UniApp开发，需同时适配iOS、Android与H5三种平台；移动端首页采用H5适配方案，包含快捷功能入口、待办提醒与KPI卡片。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 配置项清单

| 配置Key | 默认值 | 说明 | 环境差异 |
|---------|--------|------|---------|
| mobile.feature.enabled | true | 功能开关 | dev/test=true, prod按需 |
| mobile.cache.ttl | 3600 | 缓存TTL(秒) | 各环境一致 |
| mobile.api.timeout | 30000 | 接口超时(ms) | prod=30000 |

### 5.2 配置类定义

```java
@Configuration
@ConfigurationProperties(prefix = "mobile")
@Data
public class MobileProperties {
    private Feature feature;
    private Cache cache;
    // ...
}
```

### 5.3 验证

1. 配置Key命名符合 {module}.{feature}.{item} 规范
2. 各环境配置文件(dev/test/prod)完整
3. 敏感配置加密存储

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/mobile/config/MobileProperties.java | 配置类 |
| 2 | src/main/resources/application-{env}.yml | 配置文件(dev/test/prod) |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 配置Key符合{module}.{feature}.{item}命名 | 代码review |
| 2 | dev/test/prod三套配置文件完整 | 代码review |
| 3 | @ConfigurationProperties绑定正确 | 应用启动检查 |
| 4 | 敏感配置加密存储 | 代码review |
| 5 | 配置变更即时生效 | 修改配置验证 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id条件

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE而非DELETE

> ⚠️ 敏感配置（数据库密码/API密钥）必须加密存储，禁止明文

> ⚠️ 配置变更需考虑对已有业务的影响——新配置仅对新操作生效

> ⚠️ [移动端模块] 响应式设计断点：320px(手机)/768px(平板)/1024px(桌面)，使用CSS媒体查询适配

> ⚠️ [移动端模块] 离线数据同步时需处理冲突（乐观锁version校验），冲突时提示用户选择保留版本

> ⚠️ [移动端模块] 推送通知发送需记录推送日志（成功/失败/点击），失败时需重试机制（最多3次）

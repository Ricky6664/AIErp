# P2-017-001-001-001-003 补充接口文档注解

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-017-001-001-001-003 |
| 任务名称 | 补充接口文档注解 |
| 所属模块 | P2-017 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

补充MobileController接口文档注解：类级@Tag(name="移动端管理")，方法级@Operation(summary+description)，参数@Parameter，DTO/VO字段@Schema(description+example)，确保Knife4j文档页完整展示且在线调试可用

## 三、前置依赖

### 3.1 前置任务

- P2-017-001-001-001 移动端菜单Entity/Mapper/Service/Controller开发（父任务）
- P2-017-001-001-001-002 实现接口方法（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-API接口规范 | API接口设计与RESTful规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：mobile_menu(移动端菜单表), mobile_version(版本管理表), mobile_config(移动端配置表), mobile_push_message(推送消息表)
> - 涉及API：/api/mobile/menu/*, /api/mobile/approval/*, /api/mobile/message/*, /api/mobile/report/*, /api/mobile/doc/*

### 5.1 类级注解

```java
@Tag(name = "移动端管理")
```

### 5.2 方法级注解

```java
@PostMapping("/create")
@Operation(summary = "新增移动端", description = "新增移动端记录")
@SaCheckPermission("mobile:add")
```

### 5.3 DTO/VO字段注解

```java
@Schema(description = "名称", example = "示例名称", required = true)
@NotBlank(message = "名称不能为空")
private String name;
```

### 5.4 验证

1. Knife4j文档页所有接口均有描述
2. DTO/VO字段@Schema完整
3. 在线调试可正常调用

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/mobile/controller/MobileController.java | Controller(补充注解) |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 类级@Tag注解存在且name准确 | 代码review |
| 2 | 每个方法@Operation(summary+description) | 代码review |
| 3 | DTO/VO字段@Schema(description+example) | 代码review |
| 4 | Knife4j文档页完整展示所有接口 | 访问Knife4j页面 |
| 5 | 在线调试可正常调用接口 | Knife4j在线调试 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id条件

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE而非DELETE

> ⚠️ 接口路径必须符合 `/api/{module}/{resource}` 规范，复数形式

> ⚠️ @RequestBody必须配合@Valid使用，否则DTO校验不生效

> ⚠️ GET请求复杂查询参数使用@ModelAttribute而非@RequestParam

> ⚠️ [移动端模块] 响应式设计断点：320px(手机)/768px(平板)/1024px(桌面)，使用CSS媒体查询适配

> ⚠️ [移动端模块] 离线数据同步时需处理冲突（乐观锁version校验），冲突时提示用户选择保留版本

> ⚠️ [移动端模块] 推送通知发送需记录推送日志（成功/失败/点击），失败时需重试机制（最多3次）

# P2-005-002-000-001-001 KPI卡片+借用趋势图+逾期预警列表+联调

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-005-002-000-001-001 |
| 任务名称 | KPI卡片+借用趋势图+逾期预警列表+联调 |
| 所属模块 | P2-005 |
| 优先级 | P2 |
| 任务类型 | 前端KPI卡片 |

## 二、任务目标

实现工作台页面：KPI卡片区(待审批数/本月金额/执行率)+图表区(ECharts柱状/折线/饼图)+待办列表+快捷入口

## 三、前置依赖

### 3.1 前置任务

- P2-005-002-000-001 工作台KPI卡片区+图表区+联调验证（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：borrow_application(借用申请单), borrow_apply(借用出库单), borrow_return(借用归还单), borrow_in(借入单), borrow_in_return(借入归还单)
> - 涉及API：/api/borrow/application/*, /api/borrow/outbound/*, /api/borrow/return/*, /api/borrow/in/*
### 5.1 工作台布局
- KPI卡片(4个：待审批/本月金额/执行率/异常数)
- 图表(ECharts柱状/折线/饼图)
- 待办列表(前5条)
- 快捷入口(新建报销/申请/预算查询)

### 5.2 数据源
- KPI：GET /api/borrow/borrow/dashboard/kpi
- 图表：GET /api/borrow/borrow/dashboard/charts
- 待办：GET /api/workflow/todo?limit=5
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/borrow/workbench/index.vue | 工作台 |
| 2 | src/views/borrow/workbench/components/KpiCard.vue | KPI卡片 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | KPI数据正确 | 对照后端 |
| 2 | 图表正常 | 浏览器 |
| 3 | 待办点击跳转 | 点击 |
| 4 | 快捷入口跳转 | 点击 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ KPI缓存避免频繁请求

> ⚠️ ECharts resize

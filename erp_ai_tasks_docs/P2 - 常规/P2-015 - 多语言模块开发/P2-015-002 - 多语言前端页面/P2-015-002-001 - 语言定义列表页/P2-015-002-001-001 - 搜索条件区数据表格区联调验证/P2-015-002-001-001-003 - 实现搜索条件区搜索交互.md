# P2-015-002-001-001-003 实现搜索条件区搜索交互

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-015-002-001-001-003 |
| 任务名称 | 实现搜索条件区搜索交互 |
| 所属模块 | P2-015 |
| 优先级 | P2 |
| 任务类型 | 前端搜索表单 |

## 二、任务目标

实现搜索条件区搜索交互：el-form+el-row/el-col栅格布局，labelWidth统一100px，搜索字段按业务重要性从左到右排列，超过3个字段提供展开收起功能，搜索/重置按钮右对齐

## 三、前置依赖

### 3.1 前置任务

- P2-015-002-001-001 搜索条件区+数据表格区+联调验证（父任务）
- P2-015-002-001-001-002 搜索字段组件渲染（前序兄弟任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：i18n_language(语言定义表), i18n_translation(翻译键值表)
> - 涉及API：/api/i18n/language/*, /api/i18n/translation/*, /api/i18n/export/*, /api/i18n/import/*

### 5.1 表单布局

```vue
<el-form :model="searchForm" label-width="100px">
  <el-row :gutter="20">
    <el-col :span="6" v-for="field in searchFields" :key="field.prop">
      <el-form-item :label="field.label">
        <component :is="field.component" v-model="searchForm[field.prop]" />
      </el-form-item>
    </el-col>
  </el-row>
</el-form>
```

### 5.2 展开收起

- 默认展示前3个搜索字段
- 超出部分v-show="expanded"控制
- 展开按钮切换expanded状态

### 5.3 验证

1. 搜索表单布局符合设计稿
2. 展开收起交互正常
3. labelWidth统一100px

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/i18n/实现搜索条件区搜索交互.vue | Vue页面组件 |
| 2 | src/api/i18n/index.ts | API调用层 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 路由注册正确，页面可访问 | 浏览器访问 |
| 2 | 组件渲染无报错 | Chrome DevTools |
| 3 | 数据绑定正确（v-model/:prop） | 代码review |
| 4 | 交互逻辑（搜索/分页/CRUD）正常 | 手动操作验证 |
| 5 | 与后端接口联调通过 | Knife4j+前端联调 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 权限控制必须使用v-permission指令，禁止手动判断角色

> ⚠️ 删除/批量操作必须二次确认（ElMessageBox.confirm）

> ⚠️ 表格操作列fixed="right"防止横向滚动被遮挡

> ⚠️ 表单离开未保存提示——beforeRouteLeave守卫拦截

> ⚠️ 接口请求必须携带loading状态，防止重复提交

> ⚠️ [多语言模块] i18n key命名规范：{module}.{page}.{component}.{field}，例如sys.user.list.title

> ⚠️ [多语言模块] 翻译加载使用懒加载策略，首次请求某语言翻译时才加载并缓存

> ⚠️ [多语言模块] 翻译Key全局唯一性校验，新增时检查是否已存在相同key+language_code的记录

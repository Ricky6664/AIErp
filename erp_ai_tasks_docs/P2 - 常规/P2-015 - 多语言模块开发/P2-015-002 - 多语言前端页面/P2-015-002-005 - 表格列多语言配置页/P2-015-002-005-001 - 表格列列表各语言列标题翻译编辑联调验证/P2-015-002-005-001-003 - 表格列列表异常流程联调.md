# P2-015-002-005-001-003 表格列列表异常流程联调

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-015-002-005-001-003 |
| 任务名称 | 表格列列表异常流程联调 |
| 所属模块 | P2-015 |
| 优先级 | P2 |
| 任务类型 | 前端联调验证 |

## 二、任务目标

实现表格列列表异常流程联调：el-table展示列表数据，列配置包含prop/label/width/sortable，操作列固定在右侧(fixed=right)，多选列固定在左侧(fixed=left)，金额列右对齐并千分位格式化

## 三、前置依赖

### 3.1 前置任务

- P2-015-002-005-001 表格列列表+各语言列标题翻译编辑+联调验证（父任务）
- P2-015-002-005-001-002 编辑流程联调（前序兄弟任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档标准化模板与质量要求 |
| 全局规范-AI开发执行手册 | AI任务解读与执行流程规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：i18n_language(语言定义表), i18n_translation(翻译键值表)
> - 涉及API：/api/i18n/language/*, /api/i18n/translation/*, /api/i18n/export/*, /api/i18n/import/*

### 5.1 表格列配置

```vue
<el-table :data="tableData" v-loading="loading" border>
  <el-table-column type="selection" width="55" fixed="left" />
  <el-table-column prop="code" label="编码" width="150" sortable />
  <el-table-column prop="name" label="名称" min-width="200" show-overflow-tooltip />
  <el-table-column prop="amount" label="金额" width="120" align="right">
    <template #default="{scope}">{ formatMoney(scope.row.amount) }</template>
  </el-table-column>
  <el-table-column label="操作" width="200" fixed="right">
    <template #default="{scope}">
      <el-button link @click="handleEdit(scope.row)">编辑</el-button>
      <el-button link type="danger" @click="handleDelete(scope.row)">删除</el-button>
    </template>
  </el-table-column>
</el-table>
```

### 5.2 验证

1. 表格列配置完整（prop/label/width）
2. 操作列fixed="right"
3. 金额列右对齐千分位

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/i18n/表格列列表异常流程联调.vue | Vue页面组件 |
| 2 | src/api/i18n/index.ts | API调用层 |
| 3 | tests/e2e/{vue_dir}.spec.ts | 联调测试记录 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | CRUD全流程联调通过 | 手动操作验证 |
| 2 | 搜索/分页/排序联动正确 | 手动操作验证 |
| 3 | 异常场景（401/403/500）提示友好 | 模拟异常 |
| 4 | 控制台无报错 | Chrome DevTools |
| 5 | Loading状态正确展示 | 慢网络模拟 |

## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 权限控制必须使用v-permission指令，禁止手动判断角色

> ⚠️ 删除/批量操作必须二次确认（ElMessageBox.confirm）

> ⚠️ 表格操作列fixed="right"防止横向滚动被遮挡

> ⚠️ 表单离开未保存提示——beforeRouteLeave守卫拦截

> ⚠️ 接口请求必须携带loading状态，防止重复提交

> ⚠️ [多语言模块] i18n key命名规范：{module}.{page}.{component}.{field}，例如sys.user.list.title

> ⚠️ [多语言模块] 翻译加载使用懒加载策略，首次请求某语言翻译时才加载并缓存

> ⚠️ [多语言模块] 翻译Key全局唯一性校验，新增时检查是否已存在相同key+language_code的记录

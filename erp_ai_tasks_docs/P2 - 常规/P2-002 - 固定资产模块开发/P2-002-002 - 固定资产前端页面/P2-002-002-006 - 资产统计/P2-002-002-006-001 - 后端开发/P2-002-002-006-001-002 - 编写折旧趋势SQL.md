# P2-002-002-006-001-002 编写折旧趋势SQL

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-002-002-006-001-002 |
| 任务名称 | 编写折旧趋势SQL |
| 所属模块 | P2-002 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

完成编写折旧趋势SQL的开发工作，确保功能完整、质量达标、符合固定资产模块规范

## 三、前置依赖

### 3.1 前置任务

- P2-002-002-006-001 后端开发（父任务）
- P2-002-002-006-001-001 编写资产统计SQL（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：fa_card(资产卡片表), fa_depreciation(折旧计提表), fa_depreciation_detail(折旧明细), fa_change(资产变动表), fa_disposal(资产处置表), fa_asset_disposal_detail(处置明细)
> - 涉及API：/api/fa/card/*, /api/fa/depreciation/*, /api/fa/change/*, /api/fa/disposal/*, /api/fa/report/*

### 5.1 SQL示例

```sql
SELECT
    d.name AS dept_name,
    ei.name AS expense_item_name,
    SUM(r.amount) AS total_amount,
    COUNT(r.id) AS record_count
FROM asset_asset r
JOIN sys_dept d ON r.dept_id = d.id
WHERE r.is_deleted = 0
  AND r.status = 2
  AND r.biz_date BETWEEN :startDate AND :endDate
  AND r.tenant_id = :tenantId
GROUP BY d.id, ei.id
ORDER BY total_amount DESC
```

### 5.2 性能优化
- 确保biz_date/dept_id/status有索引
- 大数据量分页或限制时间范围
- EXPLAIN查看执行计划
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/resources/mapper/asset/StatsMapper.xml | 统计SQL XML |
| 2 | src/main/java/com/erp/asset/mapper/StatsMapper.java | 统计Mapper |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | SQL执行无错 | 启动测试 |
| 2 | 结果正确 | 手工对照 |
| 3 | EXPLAIN合理 | EXPLAIN |
| 4 | 性能达标 | 压测 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 确保索引(EXPLAIN)

> ⚠️ 大数据LIMIT

> ⚠️ 金额SUM+DECIMAL避免精度

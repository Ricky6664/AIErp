# P2-002-002-002-001-002 编辑流程联调

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-002-002-002-001-002 |
| 任务名称 | 编辑流程联调 |
| 所属模块 | P2-002 |
| 优先级 | P2 |
| 任务类型 | 前端联调验证 |

## 二、任务目标

编辑流程联调：GET /detail/{id}回填→修改→PUT /update；乐观锁冲突提示

## 三、前置依赖

### 3.1 前置任务

- P2-002-002-002-001 基本信息区+折旧配置区+附属设备区+联调验证（父任务）
- P2-002-002-002-001-001 新增流程联调（前序兄弟任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：fa_card(资产卡片表), fa_depreciation(折旧计提表), fa_depreciation_detail(折旧明细), fa_change(资产变动表), fa_disposal(资产处置表), fa_asset_disposal_detail(处置明细)
> - 涉及API：/api/fa/card/*, /api/fa/depreciation/*, /api/fa/change/*, /api/fa/disposal/*, /api/fa/report/*
### 5.1 编辑流程
1. 父页面[编辑]→打开弹窗(mode='edit', id)
2. api.detail(id)回填(含明细)
3. 修改→校验+联动
4. [保存]→validate()→api.update(form+version)
5. 成功→close()+emit('refresh')
6. 乐观锁冲突→提示刷新

### 5.2 回填注意
- 日期格式转换
- 字典匹配
- 明细初始化编辑状态
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/asset/components/EditForm.vue | 编辑流程 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 回填完整 | 打开编辑 |
| 2 | 乐观锁提示 | 并发 |
| 3 | 成功关闭+刷新 | 完整流程 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 回填日期格式转换

> ⚠️ 乐观锁友好提示

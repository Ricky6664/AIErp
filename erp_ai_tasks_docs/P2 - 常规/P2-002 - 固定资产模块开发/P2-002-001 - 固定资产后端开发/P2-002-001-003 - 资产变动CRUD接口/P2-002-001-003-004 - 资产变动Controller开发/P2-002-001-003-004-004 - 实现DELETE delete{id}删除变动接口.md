# P2-002-001-003-004-004 实现DELETE /delete/{id}删除变动接口

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-002-001-003-004-004 |
| 任务名称 | 实现DELETE /delete/{id}删除变动接口 |
| 所属模块 | P2-002 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现DELETE /delete/{id}删除Asset接口：@DeleteMapping+@PathVariable；校验存在+状态草稿；逻辑删除is_deleted=1；级联删除明细

## 三、前置依赖

### 3.1 前置任务

- P2-002-001-003-004 资产变动Controller开发（父任务）
- P2-002-001-003-004-003 实现PUT /update修改变动接口（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-API接口规范 | API接口设计与RESTful规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：fa_card(资产卡片表), fa_depreciation(折旧计提表), fa_depreciation_detail(折旧明细), fa_change(资产变动表), fa_disposal(资产处置表), fa_asset_disposal_detail(处置明细)
> - 涉及API：/api/fa/card/*, /api/fa/depreciation/*, /api/fa/change/*, /api/fa/disposal/*, /api/fa/report/*
### 5.1 接口
- URL: DELETE /api/asset/asset/delete/{id}
- 路径参数: id (Long)
- 响应: Result<Boolean>

### 5.2 处理流程
1. 校验存在
2. 校验草稿状态
3. UPDATE is_deleted=1
4. 级联删除明细
5. 返回true

### 5.3 错误码
- 404: 不存在
- 422: 不允许删除
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/asset/controller/AssetController.java | Controller接口方法 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | DELETE返200+true | Postman |
| 2 | 已审核禁删返422 | 状态校验 |
| 3 | is_deleted=1 | 查DB |
| 4 | 级联删除明细 | 查DB |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 编号CodeGeneratorService生成勿手动拼接

> ⚠️ 公共字段MetaHandler自动填充

> ⚠️ 明细mainId设置为主表ID

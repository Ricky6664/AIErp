# P2-002-001-002-003-004 验证折旧计算Service（单元测试：四种折旧方法计算正确性+月折旧额精度+累计折旧累加正确+残值保护+反折旧恢复验证+凭证生成正确+性能测试（大批量资产折旧））

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-002-001-002-003-004 |
| 任务名称 | 验证折旧计算Service（单元测试：四种折旧方法计算正确性+月折旧额精度+累计折旧累加正确+残值保护+反折旧恢复验证+凭证生成正确+性能测试（大批量资产折旧）） |
| 所属模块 | P2-002 |
| 优先级 | P2 |
| 任务类型 | Service服务层 |

## 二、任务目标

验证折旧计算Service（单元测试：四种折旧方法计算正确性+月折旧额精度+累计折旧累加正确+残值保护+反折旧恢复验证+凭证生成正确+性能测试（大批量资产折旧））功能：按任务目标逐项验证；单元测试+接口测试+手动验证；记录通过项/未通过项/修复方案

## 三、前置依赖

### 3.1 前置任务

- P2-002-001-002-003 折旧计算Service开发（父任务）
- P2-002-001-002-003-003 业务校验逻辑（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-测试开发规范 | 测试开发与验证规范约束 |

## 五、详细开发规格

> **📦 本任务模块上下文**（来源：P2-002模块开发指南）
> - 本模块涉及数据表：asset_card(固定资产卡片), depreciation(折旧计算单), depreciation_detail(折旧明细), asset_change(资产变动单), asset_disposal(资产处置单)
> - 本模块涉及API：/api/asset/card, /api/asset/depreciation, /api/asset/change, /api/asset/disposal
> - 本模块业务规则：折旧计算支持直线线法/双倍余额递减法/年数总和法；资产变动需重新计算折旧；处置资产需先完成当月折旧
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 单元测试用例

| 用例名 | 场景 | 预期 |
|--------|------|------|
| test_create_success | 正常创建 | 200+新建ID |
| test_create_missing_field | 缺少必填 | 400校验失败 |
| test_create_duplicate | 编号重复 | 409冲突 |
| test_update_success | 正常修改 | 200+true |
| test_update_optimistic_lock | version不一致 | 409 |
| test_delete_success | 删除草稿 | 200+true |
| test_delete_audited | 删除已审核 | 422 |
| test_page_query | 分页 | 正确分页 |
| test_detail | 详情 | 完整VO+明细 |
| test_submit_flow | 提交审核 | 状态变化 |

### 5.2 测试代码

```java
@SpringBootTest
@AutoConfigureMockMvc
class AssetControllerTest {
    @Autowired MockMvc mockMvc;

    @Test
    void test_create_success() throws Exception {
        AssetCreateDTO dto = new AssetCreateDTO();
        dto.setCode("TEST001");
        dto.setName("测试");
        mockMvc.perform(post("/api/asset/asset/create")
            .contentType(MediaType.APPLICATION_JSON)
            .content(toJson(dto)))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(200));
    }
}
```

### 5.3 验证步骤
1. mvn test -Dtest=AssetControllerTest
2. 查看surefire-reports
3. Knife4j在线调试
4. 验证数据库数据
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/test/java/com/erp/asset/controller/AssetControllerTest.java | Controller单元测试 |
| 2 | src/test/java/com/erp/asset/service/AssetServiceTest.java | Service单元测试 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 所有验证项通过 | 测试报告 | 测试执行记录文档 |
| 2 | 测试报告已记录 | 文档 |
| 3 | 问题已修复或标记 | 问题清单 | 发现的问题与修复记录 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ @TableField勿遗漏

> ⚠️ 事务边界准确避免大事务

> ⚠️ 异常统一BusinessException

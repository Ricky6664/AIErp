# P2-007-002-010-001-001 搜索表单布局

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-007-002-010-001-001 |
| 任务名称 | 搜索表单布局 |
| 所属模块 | P2-007 |
| 优先级 | P2 |
| 任务类型 | 前端搜索表单 |

## 二、任务目标

实现搜索表单布局：el-form+el-row/el-col响应式栅格(xl=6/lg=8/md=12/sm=24)；labelWidth统一100px；展开/收起功能(默认显示常用条件)

## 三、前置依赖

### 3.1 前置任务

- P2-007-002-010-001 搜索条件区+数据表格区+联调验证（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：after_sale(售后单), after_sale_remote_service(远程服务单), after_sale_onsite_service(上门服务单), after_sale_return(退货单), after_sale_exchange(换货单), after_sale_repair(维修单), after_sale_revisit(回访单), after_sale_warranty_rule(保修规则)
> - 涉及API：/api/aftersale/*, /api/aftersale/service/*, /api/aftersale/return/*, /api/aftersale/repair/*, /api/aftersale/revisit/*
### 5.1 组件结构
```vue
<template>
  <el-form :model="searchForm" label-width="100px" class="search-form">
    <el-row :gutter="20">
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item label="编号">
          <el-input v-model="searchForm.code" placeholder="请输入编号" clearable/>
        </el-form-item>
      </el-col>
      <el-col :xl="6" :lg="8" :md="12" :sm="24">
        <el-form-item>
          <el-button type="primary" @click="handleSearch">搜索</el-button>
          <el-button @click="handleReset">重置</el-button>
          <el-link @click="expand = !expand">{{ expand ? '收起' : '展开' }}</el-link>
        </el-form-item>
      </el-col>
    </el-row>
  </el-form>
</template>
```

### 5.2 规范
- 响应式栅格xl/lg/md/sm
- labelWidth统一100px
- 展开/收起
- 按钮组：搜索+重置+展开
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/aftersale/components/SearchForm.vue | 搜索表单组件 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | el-form+栅格正确 | 浏览器 |
| 2 | 响应式适配 | 调窗口 |
| 3 | labelWidth统一 | 视觉 |
| 4 | 展开/收起 | 点击 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 响应式断点测试

> ⚠️ labelWidth统一

> ⚠️ 组件PascalCase

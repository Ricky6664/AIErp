# P2-006-002-002-001-001 主表表单布局

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-006-002-002-001-001 |
| 任务名称 | 主表表单布局 |
| 所属模块 | P2-006 |
| 优先级 | P2 |
| 任务类型 | 前端表单 |

## 二、任务目标

实现主表表单布局：el-form分区(基本信息/金额/附件)+el-row/el-col栅格；必填标识*；审核后字段只读

## 三、前置依赖

### 3.1 前置任务

- P2-006-002-002-001 主表字段+从表明细区+客户选择器+商品选择器+联调验证（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：lease_contract(租赁合同), lease_outbound(租赁出库单), lease_billing(租赁计费单), lease_receipt(租赁收款单), lease_return(租赁归还单), lease_inbound(租入单), lease_in_return(租入归还单)
> - 涉及API：/api/lease/contract/*, /api/lease/outbound/*, /api/lease/billing/*, /api/lease/receipt/*, /api/lease/return/*
### 5.1 表单布局
```vue
<el-form ref="formRef" :model="form" :rules="rules" label-width="120px">
  <el-divider content-position="left">基本信息</el-divider>
  <el-row :gutter="20">
    <el-col :span="12">
      <el-form-item label="编号" prop="code">
        <el-input v-model="form.code" :disabled="isEdit"/>
      </el-form-item>
    </el-col>
  </el-row>
  <el-divider content-position="left">金额信息</el-divider>
  <el-divider content-position="left">明细信息</el-divider>
</el-form>
```

### 5.2 分区
- 基本信息/金额/附件/明细
- 审核后disabled
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/lease/components/EditForm.vue | 编辑表单 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 分区展示 | 浏览器 |
| 2 | 必填标识* | 视觉 |
| 3 | 审核后只读 | 状态切换 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ el-divider分区

> ⚠️ 审核后只读

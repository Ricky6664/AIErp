# P2-006-002-002-001-003 主表联动逻辑

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-006-002-002-001-003 |
| 任务名称 | 主表联动逻辑 |
| 所属模块 | P2-006 |
| 优先级 | P2 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

主表联动逻辑：部门→部门信息；费用项目→预算科目；金额change→预算校验；明细金额→主表合计；状态→字段只读控制

## 三、前置依赖

### 3.1 前置任务

- P2-006-002-002-001 主表字段+从表明细区+客户选择器+商品选择器+联调验证（父任务）
- P2-006-002-002-001-002 主表字段组件渲染（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：lease_contract(租赁合同), lease_outbound(租赁出库单), lease_billing(租赁计费单), lease_receipt(租赁收款单), lease_return(租赁归还单), lease_inbound(租入单), lease_in_return(租入归还单)
> - 涉及API：/api/lease/contract/*, /api/lease/outbound/*, /api/lease/billing/*, /api/lease/receipt/*, /api/lease/return/*
### 5.1 联动场景
```typescript
const handleDeptChange = (dept) => {
  form.deptName = dept.name
}

watch(() => form.amount, debounce(async (val) => {
  if (val && form.deptId) {
    budgetCheck.value = await api.checkBudget({
      deptId: form.deptId, period: currentPeriod, amount: val
    })
  }
}, 500))

const handleDetailAmountChange = () => {
  form.totalAmount = details.value.reduce((sum, d) => sum + (d.amount || 0), 0)
}
```

### 5.2 联动规则
- 部门→部门信息
- 费用项目→预算科目
- 明细金额→主表合计
- 状态→字段只读
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/lease/components/EditForm.vue | 表单联动 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 部门联动 | 选择测试 |
| 2 | 金额汇总 | 修改测试 |
| 3 | 状态只读 | 状态切换 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ watch避免死循环

> ⚠️ 金额用decimal.js避免精度丢失

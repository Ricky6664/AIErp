# P2-006-002-003-001-003 实现搜索条件区搜索交互

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P2-006-002-003-001-003 |
| 任务名称 | 实现搜索条件区搜索交互 |
| 所属模块 | P2-006 |
| 优先级 | P2 |
| 任务类型 | 前端搜索表单 |

## 二、任务目标

实现搜索条件区搜索交互：搜索按钮触发(resetPageNum=1)+重置按钮清空+回车键触发+防抖300ms+URL参数query同步

## 三、前置依赖

### 3.1 前置任务

- P2-006-002-003-001 搜索条件区+数据表格区+联调验证（父任务）
- P2-006-002-003-001-002 搜索字段组件渲染（前序兄弟任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格

> **本任务模块上下文**
> - 涉及数据表：lease_contract(租赁合同), lease_outbound(租赁出库单), lease_billing(租赁计费单), lease_receipt(租赁收款单), lease_return(租赁归还单), lease_inbound(租入单), lease_in_return(租入归还单)
> - 涉及API：/api/lease/contract/*, /api/lease/outbound/*, /api/lease/billing/*, /api/lease/receipt/*, /api/lease/return/*
### 5.1 交互逻辑
```typescript
const searchForm = reactive({ code: '', status: null, dateRange: [] })
let searchTimer: any = null

const handleSearch = () => {
  clearTimeout(searchTimer)
  searchTimer = setTimeout(() => {
    emit('search', { ...searchForm, pageNum: 1 })
  }, 300)
}

const handleReset = () => {
  Object.assign(searchForm, { code: '', status: null, dateRange: [] })
  handleSearch()
}
```

### 5.2 要点
- 防抖300ms
- 搜索时pageNum=1
- 重置清空并触发搜索
- URL参数同步
## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/lease/components/SearchForm.vue | 搜索交互 |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 搜索触发查询 | F12 Network |
| 2 | 重置清空并查 | 点击 |
| 3 | 回车触发 | 按键 |
| 4 | 防抖300ms | 快速输入 |
| 5 | URL同步 | 刷新 |


## 八、易错警示

> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 多租户隔离(tenant_id)必须正确——所有SQL查询需自动注入tenant_id

> ⚠️ 逻辑删除字段(is_deleted)正确处理——查询追加is_deleted=false，删除使用UPDATE

> ⚠️ 防抖300ms

> ⚠️ 搜索时pageNum=1

> ⚠️ URL参数同步

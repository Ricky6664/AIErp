# 全局规范-后端代码规范

> 本文档定义后端Java代码开发规范，所有后端开发任务必须严格遵守。

---

## 1. 项目结构规范

```
src/main/java/com/xxx/erp/
├── config/           # 配置类（MyBatis-Plus/Sa-Token/Redis/全局异常）
├── common/           # 公共基础（R响应类/分页类/枚举/常量/工具）
├── modules/          # 业务模块（按模块分子包）
│   ├── crm/          # 客户管理模块
│   │   ├── controller/
│   │   ├── service/
│   │   ├── mapper/
│   │   ├── entity/
│   │   ├── dto/
│   │   └── vo/
│   ├── srm/          # 供应商管理模块
│   ├── prod/         # 商品管理模块
│   ├── org/          # 组织架构模块
│   ├── hrm/          # 人力资源模块
│   ├── purchase/     # 采购管理模块
│   ├── sale/         # 销售管理模块
│   ├── inv/          # 库存管理模块
│   ├── produce/      # 生产管理模块
│   ├── subcontract/  # 委外管理模块
│   ├── ar/           # 应收管理模块
│   ├── ap/           # 应付管理模块
│   ├── voucher/      # 凭证管理模块
│   ├── ledger/       # 账簿查询模块
│   ├── report/       # 财务报表模块
│   ├── cost/         # 成本核算模块
│   ├── expense/      # 费用管理模块
│   ├── asset/        # 固定资产模块
│   ├── sample/       # 样品管理模块
│   ├── borrow/       # 借用管理模块
│   ├── lease/        # 租赁管理模块
│   ├── aftersale/    # 售后管理模块
│   ├── oa/           # OA办公模块
│   ├── ai/           # AI助手模块
│   ├── template/     # 模板中心模块
│   ├── sys/          # 系统管理模块
│   ├── ops/          # 运维管理模块
│   └── i18n/         # 多语言模块
├── engine/           # 通用引擎（编码/审核/流转/消息）
└── ApiApplication.java
```

### 后端项目完整目录结构（Spring Boot）

```
erp-server/                           # 后端项目根目录
├── src/main/java/com/erp/
│   ├── ErpApplication.java           # Spring Boot启动类
│   ├── config/                       # 配置类
│   │   ├── SaTokenConfig.java        # Sa-Token权限配置
│   │   ├── MybatisPlusConfig.java    # MyBatis-Plus配置（多租户/软删除/乐观锁）
│   │   ├── CorsConfig.java           # 跨域配置
│   │   ├── RedisConfig.java          # Redis配置
│   │   └── WebSocketConfig.java      # WebSocket配置
│   ├── common/                       # 通用基础
│   │   ├── result/                   # 统一返回结构 R<T>
│   │   ├── exception/                # 全局异常处理器
│   │   ├── constant/                 # 常量定义
│   │   └── enums/                    # 枚举定义
│   ├── module/                       # 业务模块（按模块组织）
│   │   ├── crm/                      # CRM模块
│   │   │   ├── controller/           # RESTful控制器
│   │   │   ├── service/              # 业务逻辑层
│   │   │   ├── mapper/               # MyBatis-Plus Mapper
│   │   │   ├── entity/               # 数据库实体
│   │   │   └── dto/                  # 数据传输对象
│   │   ├── srm/                      # SRM模块
│   │   ├── product/                  # 商品管理模块
│   │   ├── org/                      # 组织架构模块
│   │   ├── hrm/                      # HRM模块
│   │   ├── sale/                     # 销售管理模块
│   │   ├── purchase/                 # 采购管理模块
│   │   ├── inventory/                # 库存管理模块
│   │   ├── production/               # 生产管理模块
│   │   ├── outsource/                # 委外管理模块
│   │   ├── receivable/               # 应收管理模块
│   │   ├── payable/                  # 应付管理模块
│   │   ├── cost/                     # 成本核算模块
│   │   ├── expense/                  # 费用管理模块
│   │   ├── voucher/                  # 凭证管理模块
│   │   ├── ledger/                   # 账簿查询模块
│   │   ├── finreport/                # 财务报表模块
│   │   ├── asset/                    # 固定资产模块
│   │   ├── period/                   # 期末处理模块
│   │   ├── finsetting/               # 财务基础设置模块
│   │   ├── sample/                   # 样品管理模块
│   │   ├── borrow/                   # 借用管理模块
│   │   ├── lease/                    # 租赁管理模块
│   │   ├── aftersale/                # 售后管理模块
│   │   ├── approval/                 # 审批流程模块
│   │   ├── oa/                       # OA办公模块
│   │   ├── message/                  # 消息管理模块
│   │   ├── ai/                       # AI助手模块
│   │   ├── template/                 # 模板中心模块
│   │   ├── mobile/                   # 移动端管理模块
│   │   ├── report/                   # 报表中心模块
│   │   ├── opening/                  # 期初管理模块
│   │   ├── system/                   # 系统管理模块
│   │   ├── ops/                      # 运维管理模块
│   │   ├── i18n/                     # 多语言管理模块
│   │   ├── user/                     # 用户管理模块
│   │   ├── permission/               # 权限配置模块
│   │   └── authconfig/               # 认证配置模块
│   └── framework/                    # 框架层
│       ├── tenant/                   # 多租户拦截器
│       ├── dataperm/                 # 数据权限拦截器
│       ├── softdelete/               # 软删除插件
│       └── codegen/                  # 编码自动生成引擎
├── src/main/resources/
│   ├── application.yml               # 主配置
│   ├── application-dev.yml           # 开发环境配置
│   ├── application-prod.yml          # 生产环境配置
│   ├── mapper/                       # MyBatis XML映射文件
│   └── db/migration/                 # Flyway数据库迁移脚本
│       ├── V1.0.0__init_schema.sql
│       ├── V1.0.1__init_data.sql
│       └── ...
├── pom.xml                           # Maven依赖配置
└── Dockerfile                        # Docker构建文件
```

> **前后端目录命名说明**：前端目录使用kebab-case（如`fin-report/`、`after-sale/`），后端Java包名不使用连字符（如`finreport/`、`aftersale/`），两者模块对应关系为：`fin-report`↔`finreport`、`fin-setting`↔`finsetting`、`after-sale`↔`aftersale`、`auth-config`↔`authconfig`。

---

## 2. Controller层规范

```java
@RestController
@RequestMapping("/api/sale/order")
@Tag(name = "销售订单", description = "销售订单管理接口")
@RequiredArgsConstructor
public class SaleOrderController {
    private final SaleOrderService saleOrderService;
    
    @GetMapping
    @Operation(summary = "分页查询销售订单列表")
    public R<PageResult<SaleOrderListVO>> list(SaleOrderQueryDTO query) {
        return R.ok(saleOrderService.getPage(query));
    }
    
    @GetMapping("/{id}")
    @Operation(summary = "查询销售订单详情")
    public R<SaleOrderDetailVO> getById(@PathVariable Long id) {
        return R.ok(saleOrderService.getDetailById(id));
    }
    
    @PostMapping
    @Operation(summary = "新增销售订单")
    public R<Long> create(@Validated @RequestBody SaleOrderCreateDTO dto) {
        return R.ok(saleOrderService.create(dto));
    }
    
    @PutMapping("/{id}")
    @Operation(summary = "修改销售订单")
    public R<Void> update(@PathVariable Long id, @Validated @RequestBody SaleOrderUpdateDTO dto) {
        saleOrderService.update(id, dto);
        return R.ok();
    }
    
    @DeleteMapping("/{id}")
    @Operation(summary = "删除销售订单")
    public R<Void> delete(@PathVariable Long id) {
        saleOrderService.delete(id);
        return R.ok();
    }
    
    @PostMapping("/{id}/audit")
    @Operation(summary = "审核/反审销售订单")
    public R<Void> audit(@PathVariable Long id, @RequestBody AuditDTO dto) {
        saleOrderService.audit(id, dto);
        return R.ok();
    }
}
```

**Controller层规范要点：**

- 使用`@RestController`+`@RequestMapping`声明RESTful控制器
- 使用`@Tag`/`@Operation`（SpringDoc OpenAPI 3）声明接口文档
- 使用`@RequiredArgsConstructor`+`private final`实现构造器注入
- 所有返回值统一使用`R<T>`包装
- 入参校验使用`@Validated`+`@RequestBody`
- 每个Controller只负责一个资源（模块/业务）的CRUD及业务操作

---

## 3. Service层规范

```java
@Service
@RequiredArgsConstructor
public class SaleOrderServiceImpl extends ServiceImpl<SaleOrderMapper, SaleOrder> implements SaleOrderService {
    private final SaleOrderDetailService detailService;
    private final CodeGeneratorService codeGeneratorService;
    private final AuditService auditService;
    
    @Transactional(rollbackFor = Exception.class)
    @Override
    public Long create(SaleOrderCreateDTO dto) {
        // 1. 主表保存
        SaleOrder order = BeanUtil.copyProperties(dto, SaleOrder.class);
        order.setBillNo(codeGeneratorService.generate("SALE_ORDER"));
        order.setBillStatus(BillStatus.DRAFT.getCode());
        save(order);
        
        // 2. 从表保存
        List<SaleOrderDetail> details = BeanUtil.copyToList(dto.getDetails(), SaleOrderDetail.class);
        details.forEach(d -> d.setOrderId(order.getId()));
        detailService.saveBatch(details);
        
        // 3. 关联处理（库存占用、应收款等）
        
        return order.getId();
    }
}
```

**Service层规范要点：**

- 使用`@Service`声明业务服务类
- 继承`ServiceImpl<Mapper, Entity>`获得MyBatis-Plus内置CRUD能力
- 实现对应业务接口（如`SaleOrderService`）
- 业务逻辑处理步骤清晰，注释明确
- DTO→Entity转换使用Hutool `BeanUtil.copyProperties`/`BeanUtil.copyToList`
- 编码生成统一调用`CodeGeneratorService`
- 涉及主表+从表保存的操作必须加`@Transactional(rollbackFor = Exception.class)`

---

## 4. Mapper层规范

```java
@Mapper
public interface SaleOrderMapper extends BaseMapperX<SaleOrder> {
    // 自定义复杂查询方法
    List<SaleOrderStatisticsVO> statisticsByCustomer(@Param("customerId") Long customerId, @Param("startDate") LocalDate startDate, @Param("endDate") LocalDate endDate);
}
```

**Mapper层规范要点：**

- 使用`@Mapper`注解声明MyBatis-Plus Mapper接口
- 继承`BaseMapperX<Entity>`（项目自定义扩展基类，内含`BaseMapper`全部方法+自定义增强方法）
- 单表CRUD零代码，直接使用继承方法
- 复杂查询（多表联查、统计报表）声明自定义方法，对应XML映射文件编写SQL
- 多参数方法使用`@Param`注解显式命名参数

---

## 5. Entity层规范

```java
@Data
@TableName("sale_order")
public class SaleOrder {
    @TableId(type = IdType.ASSIGN_ID)
    private Long id;
    
    private String billNo;
    private LocalDate billDate;
    private Integer billStatus;
    private Integer auditStatus;
    private Long customerId;
    private String customerName;
    // ... 业务字段
    
    // 通用必含字段
    private Long tenantId;
    private Long creatorId;
    private LocalDateTime createTime;
    private Long updaterId;
    private LocalDateTime updateTime;
    @TableLogic
    private Boolean isDeleted;
    private Integer version;
}
```

**Entity层规范要点：**

- 使用`@Data`（Lombok）自动生成getter/setter
- 使用`@TableName`声明对应的数据库表名
- 主键使用`@TableId(type = IdType.ASSIGN_ID)`（雪花算法ID）
- 软删除字段使用`@TableLogic`注解
- 所有业务表Entity必须包含通用必含字段（tenantId/creatorId/createTime/updaterId/updateTime/isDeleted/version）
- 字段命名使用驼峰式（camelCase），MyBatis-Plus自动映射数据库下划线命名

---

## 6. DTO/VO层规范

```java
// 创建DTO
@Data
public class SaleOrderCreateDTO {
    @NotBlank(message = "客户ID不能为空")
    private Long customerId;
    
    @NotNull(message = "明细不能为空")
    @Size(min = 1, message = "明细至少1行")
    private List<SaleOrderDetailDTO> details;
    
    // ... 其他字段
}

// 列表VO
@Data
public class SaleOrderListVO {
    private Long id;
    private String billNo;
    private String billStatus;
    @JsonFormat(pattern = "yyyy-MM-dd")
    private LocalDate billDate;
    private String customerName;
    private BigDecimal totalAmount;
    
    // 字典翻译字段
    private String billStatusName;
}

// 详情VO
@Data
public class SaleOrderDetailVO {
    private Long id;
    // ... 主表字段
    private List<SaleOrderDetailItemVO> details;
}
```

**DTO/VO层规范要点：**

- **DTO（Data Transfer Object）**：用于接收前端请求数据，使用Jakarta Validation注解做参数校验
- **VO（View Object）**：用于返回前端展示数据，使用`@JsonFormat`控制日期/时间序列化格式
- 创建操作使用`XxxCreateDTO`，修改操作使用`XxxUpdateDTO`，查询操作使用`XxxQueryDTO`
- 列表展示使用`XxxListVO`，详情展示使用`XxxDetailVO`
- VO中包含字典翻译字段（如`billStatusName`），由后端翻译后直接返回前端展示
- 详情VO包含主表字段+从表明细列表
- 禁止将Entity直接暴露给前端，必须通过DTO/VO进行数据隔离

---

## 7. 事务管理规范

- 单据保存（主表+从表）使用`@Transactional`数据库事务
- 审核操作需在事务内完成状态变更+库存/财务联动
- 编码生成需在事务内完成序号递增（Redis原子操作或数据库行级锁）

**事务管理规范要点：**

- 所有涉及多表写入的操作必须加`@Transactional(rollbackFor = Exception.class)`
- `rollbackFor`必须显式指定为`Exception.class`，确保所有异常都能回滚
- 事务方法内部避免调用外部RPC/HTTP接口，防止长事务
- 对于需要跨模块联动的操作，优先使用RabbitMQ异步消息解耦，避免跨模块大事务

# P1-007-002-003-003-001 定义Service接口

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-007-002-003-003-001 |
| 任务名称 | 定义Service接口 |
| 所属模块 | P1-007 |
| 优先级 | P1 |
| 任务类型 | Service服务层 |

## 二、任务目标

定义IWmsInventoryService接口，继承IServiceX<WmsInventoryEntity>，声明inv_warehouse表CRUD方法+业务方法（提交审核、反审、下推等）


## 三、前置依赖

### 3.1 前置任务

- P1-007-002-003-003 盘点单Service开发（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-007模块开发指南）
> - 本模块涉及数据表：inv_warehouse, inv_location, inv_other_outbound, inv_other_outbound_detail, inv_other_inbound, inv_other_inbound_detail, inv_stocktake, inv_stocktake_detail, inv_transfer, inv_transfer_detail
> - 本模块涉及API：/api/inventory/stock-engine, /api/inventory/available-qty, /api/inventory/occupied-qty, /api/inventory/intransit-qty, /api/inventory/cost
> - 本模块业务规则：库存增减原子操作：inv_stock表行级锁+数量增减+版本号乐观锁，所有出入库单据审核时必须通过库存引擎Service执行增减操作，确保库存数据一致性。; 成本计算方法：系统参数inv.cost_method控制全局成本方法（移动加权平均/先进先出/手工指定），按商品可覆盖成本方法。入库时重新加权平均单价，出库时按当前均价或批次顺序匹配成本。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 Service接口定义
```java
public interface IWmsInventoryService extends IServiceX<WmsInventoryEntity> {
    /** 分页查询列表 */
    Page<WmsInventoryListVO> pageList(WmsInventoryQueryDTO query);

    /** 查询详情 */
    WmsInventoryDetailVO getDetail(Long id);

    /** 新增 */
    Long create(WmsInventoryCreateDTO dto);

    /** 修改 */
    Boolean update(WmsInventoryUpdateDTO dto);

    /** 删除 */
    Boolean delete(Long id);

    /** 提交审核 */
    Boolean submitAudit(Long id, String opinion);
}
```


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/wms/service/IWmsInventoryService.java | Service接口 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | IWmsInventoryService继承IServiceX<WmsInventoryEntity> | 代码检查 |
| 2 | CRUD方法签名完整(pageList/getDetail/create/update/delete) | 代码检查 |
| 3 | 业务方法声明正确(submitAudit等) | 代码检查 |
| 4 | 编译通过无警告 | mvn compile |
## 八、易错警示

> ⚠️ 继承IServiceX<Entity>（项目自定义扩展），非MyBatis-Plus原生IService

> ⚠️ 方法命名规范：create/update/delete/get/page/list

> ⚠️ 返回值类型统一：创建返回Long(id)，修改/删除返回Boolean，查询返回VO

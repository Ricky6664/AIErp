# P1-007-002-001-003-003 业务校验逻辑

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-007-002-001-003-003 |
| 任务名称 | 业务校验逻辑 |
| 所属模块 | P1-007 |
| 优先级 | P1 |
| 任务类型 | 验证/测试 |

## 二、任务目标

实现WmsInventory业务校验：唯一性校验（编号/名称不重复）、状态流转校验（仅草稿可提交审核）、关联数据完整性校验（明细行不为空）、金额计算校验


## 三、前置依赖

### 3.1 前置任务

- P1-007-002-001-003 其他出库单Service开发（父任务）
- P1-007-002-001-003-002 实现ServiceImpl（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-007模块开发指南）
> - 本模块涉及数据表：inv_warehouse, inv_location, inv_other_outbound, inv_other_outbound_detail, inv_other_inbound, inv_other_inbound_detail, inv_stocktake, inv_stocktake_detail, inv_transfer, inv_transfer_detail
> - 本模块涉及API：/api/inventory/stock-engine, /api/inventory/available-qty, /api/inventory/occupied-qty, /api/inventory/intransit-qty, /api/inventory/cost
> - 本模块业务规则：库存增减原子操作：inv_stock表行级锁+数量增减+版本号乐观锁，所有出入库单据审核时必须通过库存引擎Service执行增减操作，确保库存数据一致性。; 成本计算方法：系统参数inv.cost_method控制全局成本方法（移动加权平均/先进先出/手工指定），按商品可覆盖成本方法。入库时重新加权平均单价，出库时按当前均价或批次顺序匹配成本。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 校验方法清单
```java
/** 新增校验 */
private void validateCreate(WmsInventoryCreateDTO dto) {
    // 1. 名称唯一性
    long count = count(new LambdaQueryWrapper<WmsInventoryEntity>()
        .eq(WmsInventoryEntity::getWmsInventoryName, dto.getWmsInventoryName()));
    if (count > 0) throw new BusinessException("名称已存在");
    // 2. 明细行非空
    if (CollUtil.isEmpty(dto.getDetails()))
        throw new BusinessException("明细行不能为空");
}

/** 状态流转校验 */
private void validateStatusTransition(Integer fromStatus, Integer toStatus) {
    // 仅草稿(0)可提交审核(1)，仅已审核(2)可反审(0)
}
```

### 5.2 校验规则汇总
| 校验项 | 校验时机 | 失败处理 |
|--------|---------|---------|
| 名称唯一性 | 新增/修改 | 抛出BusinessException |
| 明细行非空 | 新增/修改 | 抛出BusinessException |
| 状态流转合法性 | 审核/反审/作废 | 抛出BusinessException |
| 金额计算正确性 | 新增/修改 | 重新计算并校验 |


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/wms/service/impl/WmsInventoryServiceImpl.java | 校验方法(在ServiceImpl中) |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 名称唯一性校验生效 | 单元测试全部通过 + 手动验证核心场景 |
| 2 | 明细行非空校验生效 | 单元测试全部通过 + 手动验证核心场景 |
| 3 | 状态流转校验正确(仅草稿可审核) | 单元测试全部通过 + 手动验证核心场景 |
| 4 | 校验失败抛出BusinessException+明确提示 | 单元测试全部通过 + 手动验证核心场景 |
## 八、易错警示

> ⚠️ 校验顺序：先非空→再格式→再唯一性→再业务规则(性能优化)

> ⚠️ 唯一性校验要排除当前记录(update时WHERE id != currentId)

> ⚠️ 状态流转校验使用状态机配置，勿硬编码if/else

> ⚠️ 校验失败统一抛出BusinessException，携带明确中文提示

# P1-007-002-006-001-003 定义VO类

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-007-002-006-001-003 |
| 任务名称 | 定义VO类 |
| 所属模块 | P1-007 |
| 优先级 | P1 |
| 任务类型 | Entity/DTO/VO数据模型 |

## 二、任务目标

定义WmsInventory的ListVO(列表展示字段)+DetailVO(完整字段+关联数据)，使用@JsonFormat日期格式化+字典翻译字段，用于inv_warehouse表数据返回


## 三、前置依赖

### 3.1 前置任务

- P1-007-002-006-001 报溢单Entity/DTO/VO定义+Mapper开发（父任务）
- P1-007-002-006-001-002 定义DTO类（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-数据库规范 | 数据库字段映射与Entity模型规范 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-007模块开发指南）
> - 本模块涉及数据表：inv_warehouse, inv_location, inv_other_outbound, inv_other_outbound_detail, inv_other_inbound, inv_other_inbound_detail, inv_stocktake, inv_stocktake_detail, inv_transfer, inv_transfer_detail
> - 本模块涉及API：/api/inventory/stock-engine, /api/inventory/available-qty, /api/inventory/occupied-qty, /api/inventory/intransit-qty, /api/inventory/cost
> - 本模块业务规则：库存增减原子操作：inv_stock表行级锁+数量增减+版本号乐观锁，所有出入库单据审核时必须通过库存引擎Service执行增减操作，确保库存数据一致性。; 成本计算方法：系统参数inv.cost_method控制全局成本方法（移动加权平均/先进先出/手工指定），按商品可覆盖成本方法。入库时重新加权平均单价，出库时按当前均价或批次顺序匹配成本。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 ListVO（列表展示）
```java
@Data
public class WmsInventoryListVO {
    private Long id;
    private String wmsNo;
    private String wmsName;
    private Integer status;
    private String statusName;  // 字典翻译：状态名称
    @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private LocalDateTime createTime;
    private String creatorName;  // 创建人姓名
}
```

### 5.2 DetailVO（详情展示）
```java
@Data
public class WmsInventoryDetailVO extends WmsInventoryListVO {
    private String remark;
    private Integer version;
    private List<WmsInventoryDetailVO> details;  // 明细行列表
    @JsonFormat(pattern = "yyyy-MM-dd")
    private LocalDate bizDate;
}
```


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/wms/vo/WmsInventoryListVO.java | 列表VO |
| 2 | src/main/java/com/erp/wms/vo/WmsInventoryDetailVO.java | 详情VO |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | ListVO包含列表展示所需字段 | 代码检查 |
| 2 | DetailVO包含完整字段+明细行列表 | 代码检查 |
| 3 | @JsonFormat日期格式注解正确 | 代码检查 |
| 4 | 字典翻译字段(statusName等)已定义 | 代码检查 |
| 5 | 编译通过无警告 | mvn compile |
## 八、易错警示

> ⚠️ @JsonFormat的pattern格式与服务端统一(yyyy-MM-dd HH:mm:ss)

> ⚠️ 字典翻译字段(如statusName)命名规范：原字段名+Name/Label

> ⚠️ 金额字段使用BigDecimal，勿用double/float

> ⚠️ ListVO仅包含列表展示字段，避免返回过多数据

# P1-007-003-014-002-002 数据加载与分页

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-007-003-014-002-002 |
| 任务名称 | 数据加载与分页 |
| 所属模块 | P1-007 |
| 优先级 | P1 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现WmsInventory列表数据加载与分页：调用GET /api/inventory/stock接口、分页参数传递(pageNum/pageSize)、响应数据绑定vxe-table、加载状态展示


## 三、前置依赖

### 3.1 前置任务

- P1-007-003-014-002 查询结果表格区（父任务）
- P1-007-003-014-002-001 vxe-table列定义（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-007模块开发指南）
> - 本模块涉及数据表：inv_warehouse, inv_location, inv_other_outbound, inv_other_outbound_detail, inv_other_inbound, inv_other_inbound_detail, inv_stocktake, inv_stocktake_detail, inv_transfer, inv_transfer_detail
> - 本模块涉及API：/api/inventory/stock-engine, /api/inventory/available-qty, /api/inventory/occupied-qty, /api/inventory/intransit-qty, /api/inventory/cost
> - 本模块业务规则：库存增减原子操作：inv_stock表行级锁+数量增减+版本号乐观锁，所有出入库单据审核时必须通过库存引擎Service执行增减操作，确保库存数据一致性。; 成本计算方法：系统参数inv.cost_method控制全局成本方法（移动加权平均/先进先出/手工指定），按商品可覆盖成本方法。入库时重新加权平均单价，出库时按当前均价或批次顺序匹配成本。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 数据加载逻辑
```typescript
const loading = ref(false)
const tableData = ref([])
const pagination = reactive({ pageNum: 1, pageSize: 10, total: 0 })

const loadData = async () => {
  loading.value = true
  try {
    const res = await api.getWmsInventoryPage({ ...searchForm, ...pagination })
    tableData.value = res.data.records
    pagination.total = res.data.total
  } finally {
    loading.value = false
  }
}
```

### 5.2 分页事件
- 页码变更(current-change)：更新pageNum，重新加载
- 每页条数变更(size-change)：更新pageSize，重置pageNum=1，重新加载


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/wms/index.vue | 列表页主组件 |
| 2 | src/api/wms.ts | API调用层 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 列表数据正确加载 | 浏览器验证 |
| 2 | 分页切换正常(页码/每页条数) | 交互验证 |
| 3 | 加载状态展示正确(loading) | 视觉检查 |
| 4 | 空数据展示正确(暂无数据) | 视觉检查 |
| 5 | 与后端接口联调通过 | 联调验证 |
## 八、易错警示

> ⚠️ API调用必须在try/finally中设置loading=false

> ⚠️ 分页total从后端响应中获取，勿前端计算

> ⚠️ 搜索条件变更时重置pageNum=1，避免查询空结果

> ⚠️ 防抖处理：搜索输入框使用debounce(300ms)

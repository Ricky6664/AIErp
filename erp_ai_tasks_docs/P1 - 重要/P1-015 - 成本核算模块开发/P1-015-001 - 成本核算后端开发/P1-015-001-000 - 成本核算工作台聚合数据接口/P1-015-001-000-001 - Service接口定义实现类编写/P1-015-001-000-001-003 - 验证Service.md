# P1-015-001-000-001-003 验证Service

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-015-001-000-001-003 |
| 任务名称 | 验证Service |
| 所属模块 | P1-015 |
| 优先级 | P1 |
| 任务类型 | Service服务层 |

## 二、任务目标

验证成本核算模块Service层方法的正确性，覆盖单元测试、事务回滚、并发场景、异常分支，确保核心业务分支覆盖率≥80%

## 三、前置依赖

### 3.1 前置任务

- P1-015-001-000-001 Service接口定义+实现类编写（父任务）
- P1-015-001-000-001-002 编写ServiceImpl实现类（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-测试开发规范 | 测试开发与验证规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：P1-015模块开发指南）
> - 本模块涉及数据表：cost_sale_order, cost_sale_outbound, cost_production, cost_outsource, cost_inventory_adjust, cost_inventory_adjust_detail
> - 本模块涉及API：/api/cost/workbench, /api/cost/sale-order, /api/cost/sale-order/export, /api/cost/sale-outbound, /api/cost/sale-outbound/export
> - 本模块业务规则：成本方法统一：销售出库成本核算的成本方法（移动加权平均/FIFO/手工指定）统一由inv.cost_method系统参数控制，与P1-007库存引擎保持一致。; 生产成本归集：生产成本包括材料成本（生产领料单）、人工成本（工资分摊）、制造费用（间接费用分摊）。分摊方法支持按工时/产量/金额比例分摊。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 验证清单
1. Service方法入参校验：空值/边界值/非法格式触发IllegalArgumentException
2. 业务规则校验：状态机/唯一性/关联约束触发BusinessException
3. 数据库持久化：主从表数据一致，索引/唯一约束生效
4. 事务边界：异常时回滚，正常时提交

### 5.2 测试方法
1. JUnit5 + @SpringBootTest + 嵌入式PostgreSQL
2. 使用@Transactional测试注解，每个测试自动回滚
3. 覆盖率工具JaCoCo，目标分支覆盖≥80%

### 5.3 结果记录
1. 通过项+未通过项清单
2. 覆盖率报告截图
3. 失败用例修复方案与回归结果

## 六、交付物清单
| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/cost/service/impl/XxxServiceImpl.java | Service实现类 |
| 2 | src/test/java/com/erp/cost/XxxServiceImplTest.java | 单元测试 |

## 七、验收标准
| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 所有验证用例通过 | 测试报告 | 测试执行记录文档 |
| 2 | 分支覆盖率≥80% | JaCoCo报告 |
| 3 | 事务回滚场景验证通过 | 异常注入测试 |
| 4 | 失败用例已修复或标记 | 问题清单Review |
| 5 | 回归测试通过 | 回归测试执行 |

## 八、易错警示
> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 注意多租户隔离（tenant_id）贯穿SQL/业务逻辑/缓存

> ⚠️ 确保逻辑删除字段（is_deleted）正确处理，避免数据'复活'

> ⚠️ 验证用例必须覆盖正常/边界/异常三类场景

> ⚠️ 事务回滚场景必须显式验证（不能仅依赖框架）

> ⚠️ 并发场景必须实际执行（不能仅写注释）

> ⚠️ 失败用例必须有明确修复方案或标记

# P1-004-001-005-001-001 编写CREATE TABLE语句

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-004-001-005-001-001 |
| 任务名称 | 编写CREATE TABLE语句 |
| 所属模块 | P1-004 |
| 优先级 | P1 |
| 任务类型 | DDL/数据库建表 |

## 二、任务目标

实现编写CREATE TABLE语句：基于doc_relation表和/api/doc接口，完成本任务的具体开发工作


## 三、前置依赖

### 3.1 前置任务

- P1-004-001-005-001 编写单据关联关系表DDL（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-数据库规范 | 数据库字段映射与SQL开发规范 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-004模块开发指南）
> - 本模块涉及数据表：doc_relation
> - 本模块涉及API：/api/doc-flow/import, /api/doc-flow/import/source, /api/doc-flow/push, /api/doc-flow/push/target-type, /api/doc-flow/copy
> - 本模块业务规则：引入服务：源单查询需支持按客户/供应商/日期/编号等多维度筛选，明细行勾选支持全选/部分选/反选。引入数据自动填充目标单主表/明细，支持字段映射配置。; 下推服务：下推操作需记录doc_relation关联关系，源单已下推数量需实时更新。下推时源单状态可配置（如"全部下推后自动标记已转单"）。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 开发规格
- 涉及数据表：doc_relation
- 涉及API：/api/doc-flow/import, /api/doc-flow/import/source, /api/doc-flow/push, /api/doc-flow/push/target-type, /api/doc-flow/copy
- 业务规则：引入服务：源单查询需支持按客户/供应商/日期/编号等多维度筛选，明细行勾选支持全选/部分选/反选。引入数据自动填充目标单主表/明细，支持字段映射配置。; 下推服务：下推操作需记录doc_relation关联关系，源单已下推数量需实时更新。下推时源单状态可配置（如"全部下推后自动标记已转单"）。

### 5.2 实现要求
1. 按任务目标逐项实现
2. 遵循项目代码规范
3. 关键操作添加日志记录
4. 异常场景统一抛出BusinessException


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/resources/mapper/flow/ | SQL映射文件 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | SQL语法正确 | SQL执行验证 |
| 2 | 查询结果正确 | 数据验证 |
| 3 | 性能指标达标(无全表扫描) | EXPLAIN分析 |
| 4 | 多租户隔离(tenant_id)条件完整 | 代码检查 |
## 八、易错警示

> ⚠️ SQL中勿使用SELECT *，明确列出所需字段

> ⚠️ 多表关联注意JOIN顺序和索引使用

> ⚠️ 动态SQL中注意SQL注入防护(使用#{}而非${})

> ⚠️ 多租户隔离(tenant_id)条件由拦截器自动添加

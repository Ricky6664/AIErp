# P1-008-002-010-001-003 主表联动逻辑

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-008-002-010-001-003 |
| 任务名称 | 主表联动逻辑 |
| 所属模块 | P1-008 |
| 优先级 | P1 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现ProdWorkOrder主表联动逻辑：客户变更联动刷新商品列表/价格策略、仓库变更联动刷新库位列表、日期变更联动刷新汇率


## 三、前置依赖

### 3.1 前置任务

- P1-008-002-010-001 主表字段+从表明细区（父任务）
- P1-008-002-010-001-002 主表字段组件渲染（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-008模块开发指南）
> - 本模块涉及数据表：prod_notice, prod_notice_detail, prod_notice_change, prod_mrp_config, prod_mrp_result, prod_picking, prod_picking_detail, prod_over_picking, prod_over_picking_detail, prod_return
> - 本模块涉及API：/api/production/unproduced, /api/production/unproduced/export, /api/production/notice, /api/production/notice-change, /api/production/mrp/config
> - 本模块业务规则：BOM自动展开：生产通知单明细选择产品后，系统根据BOM（来自P0-007商品管理）自动递归展开子件，展示预计用料（BOM子件用量×生产数量），支持多级BOM与替代料展示。; MRP运算引擎：MRP运算包含四个核心步骤——需求分析（销售订单+生产通知需求汇总）、BOM展开（多级递归+用量计算）、库存可用量检查（现有库存+在途+占用→净需求）、计划订单建议（净需求→采购建议/生产建议/委外建议）。运...
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 联动规则
```typescript
// 客户变更→联动刷新
watch(() => formData.customerId, async (newVal) => {
  if (newVal) {
    // 刷新客户价格策略
    priceStrategy.value = await api.getPriceStrategy(newVal)
    // 刷新客户收货地址
    addresses.value = await api.getCustomerAddresses(newVal)
  }
})

// 仓库变更→联动刷新库位列表
watch(() => formData.warehouseId, async (newVal) => {
  if (newVal) {
    locations.value = await api.getStorageLocations(newVal)
  }
})
```


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/production/form/index.vue | 联动逻辑 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 组件渲染无报错 | 浏览器验证 |
| 2 | 数据绑定正确 | 交互验证 |
| 3 | 交互逻辑正常 | 交互验证 |
| 4 | 与后端接口联调通过 | 联调验证 |
| 5 | 响应式布局正确 | 多分辨率验证 |
## 八、易错警示

> ⚠️ 组件命名遵循PascalCase，文件名与组件名一致

> ⚠️ 注意Hydration错误预防(SSR场景)

> ⚠️ 表格大数据量使用vxe-table虚拟滚动

> ⚠️ 表单校验与后端校验保持一致

> ⚠️ API调用错误统一使用ElMessage提示

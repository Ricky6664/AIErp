# P1-008-001-007-001-002 定义DTO类

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-008-001-007-001-002 |
| 任务名称 | 定义DTO类 |
| 所属模块 | P1-008 |
| 优先级 | P1 |
| 任务类型 | Entity/DTO/VO数据模型 |

## 二、任务目标

定义ProdWorkOrder的CreateDTO(@NotBlank/@NotNull校验必填字段)+UpdateDTO(含id)+QueryDTO(分页参数+筛选条件)，用于prod_notice表数据交互


## 三、前置依赖

### 3.1 前置任务

- P1-008-001-007-001 生产领料单Entity/DTO/VO定义（父任务）
- P1-008-001-007-001-001 定义Entity类（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-数据库规范 | 数据库字段映射与Entity模型规范 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-008模块开发指南）
> - 本模块涉及数据表：prod_notice, prod_notice_detail, prod_notice_change, prod_mrp_config, prod_mrp_result, prod_picking, prod_picking_detail, prod_over_picking, prod_over_picking_detail, prod_return
> - 本模块涉及API：/api/production/unproduced, /api/production/unproduced/export, /api/production/notice, /api/production/notice-change, /api/production/mrp/config
> - 本模块业务规则：BOM自动展开：生产通知单明细选择产品后，系统根据BOM（来自P0-007商品管理）自动递归展开子件，展示预计用料（BOM子件用量×生产数量），支持多级BOM与替代料展示。; MRP运算引擎：MRP运算包含四个核心步骤——需求分析（销售订单+生产通知需求汇总）、BOM展开（多级递归+用量计算）、库存可用量检查（现有库存+在途+占用→净需求）、计划订单建议（净需求→采购建议/生产建议/委外建议）。运...
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 CreateDTO（新增请求）
```java
@Data
public class ProdWorkOrderCreateDTO {
    @NotBlank(message = "名称不能为空")
    private String prodName;

    @NotNull(message = "日期不能为空")
    @JsonFormat(pattern = "yyyy-MM-dd")
    private LocalDate bizDate;

    @NotNull(message = "明细行不能为空")
    @Size(min = 1, message = "至少一条明细行")
    private List<ProdWorkOrderDetailCreateDTO> details;
}
```

### 5.2 UpdateDTO（修改请求）
```java
@Data
public class ProdWorkOrderUpdateDTO extends ProdWorkOrderCreateDTO {
    @NotNull(message = "ID不能为空")
    private Long id;
    private Integer version;  // 乐观锁版本号
}
```

### 5.3 QueryDTO（查询请求）
```java
@Data
public class ProdWorkOrderQueryDTO extends PageQuery {
    private String prodNo;    // 单据编号(模糊查询)
    private String prodName;  // 名称(模糊查询)
    private Integer status;        // 审核状态
    private LocalDate startDate;   // 开始日期
    private LocalDate endDate;     // 结束日期
}
```


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/production/dto/ProdWorkOrderCreateDTO.java | 新增DTO |
| 2 | src/main/java/com/erp/production/dto/ProdWorkOrderUpdateDTO.java | 修改DTO |
| 3 | src/main/java/com/erp/production/dto/ProdWorkOrderQueryDTO.java | 查询DTO |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | CreateDTO的@NotBlank/@NotNull校验注解完整 | 代码检查 |
| 2 | UpdateDTO包含id字段+@NotNull校验 | 代码检查 |
| 3 | QueryDTO继承PageQuery+筛选字段完整 | 代码检查 |
| 4 | 编译通过无警告 | mvn compile |
## 八、易错警示

> ⚠️ CreateDTO的@NotBlank/@NotNull校验message必须明确(中文提示)

> ⚠️ UpdateDTO必须包含id字段+@NotNull校验

> ⚠️ QueryDTO继承PageQuery，勿自定义pageNum/pageSize字段

> ⚠️ 日期类型使用LocalDate/LocalDateTime，勿用java.util.Date

> ⚠️ 明细行DTO使用@Size(min=1)校验非空，勿用@NotNull

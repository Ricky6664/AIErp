# P1-008-001-004-003-002 实现ServiceImpl

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-008-001-004-003-002 |
| 任务名称 | 实现ServiceImpl |
| 所属模块 | P1-008 |
| 优先级 | P1 |
| 任务类型 | Service服务层 |

## 二、任务目标

实现ProdWorkOrderServiceImpl，继承ServiceImpl<ProdWorkOrderMapper, ProdWorkOrderEntity>，实现prod_notice表核心业务逻辑+数据校验+事务管理+编码生成+审核状态流转


## 三、前置依赖

### 3.1 前置任务

- P1-008-001-004-003 库存可用量检查Service（父任务）
- P1-008-001-004-003-001 定义Service接口（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-008模块开发指南）
> - 本模块涉及数据表：prod_notice, prod_notice_detail, prod_notice_change, prod_mrp_config, prod_mrp_result, prod_picking, prod_picking_detail, prod_over_picking, prod_over_picking_detail, prod_return
> - 本模块涉及API：/api/production/unproduced, /api/production/unproduced/export, /api/production/notice, /api/production/notice-change, /api/production/mrp/config
> - 本模块业务规则：BOM自动展开：生产通知单明细选择产品后，系统根据BOM（来自P0-007商品管理）自动递归展开子件，展示预计用料（BOM子件用量×生产数量），支持多级BOM与替代料展示。; MRP运算引擎：MRP运算包含四个核心步骤——需求分析（销售订单+生产通知需求汇总）、BOM展开（多级递归+用量计算）、库存可用量检查（现有库存+在途+占用→净需求）、计划订单建议（净需求→采购建议/生产建议/委外建议）。运...
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 ServiceImpl核心结构
```java
@Service
@RequiredArgsConstructor
public class ProdWorkOrderServiceImpl extends ServiceImpl<ProdWorkOrderMapper, ProdWorkOrderEntity> implements IProdWorkOrderService {
    private final ProdWorkOrderMapper prodMapper;
    private final IAuditEngineService auditEngineService;
    private final ICodeGeneratorService codeGeneratorService;

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long create(ProdWorkOrderCreateDTO dto) {
        // 1. 业务校验
        validateCreate(dto);
        // 2. 生成编码
        String code = codeGeneratorService.generate("prod");
        // 3. 构建Entity并保存
        ProdWorkOrderEntity entity = BeanUtil.copyProperties(dto, ProdWorkOrderEntity.class);
        entity.setNo(code);
        entity.setStatus(0); // 草稿
        save(entity);
        // 4. 保存明细行
        saveDetails(entity.getId(), dto.getDetails());
        return entity.getId();
    }
}
```

### 5.2 业务校验规则
1. 名称唯一性校验：同一租户下名称不重复
2. 明细行非空校验：至少包含一条明细行
3. 金额计算校验：单价×数量=金额，精度保留4位小数
4. 状态流转校验：仅草稿状态(0)可提交审核


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/production/service/IProdWorkOrderService.java | Service接口 |
| 2 | src/main/java/com/erp/production/service/impl/ProdWorkOrderServiceImpl.java | Service实现类 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | ProdWorkOrderServiceImpl继承ServiceImpl<Mapper, Entity> | 代码检查 |
| 2 | @Service注解+@Transactional事务注解正确 | 代码检查 |
| 3 | CRUD方法逻辑正确(校验+转换+保存) | 代码审查 |
| 4 | 编码生成调用幂等 | 单元测试全部通过 + 手动验证核心场景 |
| 5 | 审核状态流转正确 | 单元测试全部通过 + 手动验证核心场景 |
| 6 | 单元测试通过 | mvn test |
## 八、易错警示

> ⚠️ @Transactional(rollbackFor=Exception.class)必须加在public方法上

> ⚠️ 避免大事务：仅包含数据库操作，远程调用/RPC放在事务外

> ⚠️ 编码生成调用要幂等（重试时不产生重复编码）

> ⚠️ 异常统一抛出BusinessException，携带错误码+中文提示

> ⚠️ 乐观锁@Version字段在update时通过WHERE version=#{oldVersion}实现

> ⚠️ 多表操作注意操作顺序：先主表后从表(新增)，先从表后主表(删除)

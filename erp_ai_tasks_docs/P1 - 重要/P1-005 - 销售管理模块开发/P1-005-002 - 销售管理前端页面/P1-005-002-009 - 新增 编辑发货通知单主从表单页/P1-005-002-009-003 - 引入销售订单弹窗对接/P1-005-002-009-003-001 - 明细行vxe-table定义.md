# P1-005-002-009-003-001 明细行vxe-table定义

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-005-002-009-003-001 |
| 任务名称 | 明细行vxe-table定义 |
| 所属模块 | P1-005 |
| 优先级 | P1 |
| 任务类型 | 前端数据表格 |

## 二、任务目标

定义SaleQuotation明细行vxe-table列配置：物料/数量/单价/金额等字段列、可编辑列配置、行内编辑模式


## 三、前置依赖

### 3.1 前置任务

- P1-005-002-009-003 引入销售订单弹窗对接（父任务）

### 3.2 前置资源

- 对应后端Controller接口已就绪
- API接口文档已确认
- 前端框架基础组件就绪

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-005模块开发指南）
> - 本模块涉及数据表：sale_quotation, sale_quotation_detail, sale_order, sale_order_detail, sale_order_detail_serial, sale_order_change, sale_delivery_notice, sale_delivery_notice_detail, sale_outbound, sale_outbound_detail
> - 本模块涉及API：/api/sale/quotation, /api/sale/order, /api/sale/order-change, /api/sale/unshipped, /api/sale/unshipped/export
> - 本模块业务规则：单据流转路径：报价→订单→发货通知→出库→退货→对账，流转路径由系统参数控制（如sale.order_flow_path），参数缺失时提示用户。强制流转选项允许跳过中间环节。; 金额计算引擎：所有销售单据的金额计算（单价×数量+税率+折扣=价税合计）统一调用金额计算引擎，确保精度一致（金额保留4位小数）。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 明细行列定义
```typescript
const detailColumns: VxeTablePropTypes.Columns = [
  { type: 'index', title: '序号', width: 60 },
  { field: 'materialCode', title: '物料编码', width: 150, editRender: { name: 'MaterialSelector' } },
  { field: 'materialName', title: '物料名称', minWidth: 200 },
  { field: 'qty', title: '数量', width: 120, editRender: { name: 'input', attrs: { type: 'number' } } },
  { field: 'price', title: '单价', width: 120, editRender: { name: 'input', attrs: { type: 'number' } } },
  { field: 'amount', title: '金额', width: 120, formatter: formatAmount },
  { field: 'action', title: '操作', width: 80, slots: { default: 'detail_action_slot' } }
]
```

### 5.2 编辑配置
- 编辑模式：行内编辑(click激活)
- 可编辑列：物料编码、数量、单价、备注
- 自动计算列：金额=数量×单价


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/views/sale/columns/detailColumns.ts | 明细行列定义 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 明细行表格列定义正确 | 浏览器验证 |
| 2 | 可编辑列配置生效 | 交互验证 |
| 3 | 行内编辑模式正常 | 交互验证 |
## 八、易错警示

> ⚠️ 编辑列edit_render配置必须与表格edit-config配合使用

> ⚠️ 可编辑表格必须设置row-config.useKey=true(有唯一标识)

> ⚠️ 数字输入框type=number，配合min/max限制范围

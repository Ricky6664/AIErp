# P1-009-001-007-001-002 验证功能

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-009-001-007-001-002 |
| 任务名称 | 验证功能 |
| 所属模块 | P1-009 |
| 优先级 | P1 |
| 任务类型 | 验证/测试 |

## 二、任务目标

验证委外管理模块核心接口的业务逻辑正确性，覆盖正常流程、边界值、并发场景与异常降级场景，确保单据状态流转、金额计算、数量累计等关键指标符合业务规则

## 三、前置依赖

### 3.1 前置任务

- P1-009-001-007-001 实现核心逻辑（父任务）
- P1-009-001-007-001-001 编写核心代码（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-测试开发规范 | 测试开发与验证规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：P1-009模块开发指南）
> - 本模块涉及数据表：sub_notice, sub_notice_detail, sub_notice_change, sub_picking, sub_picking_detail, sub_over_picking, sub_over_picking_detail, sub_return, sub_return_detail, sub_receipt
> - 本模块涉及API：/api/subcontract/unproduced, /api/subcontract/unproduced/export, /api/subcontract/notice, /api/subcontract/notice-change, /api/subcontract/kanban
> - 本模块业务规则：委外与生产结构对称：委外管理的单据结构与生产管理高度对称（通知→领料→入库→退库→工序移交接收），但委外管理关联供应商维度，生产管理关联生产部门维度。; 加工费管理：委外通知单明细包含加工单价和加工费字段，入库时自动汇总加工费，为后续应付管理和成本核算提供数据依据。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 验证清单
1. 核心用例验证：按委外管理业务规则逐项验证主流程（新增/修改/删除/查询/审核）
2. 数值计算验证：金额=单价×数量、合计=∑明细，误差≤0.01
3. 状态流转验证：按状态机规则验证非法流转拒绝（如已审核→草稿被拦截）
4. 并发场景验证：乐观锁冲突时正确重试/报错

### 5.2 测试方法
1. 单元测试：JUnit5 + Mockito，覆盖Service层所有public方法
2. 接口测试：Postman/Swagger UI验证/api/subcontract/*接口契约
3. 事务测试：手动抛异常验证回滚行为
4. 并发测试：使用CountDownLatch模拟并发更新，验证乐观锁生效

### 5.3 结果记录
1. 通过项：记录用例名+执行时间+断言结果
2. 未通过项：记录失败原因+堆栈+数据快照
3. 修复方案：标注代码位置+修复思路+回归验证结果

## 六、交付物清单
| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/test/java/com/erp/subcontract/XxxTest.java | 测试类 |
| 2 | docs/test-reports/验证报告.md | 验证结果记录 |

## 七、验收标准
| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 所有验证用例通过 | 测试报告 | 测试执行记录文档 |
| 2 | 分支覆盖率≥80% | JaCoCo报告 |
| 3 | 事务回滚场景验证通过 | 异常注入测试 |
| 4 | 失败用例已修复或标记 | 问题清单Review |
| 5 | 回归测试通过 | 回归测试执行 |

## 八、易错警示
> ⚠️ 代码提交前确保无敏感信息硬编码（密码/密钥/token）

> ⚠️ 注意多租户隔离（tenant_id）贯穿SQL/业务逻辑/缓存

> ⚠️ 确保逻辑删除字段（is_deleted）正确处理，避免数据'复活'

> ⚠️ 验证用例必须覆盖正常/边界/异常三类场景

> ⚠️ 事务回滚场景必须显式验证（不能仅依赖框架）

> ⚠️ 并发场景必须实际执行（不能仅写注释）

> ⚠️ 失败用例必须有明确修复方案或标记

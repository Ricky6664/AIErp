# P1-003-001-001-001-001 编写接口定义Service接口

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-003-001-001-001-001 |
| 任务名称 | 编写接口定义Service接口 |
| 所属模块 | P1-003 |
| 优先级 | P1 |
| 任务类型 | Service服务层 |

## 二、任务目标

实现编写接口定义Service接口：基于msg_message表和/api/message/workbench接口，完成本任务的具体开发工作


## 三、前置依赖

### 3.1 前置任务

- P1-003-001-001-001 Service接口定义+实现类编写（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P1-003模块开发指南）
> - 本模块涉及数据表：msg_message, msg_template, msg_type, msg_todo, msg_push_config, msg_alert_rule, msg_doc_comment, msg_discussion, msg_discussion_reply
> - 本模块涉及API：/api/message/workbench, /api/message/message, /api/message/message/read-all, /api/message/template, /api/message/type
> - 本模块业务规则：WebSocket连接管理：WebSocket推送服务需支持连接池管理、断线重连、心跳检测。每个用户仅维护一个有效连接，多设备登录时需同步推送。; 消息模板变量替换：模板内容支持${变量名}格式的变量替换，变量值由业务模块在发送消息时传入。模板变量列表需在模板定义时声明。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。
### 5.1 开发规格
- 涉及数据表：msg_message, msg_template, msg_type, msg_todo, msg_push_config, msg_alert_rule, msg_doc_comment, msg_discussion, msg_discussion_reply
- 涉及API：/api/message/workbench, /api/message/message, /api/message/message/read-all, /api/message/template, /api/message/type
- 业务规则：WebSocket连接管理：WebSocket推送服务需支持连接池管理、断线重连、心跳检测。每个用户仅维护一个有效连接，多设备登录时需同步推送。; 消息模板变量替换：模板内容支持${变量名}格式的变量替换，变量值由业务模块在发送消息时传入。模板变量列表需在模板定义时声明。

### 5.2 实现要求
1. 按任务目标逐项实现
2. 遵循项目代码规范
3. 关键操作添加日志记录
4. 异常场景统一抛出BusinessException


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/message/ | Service接口定义文件 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 所有功能点已实现且通过验证 | 人工检查/自动化测试 |
| 2 | 代码编译/运行无错 | mvn compile + mvn test |
| 3 | 符合全局规范与层级规范约束 | 代码审查 |
| 4 | 无遗漏交付物 | 清单核对 |
## 八、易错警示

> ⚠️ 事务注解@Transactional要加在正确的位置，避免大事务

> ⚠️ 异常统一抛出BusinessException，携带错误码+中文提示

> ⚠️ 注意多租户隔离(tenant_id)，由拦截器自动附加

> ⚠️ 确保逻辑删除字段(is_deleted)正确处理，查询自动过滤

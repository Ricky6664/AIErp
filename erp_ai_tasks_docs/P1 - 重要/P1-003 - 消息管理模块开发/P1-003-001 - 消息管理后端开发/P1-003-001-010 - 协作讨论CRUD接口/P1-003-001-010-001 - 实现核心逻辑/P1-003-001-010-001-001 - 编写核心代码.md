# P1-003-001-010-001-001 编写核心代码

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P1-003-001-010-001-001 |
| 任务名称 | 编写核心代码 |
| 所属模块 | P1-003 |
| 优先级 | P1 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现MsgCollaborationService协作讨论核心业务逻辑：讨论主题CRUD（含参与人管理）、讨论回复（楼中楼回复+参与人校验+讨论关闭拦截）、@提及解析与通知推送、讨论关闭/重开权限控制，遵循参与人权限管控+消息通知联动要求

## 三、前置依赖

### 3.1 前置任务

- P1-003-001-010-001 实现核心逻辑（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量规范 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格

### 5.1 核心Service实现
```java
@Service
@RequiredArgsConstructor
@Slf4j
public class MsgCollaborationServiceImpl implements MsgCollaborationService {

    private final MsgCollaborationMapper collaborationMapper;
    private final MsgCollaborationReplyMapper replyMapper;
    private final MsgMessageService messageService;
    private final MsgWebSocketService webSocketService;
    private final MentionParser mentionParser;

    @Override
    public PageResult<CollaborationListVO> page(CollaborationQueryDTO query) {
        Long currentUserId = SecurityUtils.getCurrentUserId();
        query.setParticipantId(currentUserId);
        Page<MsgCollaborationEntity> page = collaborationMapper.selectPage(query);
        return convertToVO(page);
    }

    @Override
    public CollaborationDetailVO getById(Long id) {
        MsgCollaborationEntity collab = collaborationMapper.selectById(id);
        if (collab == null) throw new BusinessException("讨论主题不存在");
        CollaborationDetailVO vo = toDetailVO(collab);
        List<MsgCollaborationReplyEntity> replies = replyMapper.selectByCollaborationId(id);
        vo.setReplies(buildReplyTree(replies));
        vo.setParticipants(getParticipantUsers(collab.getParticipants()));
        return vo;
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long create(CollaborationCreateDTO dto) {
        Long currentUserId = SecurityUtils.getCurrentUserId();
        if (dto.getParticipantIds() == null || dto.getParticipantIds().isEmpty()) {
            throw new BusinessException("至少需要一个参与人");
        }
        MsgCollaborationEntity entity = new MsgCollaborationEntity();
        entity.setTopic(dto.getTopic());
        entity.setCategory(dto.getCategory() != null ? dto.getCategory() : "general");
        entity.setContent(dto.getContent());
        entity.setCreatorId(currentUserId);
        List<Long> participants = new ArrayList<>(dto.getParticipantIds());
        if (!participants.contains(currentUserId)) participants.add(currentUserId);
        entity.setParticipants(JsonUtils.toJson(participants));
        collaborationMapper.insert(entity);
        notifyParticipants(participants, currentUserId, "您有新的协作讨论：" + dto.getTopic(), entity);
        return entity.getId();
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long reply(Long collabId, ReplyCreateDTO dto) {
        Long currentUserId = SecurityUtils.getCurrentUserId();
        MsgCollaborationEntity collab = collaborationMapper.selectById(collabId);
        if (collab == null) throw new BusinessException("讨论主题不存在");
        if (collab.getIsClosed()) throw new BusinessException("讨论已关闭，无法回复");
        List<Long> participants = JsonUtils.parseList(collab.getParticipants(), Long.class);
        if (!participants.contains(currentUserId) && !collab.getCreatorId().equals(currentUserId)) {
            throw new BusinessException("仅参与人可回复讨论");
        }
        MsgCollaborationReplyEntity reply = new MsgCollaborationReplyEntity();
        reply.setCollaborationId(collabId);
        reply.setReplyContent(dto.getReplyContent());
        reply.setReplierId(currentUserId);
        reply.setParentReplyId(dto.getParentReplyId() != null ? dto.getParentReplyId() : 0L);
        replyMapper.insert(reply);
        // 推送通知给所有参与人（排除自己）
        List<Long> notifyUsers = participants.stream()
            .filter(uid -> !uid.equals(currentUserId)).collect(Collectors.toList());
        List<Long> mentionedUsers = mentionParser.parseMentionedUsers(dto.getReplyContent());
        notifyUsers.addAll(mentionedUsers);
        notifyUsers = notifyUsers.stream().distinct().collect(Collectors.toList());
        notifyUsers.forEach(uid -> {
            messageService.sendMessage("协作讨论新回复", collab.getTopic(), "discussion", uid);
            webSocketService.pushToUser(uid, buildReplyPushMessage(collab, reply));
        });
        return reply.getId();
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public void closeDiscussion(Long collabId) {
        MsgCollaborationEntity collab = collaborationMapper.selectById(collabId);
        if (collab == null) throw new BusinessException("讨论主题不存在");
        Long currentUserId = SecurityUtils.getCurrentUserId();
        if (!collab.getCreatorId().equals(currentUserId) && !SecurityUtils.isAdmin()) {
            throw new BusinessException("仅发起人或管理员可关闭讨论");
        }
        collab.setIsClosed(true);
        collab.setCloseTime(LocalDateTime.now());
        collaborationMapper.updateById(collab);
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public void reopenDiscussion(Long collabId) {
        MsgCollaborationEntity collab = collaborationMapper.selectById(collabId);
        if (collab == null) throw new BusinessException("讨论主题不存在");
        Long currentUserId = SecurityUtils.getCurrentUserId();
        if (!collab.getCreatorId().equals(currentUserId) && !SecurityUtils.isAdmin()) {
            throw new BusinessException("仅发起人或管理员可重开讨论");
        }
        collab.setIsClosed(false);
        collab.setCloseTime(null);
        collaborationMapper.updateById(collab);
    }
}
```

### 5.2 回复树构建
```java
private List<ReplyTreeVO> buildReplyTree(List<MsgCollaborationReplyEntity> replies) {
    Map<Long, ReplyTreeVO> voMap = new LinkedHashMap<>();
    List<ReplyTreeVO> roots = new ArrayList<>();
    replies.forEach(r -> {
        ReplyTreeVO vo = toReplyVO(r);
        vo.setChildReplies(new ArrayList<>());
        voMap.put(r.getId(), vo);
    });
    replies.forEach(r -> {
        ReplyTreeVO vo = voMap.get(r.getId());
        if (r.getParentReplyId() == null || r.getParentReplyId() == 0L || !voMap.containsKey(r.getParentReplyId())) {
            roots.add(vo);
        } else {
            voMap.get(r.getParentReplyId()).getChildReplies().add(vo);
        }
    });
    return roots;
}
```

### 5.3 数据交互
- 涉及数据表：msg_collaboration（协作讨论主题表）、msg_collaboration_reply（协作讨论回复表）
- 涉及API：GET /api/message/discussion（分页查询）、GET /api/message/discussion/{id}（详情含回复）、POST /api/message/discussion（新增主题）、POST /api/message/discussion/{id}/reply（回复）、PUT /api/message/discussion/{id}/close（关闭）、PUT /api/message/discussion/{id}/reopen（重开）
- 依赖服务：MsgMessageService（通知推送）、MsgWebSocketService（实时推送）、MentionParser（@提及解析）

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/message/service/impl/MsgCollaborationServiceImpl.java | 协作讨论Service实现 |
| 2 | src/main/java/com/erp/message/vo/ReplyTreeVO.java | 楼中楼回复树形VO |
| 3 | src/main/java/com/erp/message/controller/MsgCollaborationController.java | 协作讨论接口Controller |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 创建讨论正确保存主题并将创建人加入参与人列表 | 单元测试 |
| 2 | 参与人非空校验：无参与人时抛出BusinessException | 单元测试 |
| 3 | 回复时正确校验参与人权限，非参与人无法回复 | 单元测试 |
| 4 | 讨论已关闭时回复抛出"讨论已关闭"异常 | 单元测试 |
| 5 | 楼中楼回复（parentReplyId）正确构建树形结构 | 单元测试 |
| 6 | 新回复通知推送给所有参与人（排除回复人自己） | 集成测试 |
| 7 | 关闭/重开讨论仅创建人或管理员可操作 | 单元测试 |
| 8 | 编译通过无警告 | mvn compile |

## 八、易错警示

> ⚠️ participants字段为jsonb类型，存储为JSON数组，读取时需使用JsonUtils.parseList反序列化

> ⚠️ 楼中楼回复的树构建需注意parentReplyId指向的回复可能已被删除，需做容错处理（归入根级）

> ⚠️ 关闭/重开权限校验需同时判断creatorId和isAdmin()，避免仅依赖创建人导致管理员无法操作

> ⚠️ 回复通知需做去重：同一用户既是参与人又被@提及时，只发一次通知
# P0-003-021-001-001-001 编写核心代码

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-003-021-001-001-001 |
| 任务名称 | 编写核心代码 |
| 所属模块 | P0-003 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标


编写系统初始化数据DML脚本：通过Flyway迁移脚本插入系统预置数据(管理员账号/默认角色/系统参数/数据字典/编码规则/菜单/数据视图)，使用INSERT INTO...ON CONFLICT DO NOTHING避免重复

## 三、前置依赖

### 3.1 前置任务

- P0-003-021-001-001 实现核心逻辑（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用



| 规范文档名 | 引用原因 |
|-----------|--------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量标准 |
| 全局规范-数据库规范 | 数据库设计与DDL规范约束 |

## 五、详细开发规格





> **本任务模块上下文**（来源：P0-003模块开发指南）
> - 本模块技术栈：PostgreSQL 15+ / Flyway迁移 / Schema多租户 / 公共字段基座规范
> - 通用字段强制约束：所有业务表必须包含10个通用字段(id(BIGINT PK), tenant_id(BIGINT NOT NULL), created_by(BIGINT), created_at(TIMESTAMP DEFAULT CURRENT_TIMESTAMP), updated_by(BIGINT), updated_at(TIMESTAMP DEFAULT CURRENT_TIMESTAMP), is_deleted(BOOLEAN DEFAULT false), owner_dept_id(BIGINT), owner_id(BIGINT), version(INT DEFAULT 1))
> - 数值精度约束：所有金额/单价/数量/转换率字段统一使用decimal(18,8)
> - 部分唯一索引约束：业务唯一性字段必须采用`CREATE UNIQUE INDEX ... WHERE is_deleted = false`
> - 多租户隔离：tenant_id字段不可为空，联合索引必须以tenant_id为首列
> - Flyway命名规范：V{yyyyMMdd}{seq}__{description}.sql

### 5.1 初始化数据范围
- 超级管理员账号(admin/BCrypt加密)、默认角色、系统预置参数、数据字典、编码规则、菜单数据、数据视图

### 5.2 DML脚本规范
- INSERT INTO...ON CONFLICT DO NOTHING保证幂等性
- 系统级数据tenant_id=0、密码BCrypt加密

### 5.3 Flyway迁移脚本
- 命名：V20260526900__init_system_data.sql（序号900+，在DDL之后）

## 六、交付物清单



| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | db/migration/V20260526900__init_system_data.sql | 系统初始化数据DML脚本 |
| 2 | db/migration/V20260526901__init_dict_data.sql | 数据字典初始化脚本 |
| 3 | db/migration/V20260526902__init_menu_data.sql | 菜单数据初始化脚本 |

## 七、验收标准



| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | 管理员账号admin插入成功 | 登录验证 |
| 2 | 系统参数/字典/编码/菜单数据完整 | SELECT COUNT(*)验证 |
| 3 | DML幂等(重复执行不报错不重复) | 执行两次验证 |
| 4 | 预置数据tenant_id=0 | SELECT验证 |
| 5 | Flyway记录success=true | flyway_schema_history查询 |

## 八、易错警示


> ⚠️ DML脚本必须使用INSERT INTO...ON CONFLICT DO NOTHING保证幂等性，避免重复执行报唯一约束冲突

> ⚠️ 系统级预置数据tenant_id必须设置为0，避免被多租户插件过滤导致所有租户都无法访问

> ⚠️ 管理员密码必须使用BCrypt加密存储，禁止明文存储

> ⚠️ 初始化数据执行顺序：先DDL建表(001-899)再初始化数据(900+)

> ⚠️ 菜单数据parent_id引用必须正确，树形结构断裂将导致前端菜单渲染异常

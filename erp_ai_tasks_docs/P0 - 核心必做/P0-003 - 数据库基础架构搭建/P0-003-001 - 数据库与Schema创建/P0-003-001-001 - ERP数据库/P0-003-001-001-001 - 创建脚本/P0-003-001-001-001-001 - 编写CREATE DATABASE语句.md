# P0-003-001-001-001-001 编写CREATE DATABASE语句

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-003-001-001-001-001 |
| 任务名称 | 编写CREATE DATABASE语句 |
| 所属模块 | P0-003 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标


编写CREATE DATABASE语句创建ERP系统PostgreSQL数据库，指定UTF8编码、LC_COLLATE/LC_CTYPE区域设置、默认表空间，并创建业务Schema(多租户隔离Schema)

## 三、前置依赖

### 3.1 前置任务

- P0-003-001-001-001 执行创建脚本（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用



| 规范文档名 | 引用原因 |
|-----------|--------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量标准 |
| 全局规范-数据库规范 | 数据库设计与DDL规范约束 |

## 五、详细开发规格





> **本任务模块上下文**（来源：P0-003模块开发指南）
> - 本模块技术栈：PostgreSQL 15+ / Flyway迁移 / Schema多租户 / 公共字段基座规范
> - 通用字段强制约束：所有业务表必须包含10个通用字段(id(BIGINT PK), tenant_id(BIGINT NOT NULL), created_by(BIGINT), created_at(TIMESTAMP DEFAULT CURRENT_TIMESTAMP), updated_by(BIGINT), updated_at(TIMESTAMP DEFAULT CURRENT_TIMESTAMP), is_deleted(BOOLEAN DEFAULT false), owner_dept_id(BIGINT), owner_id(BIGINT), version(INT DEFAULT 1))
> - 数值精度约束：所有金额/单价/数量/转换率字段统一使用decimal(18,8)
> - 部分唯一索引约束：业务唯一性字段必须采用`CREATE UNIQUE INDEX ... WHERE is_deleted = false`
> - 多租户隔离：tenant_id字段不可为空，联合索引必须以tenant_id为首列
> - Flyway命名规范：V{yyyyMMdd}{seq}__{description}.sql

### 5.1 核心功能
1. 实现编写CREATE DATABASE语句的核心逻辑
2. 遵循PostgreSQL 15+语法规范
3. 遵循Flyway迁移脚本命名规范
4. 确保通用字段10个完整包含

### 5.2 技术规范
1. 数值字段统一使用decimal(18,8)
2. 部分唯一索引包含WHERE is_deleted=false
3. 多租户tenant_id作为联合索引首列
4. 所有表和字段必须包含COMMENT注释

### 5.3 验证方式
1. DDL/DML在PostgreSQL 15中执行无错误
2. Flyway版本号正确无冲突
3. 数据完整性校验通过

## 六、交付物清单



| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | db/migration/V20260526001__chema_related.sql | chema相关DDL/DML脚本 |
| 2 | docs/specs/P0_003_001_001_001_001_spec.md | 规范文档 |

## 七、验收标准



| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | 脚本执行成功，无SQL语法错误 | psql执行验证 |
| 2 | 数据完整性校验通过 | SELECT COUNT(*)验证 |
| 3 | 幂等性验证 | 执行两次验证 |
| 4 | Flyway版本号无冲突 | flyway_schema_history查询 |
| 5 | COMMENT注释完整 | pg_class/pg_attribute查询 |

## 八、易错警示


> ⚠️ 通用字段10个必须完整包含(id/tenant_id/created_at/updated_at/created_by/updated_by/is_deleted/owner_dept_id/owner_id/version)

> ⚠️ 部分唯一索引必须包含`WHERE is_deleted = false`条件

> ⚠️ 数值字段统一使用decimal(18,8)

> ⚠️ Flyway脚本命名使用V{yyyyMMdd}{seq}__{description}.sql（双下划线）

> ⚠️ DML脚本使用ON CONFLICT保证幂等性

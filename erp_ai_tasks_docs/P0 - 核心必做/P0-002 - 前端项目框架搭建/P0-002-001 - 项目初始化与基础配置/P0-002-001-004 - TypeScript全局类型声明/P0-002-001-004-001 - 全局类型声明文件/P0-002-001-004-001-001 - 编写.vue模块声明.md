# P0-002-001-004-001-001 编写*.vue模块声明

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-001-004-001-001 |
| 任务名称 | 编写*.vue模块声明 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

编写src/types/shims-vue.d.ts模块声明文件：使用`declare module '*.vue'`声明Vue单文件组件模块类型，导出类型为`DefineComponent<{}, {}, any>`使TypeScript能正确识别.vue文件导入，避免在import VueComponent from './xxx.vue'时IDE报类型错误

## 三、前置依赖

### 3.1 前置任务

- P0-002-001-004-001 全局类型声明文件（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node.js 18+ + pnpm 9.x + Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：TypeScript类型声明
> - 技术栈：TypeScript 5.x + Vue 3.5.x
> - 目的：让TypeScript识别.vue文件导入，消除IDE类型错误

### 5.1 声明文件内容
```typescript
// src/types/shims-vue.d.ts
declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

### 5.2 关键设计点
| 要点 | 说明 |
|------|------|
| DefineComponent泛型 | 使用`<{}, {}, any>`分别对应Props/RawBindings/公实例类型 |
| 不使用any直接导出 | `export default component`而非`export default any`保留类型推断 |
| 文件位置 | 必须在src/types/目录下且被tsconfig.json的include覆盖 |
| 与Volar兼容 | 不安装@vue/runtime-core类型包时此声明生效，安装后Volar自动接管 |

### 5.3 验证集成
- tsconfig.json的include必须包含`"src/types/**/*.d.ts"`
- 在任意.ts文件中`import App from './App.vue'`，IDE应正确推断类型为DefineComponent

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-web/src/types/shims-vue.d.ts | Vue单文件组件模块类型声明 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | declare module '*.vue' 包含Component类型定义 | tsc --noEmit 编译通过 |
| 2 | 导入.vue文件时IDE不报类型错误 | VSCode/WebStorm中验证导入提示 |
| 3 | 使用DefineComponent泛型约束组件类型 | 人工检查类型推断 |

## 八、易错警示

> ⚠️ declare module '*.vue' 必须使用 DefineComponent 泛型，不能简单声明为 any

> ⚠️ 文件必须放在 src/types/ 目录下且被 tsconfig.json 的 include 覆盖

> ⚠️ 不要与 volar 的自动类型推断冲突，避免重复声明

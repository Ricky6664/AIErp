# P0-002-001-004-001-002 编写env.d.ts环境变量声明

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-001-004-001-002 |
| 任务名称 | 编写env.d.ts环境变量声明 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

编写Vite环境变量TypeScript类型声明文件src/env.d.ts：声明ImportMetaEnv接口包含所有VITE_前缀变量的类型定义（VITE_APP_TITLE: string、VITE_API_BASE_URL: string、VITE_USE_MOCK: string等）、扩展ImportMeta接口使import.meta.env具有完整类型提示和自动补全

## 三、前置依赖

### 3.1 前置任务

- P0-002-001-004-001 全局类型声明文件（父任务）
- P0-002-001-004-001-001 编写*.vue模块声明（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node.js 18+ + pnpm 9.x + Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-部署架构规范 | 部署与环境配置规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：TypeScript类型声明
> - 技术栈：TypeScript 5.x + Vite 6.x
> - 目的：让import.meta.env具有完整类型提示和自动补全，避免运行时拼写错误

### 5.1 声明文件内容
```typescript
// src/env.d.ts
/// <reference types="vite/client" />

interface ImportMetaEnv {
  /** 应用标题 */
  readonly VITE_APP_TITLE: string
  /** 后端API基础URL */
  readonly VITE_API_BASE_URL: string
  /** 是否启用Mock数据 (true/false) */
  readonly VITE_USE_MOCK: string
  /** 当前环境标识 */
  readonly VITE_APP_ENV: 'development' | 'staging' | 'production'
  /** WebSocket地址 */
  readonly VITE_WS_URL: string
  /** 文件上传CDN地址 */
  readonly VITE_CDN_BASE_URL: string
}

interface ImportMeta {
  readonly env: ImportMetaEnv
}
```

### 5.2 关键设计点
| 要点 | 说明 |
|------|------|
| 所有字段为string | Vite环境变量统一为字符串类型，boolean/number需在代码中转换 |
| readonly修饰 | 防止代码中意外修改环境变量值 |
| JSDoc注释 | 每个变量必须有中文说明，IDE悬浮提示时展示 |
| 与.env文件同步 | 新增VITE_变量时必须同步更新此声明文件 |

### 5.3 环境文件对照
| .env文件 | VITE_API_BASE_URL示例 | 说明 |
|----------|---------------------|------|
| .env | http://localhost:8080 | 所有环境默认 |
| .env.development | http://localhost:8080 | 本地开发 |
| .env.staging | https://staging-api.example.com | 测试环境 |
| .env.production | https://api.example.com | 生产环境 |

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-web/src/env.d.ts | Vite环境变量类型声明文件 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | ImportMetaEnv接口包含所有VITE_前缀变量 | tsc --noEmit 编译通过 |
| 2 | import.meta.env.VITE_* 有正确类型提示 | VSCode中验证自动补全 |
| 3 | 声明了ImportMeta接口扩展 | 人工检查接口定义完整性 |

## 八、易错警示

> ⚠️ ImportMetaEnv 中的字段类型应为 string，Vite 环境变量均为字符串

> ⚠️ 不要硬编码敏感值到类型声明文件中

> ⚠️ 确保 .env / .env.production 中的变量名与 env.d.ts 声明一一对应

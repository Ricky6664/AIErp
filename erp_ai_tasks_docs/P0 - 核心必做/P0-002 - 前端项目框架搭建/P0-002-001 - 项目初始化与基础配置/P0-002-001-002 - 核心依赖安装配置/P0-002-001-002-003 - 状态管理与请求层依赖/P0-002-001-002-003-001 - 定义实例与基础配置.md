# P0-002-001-002-003-001 定义实例与基础配置

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-001-002-003-001 |
| 任务名称 | 定义实例与基础配置 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 配置/基础设施 |

## 二、任务目标

安装状态管理与请求层依赖：pnpm add pinia axios nprogress @vueuse/core dayjs echarts lodash-es、pnpm add -D @types/nprogress @types/lodash-es、确认package.json中版本号：pinia 2.x, axios 1.x, vueuse 10.x, dayjs 1.x

## 三、前置依赖

### 3.1 前置任务

- P0-002-001-002-003 状态管理与请求层依赖（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node.js 18+ + pnpm 9.x + Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-部署架构规范 | 部署与环境配置规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：前端基础设施配置
> - 技术栈：Vue 3.5.x + Pinia 2.x + Axios 1.x + TypeScript 5.x
> - 项目目录：erp-web/（前端项目根目录）

### 5.1 安装依赖
```bash
# 运行时依赖
pnpm add pinia axios nprogress @vueuse/core dayjs echarts lodash-es

# 类型声明（开发依赖）
pnpm add -D @types/nprogress @types/lodash-es
```

### 5.2 版本确认（package.json）
| 依赖 | 版本要求 | 用途 |
|------|---------|------|
| `pinia` | `^2.x` | Vue 3 状态管理 |
| `axios` | `^1.x` | HTTP 请求 |
| `nprogress` | `^0.2.x` | 页面加载进度条 |
| `@vueuse/core` | `^10.x` | Vue 组合式工具库 |
| `dayjs` | `^1.x` | 日期处理 |
| `echarts` | `^5.x` | 图表库 |
| `lodash-es` | `^4.x` | 工具函数库（ES Module 版） |

### 5.3 Pinia 实例创建
```ts
// src/stores/index.ts
import { createPinia } from 'pinia'
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'

const pinia = createPinia()
pinia.use(piniaPluginPersistedstate)

export default pinia
```

### 5.4 Axios 实例创建
```ts
// src/utils/request.ts
import axios from 'axios'
import type { AxiosInstance, InternalAxiosRequestConfig, AxiosResponse } from 'axios'

const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL as string,
  timeout: 15000,
  headers: { 'Content-Type': 'application/json' }
})

// 请求拦截器（后续任务完善）
service.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    // 注入 Token
    return config
  },
  (error) => Promise.reject(error)
)

// 响应拦截器（后续任务完善）
service.interceptors.response.use(
  (response: AxiosResponse) => response.data,
  (error) => Promise.reject(error)
)

export default service
```

### 5.5 关键配置项
| 配置项 | 作用 | 注意事项 |
|--------|------|----------|
| `pinia-plugin-persistedstate` | Pinia 状态持久化插件 | 可选，需额外安装 |
| `axios.baseURL` | API 请求基础路径 | 从 `import.meta.env.VITE_API_BASE_URL` 读取 |
| `axios.timeout: 15000` | 请求超时 15 秒 | 与后端接口约定一致 |
| `lodash-es` | 使用 ES Module 版 lodash | 支持 tree-shaking，不要安装 `lodash`（CJS 版） |
| `@types/nprogress` | NProgress 类型声明 | NProgress 自身无类型，需额外安装 |

### 5.6 环境适配
- 安装完成后执行 `pnpm install` 确认无版本冲突
- 确认 `package.json` 中 `dependencies` 和 `devDependencies` 分类正确
- `lodash-es` 替代 `lodash` 以支持 tree-shaking 减小产物体积
- `dayjs` 替代 `moment` 以减小产物体积（dayjs ~2KB vs moment ~70KB）

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | package.json | 状态管理与请求层依赖 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 依赖安装成功且版本号正确（pinia 2.x / axios 1.x / @vueuse/core 10.x） | `pnpm install` 无报错，`pnpm ls` 确认版本 |
| 2 | Pinia 实例创建并注册持久化插件 | `src/stores/index.ts` 中 `createPinia()` 正常 |
| 3 | Axios 实例创建并配置 baseURL 和 timeout | `src/utils/request.ts` 中实例配置正确 |
| 4 | `@types/nprogress` 和 `@types/lodash-es` 类型声明安装 | TypeScript 编译无类型缺失报错 |

## 八、易错警示

> ⚠️ 使用 `lodash-es` 而非 `lodash`（ES Module 版本支持 tree-shaking）

> ⚠️ 使用 `dayjs` 而非 `moment`（体积小 70 倍）

> ⚠️ `@types/nprogress` 必须安装为 devDependency，NProgress 自身无类型声明

> ⚠️ Axios 的 `baseURL` 必须从 `import.meta.env.VITE_API_BASE_URL` 读取，不要硬编码

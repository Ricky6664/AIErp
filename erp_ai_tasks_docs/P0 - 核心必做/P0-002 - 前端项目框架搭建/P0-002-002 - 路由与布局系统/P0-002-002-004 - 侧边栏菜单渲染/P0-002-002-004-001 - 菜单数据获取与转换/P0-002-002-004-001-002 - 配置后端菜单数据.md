# P0-002-002-004-001-002 配置后端菜单数据

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-002-004-001-002 |
| 任务名称 | 配置后端菜单数据 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 配置/基础设施 |

## 二、任务目标

实现菜单数据转换管道：将后端getInfo返回的菜单树转换为前端路由组件可直接使用的格式、处理菜单图标字段映射(Element Plus图标名/SVG名/外部URL)、处理菜单路径补全(相对路径→绝对路径)、缓存转换结果避免重复计算

## 三、前置依赖

### 3.1 前置任务

- P0-002-002-004-001 菜单数据获取与转换（父任务）
- P0-002-002-004-001-001 定义前端路由配置（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node 18+ / pnpm 9.x / Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：前端菜单数据转换管道
> - 技术栈：Vue 3.5.x + Vue Router 4.x + TypeScript 5.x + Element Plus 2.8.x
> - 路由机制：后端菜单数据经转换管道处理后供侧边栏组件和动态路由使用

### 5.1 配置结构

**文件：`erp-web/src/utils/menuPipeline.ts` — 菜单转换管道**

```typescript
import type { MenuItem } from '@/api/types/menu'

/** 图标类型判断 */
export function isExternalIcon(icon: string): boolean {
  return /^(https?:\/\/|\/\/)/.test(icon)
}

export function isSvgIcon(icon: string): boolean {
  return icon.startsWith('svg:')
}

/** 路径补全：确保路径以/开头 */
export function normalizePath(path: string, parentPath?: string): string {
  if (path.startsWith('/')) return path
  if (parentPath) {
    const base = parentPath.endsWith('/') ? parentPath : parentPath + '/'
    return base + path
  }
  return '/' + path
}

/** 菜单数据转换管道 */
export function processMenuData(menus: MenuItem[]): MenuItem[] {
  return menus.map(menu => {
    const processed: MenuItem = { ...menu }

    // 1. 路径补全
    processed.path = normalizePath(menu.path)

    // 2. 图标字段标准化
    if (menu.icon) {
      if (isExternalIcon(menu.icon)) {
        processed.iconType = 'external'
      } else if (isSvgIcon(menu.icon)) {
        processed.iconType = 'svg'
        processed.icon = menu.icon.replace('svg:', '')
      } else {
        processed.iconType = 'element'  // Element Plus图标
      }
    }

    // 3. 递归处理子菜单
    if (menu.children?.length) {
      processed.children = processMenuData(menu.children)
    }

    return processed
  })
}

/** 缓存转换结果 */
let cachedMenus: MenuItem[] | null = null
let cachedKey: string = ''

export function getCachedMenus(menus: MenuItem[]): MenuItem[] {
  const key = JSON.stringify(menus)
  if (key === cachedKey && cachedMenus) return cachedMenus
  cachedMenus = processMenuData(menus)
  cachedKey = key
  return cachedMenus
}
```

### 5.2 关键配置项

| 函数 | 用途 |
|------|------|
| isExternalIcon | 判断图标是否为外部URL(http/https开头) |
| isSvgIcon | 判断图标是否为SVG(svg:前缀) |
| normalizePath | 路径补全，确保绝对路径 |
| processMenuData | 完整转换管道：路径+图标+递归子菜单 |
| getCachedMenus | 带缓存的转换，相同输入不重复计算 |

### 5.3 与路由守卫的集成

1. permissionStore.generateRoutes()中调用getCachedMenus获取处理后的菜单数据
2. 处理后的数据同时传给transformMenuToRoutes(生成路由)和侧边栏组件(渲染菜单)
3. 缓存机制确保同一次登录只转换一次，减少计算开销

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | `erp-web/src/utils/menuPipeline.ts` | 菜单数据转换管道函数 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | isExternalIcon正确识别http/https URL | 传入各类型图标名验证返回值 |
| 2 | normalizePath将相对路径补全为绝对路径 | 传入'order/list'和'/sale'验证输出 |
| 3 | processMenuData正确处理3级嵌套菜单 | 传入嵌套数据验证每级都被处理 |
| 4 | getCachedMenus相同输入返回缓存引用 | 两次调用验证返回值===同一引用 |
| 5 | pnpm dev编译无TypeScript类型错误 | 终端日志无error输出 |

## 八、易错警示

> ⚠️ 缓存使用JSON.stringify比较，大数据量时可能性能不佳，必要时改用WeakMap或版本号

> ⚠️ normalizePath处理parentPath时注意末尾斜杠拼接，避免出现//双斜杠

> ⚠️ processMenuData使用展开运算符{...menu}创建浅拷贝，不要修改原始菜单对象

> ⚠️ 退出登录时要清除cachedMenus缓存，避免下次登录使用旧数据

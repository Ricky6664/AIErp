# P0-002-002-002-001-002 实现布局状态管理

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-002-002-001-002 |
| 任务名称 | 实现布局状态管理 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

创建布局状态Pinia Store src/stores/modules/layout.ts：管理侧边栏折叠状态isCollapsed(boolean)、全屏状态isFullscreen(boolean)、提供toggleCollapse/toggleFullscreen actions、isCollapsed持久化到localStorage

## 三、前置依赖

### 3.1 前置任务

- P0-002-002-002-001 AdminLayout布局组件（父任务）
- P0-002-002-002-001-001 实现布局容器结构（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node 18+ / pnpm 9.x / Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：前端布局状态管理
> - 技术栈：Vue 3.5.x + Vue Router 4.x + TypeScript 5.x + Element Plus 2.8.x + Pinia 2.x
> - 脚本规范：`<script setup lang="ts">`，禁止any类型
> - 布局结构：el-container嵌套，侧边栏可折叠，顶部栏固定，内容区自适应

### 5.1 文件结构与组件设计

文件路径：`erp-web/src/stores/modules/layout.ts`

```typescript
import { defineStore } from 'pinia'
import { ref, watch } from 'vue'

const COLLAPSED_KEY = 'erp_layout_collapsed'

interface LayoutState {
  isCollapsed: boolean
  isFullscreen: boolean
}

export const useLayoutStore = defineStore('layout', () => {
  // 从localStorage恢复折叠状态
  const savedCollapsed = localStorage.getItem(COLLAPSED_KEY)
  const isCollapsed = ref<boolean>(savedCollapsed === 'true')
  const isFullscreen = ref<boolean>(!!document.fullscreenElement)

  // 折叠状态持久化
  watch(isCollapsed, (val) => {
    localStorage.setItem(COLLAPSED_KEY, String(val))
  })

  function toggleCollapse() {
    isCollapsed.value = !isCollapsed.value
  }

  async function toggleFullscreen() {
    if (!document.fullscreenElement) {
      await document.documentElement.requestFullscreen()
      isFullscreen.value = true
    } else {
      await document.exitFullscreen()
      isFullscreen.value = false
    }
  }

  // 监听fullscreenchange事件同步状态
  function initFullscreenListener() {
    document.addEventListener('fullscreenchange', () => {
      isFullscreen.value = !!document.fullscreenElement
    })
  }

  return {
    isCollapsed,
    isFullscreen,
    toggleCollapse,
    toggleFullscreen,
    initFullscreenListener
  }
})
```

### 5.2 核心实现逻辑

1. **isCollapsed持久化**：通过watch监听变化写入localStorage，初始化时从localStorage读取
2. **toggleFullscreen**：使用Fullscreen API，通过document.fullscreenElement判断当前状态
3. **fullscreenchange监听**：浏览器F11也会触发全屏变化，需监听事件同步状态
4. **Setup Store语法**：使用`defineStore('layout', () => { ... })`函数式写法

### 5.3 交互逻辑

1. Navbar点击折叠按钮 → layoutStore.toggleCollapse() → isCollapsed变化 → aside宽度动画
2. Navbar点击全屏按钮 → layoutStore.toggleFullscreen() → Fullscreen API → isFullscreen更新
3. 刷新页面 → 从localStorage读取isCollapsed → 恢复折叠状态 → 避免闪烁

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | `erp-web/src/stores/modules/layout.ts` | 布局状态Pinia Store |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | isCollapsed初始值从localStorage读取 | 折叠后刷新页面验证状态保持 |
| 2 | toggleCollapse切换isCollapsed值 | DevTools Pinia面板查看状态变化 |
| 3 | toggleFullscreen调用Fullscreen API | 点击全屏按钮浏览器进入/退出全屏 |
| 4 | isFullscreen与document.fullscreenElement同步 | F11切换后Pinia状态正确更新 |
| 5 | localStorage中erp_layout_collapsed值与状态一致 | DevTools Application → LocalStorage检查 |

## 八、易错警示

> ⚠️ isCollapsed状态要持久化到localStorage，刷新后保持，避免布局闪烁

> ⚠️ 全屏切换要用document.fullscreenElement判断当前状态，不能仅依赖isFullscreen变量

> ⚠️ watch持久化不要在setup顶层直接调用localStorage.setItem，要在回调函数中延迟写入

> ⚠️ Fullscreen API的requestFullscreen/exitFullscreen返回Promise，需要async/await处理

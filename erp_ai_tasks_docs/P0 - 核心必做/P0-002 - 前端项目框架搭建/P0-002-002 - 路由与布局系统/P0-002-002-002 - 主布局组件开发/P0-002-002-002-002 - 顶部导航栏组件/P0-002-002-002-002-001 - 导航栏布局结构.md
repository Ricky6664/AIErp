# P0-002-002-002-002-001 导航栏布局结构

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-002-002-002-001 |
| 任务名称 | 导航栏布局结构 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现Navbar.vue顶部导航栏布局结构：左侧区域(折叠按钮+面包屑)、右侧区域(全局搜索图标+消息铃铛+用户头像下拉+全屏切换按钮)，使用flex布局space-between分配左右区域

## 三、前置依赖

### 3.1 前置任务

- P0-002-002-002-002 顶部导航栏组件（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node 18+ / pnpm 9.x / Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：前端导航栏组件开发
> - 技术栈：Vue 3.5.x + Vue Router 4.x + TypeScript 5.x + Element Plus 2.8.x + Pinia 2.x
> - 脚本规范：`<script setup lang="ts">`，禁止any类型
> - 布局结构：el-container嵌套，侧边栏可折叠，顶部栏固定，内容区自适应

### 5.1 文件结构与组件设计

文件路径：`erp-web/src/layouts/components/Navbar.vue`

```vue
<template>
  <div class="navbar">
    <!-- 左侧区域 -->
    <div class="navbar__left">
      <!-- 折叠/展开按钮 -->
      <el-icon
        class="navbar__collapse-btn"
        :size="20"
        @click="layoutStore.toggleCollapse()"
      >
        <Fold v-if="!layoutStore.isCollapsed" />
        <Expand v-else />
      </el-icon>
      <!-- 面包屑导航 -->
      <Breadcrumb />
    </div>

    <!-- 右侧区域 -->
    <div class="navbar__right">
      <!-- 全局搜索 -->
      <el-tooltip content="搜索" placement="bottom">
        <el-icon class="navbar__action-btn" :size="18">
          <Search />
        </el-icon>
      </el-tooltip>

      <!-- 消息铃铛 -->
      <el-badge :value="unreadCount" :hidden="!unreadCount" class="navbar__badge">
        <el-icon class="navbar__action-btn" :size="18">
          <Bell />
        </el-icon>
      </el-badge>

      <!-- 用户头像+下拉菜单 -->
      <el-dropdown trigger="click" @command="handleUserCommand">
        <div class="navbar__user">
          <el-avatar :size="32" :src="userStore.avatar">
            {{ userStore.nickname?.charAt(0) }}
          </el-avatar>
          <span class="navbar__username">{{ userStore.nickname }}</span>
        </div>
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item command="profile">个人中心</el-dropdown-item>
            <el-dropdown-item divided command="logout">退出登录</el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>

      <!-- 全屏切换 -->
      <el-tooltip content="全屏" placement="bottom">
        <el-icon
          class="navbar__action-btn"
          :size="18"
          @click="layoutStore.toggleFullscreen()"
        >
          <FullScreen />
        </el-icon>
      </el-tooltip>
    </div>
  </div>
</template>

<script setup lang="ts">
import { Fold, Expand, Search, Bell, FullScreen } from '@element-plus/icons-vue'
import Breadcrumb from './Breadcrumb.vue'
import { useLayoutStore } from '@/stores/modules/layout'
import { useUserStore } from '@/stores/modules/user'

defineOptions({ name: 'Navbar' })

const layoutStore = useLayoutStore()
const userStore = useUserStore()

const unreadCount = ref<number>(0)  // 未读消息数(后续对接消息API)

function handleUserCommand(command: string) {
  if (command === 'logout') {
    userStore.logout()
  } else if (command === 'profile') {
    router.push('/profile')
  }
}
</script>

<style lang="scss" scoped>
.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 100%;

  &__left {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  &__right {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  &__collapse-btn {
    cursor: pointer;
    transition: color 0.2s;
    &:hover { color: var(--el-color-primary); }
  }

  &__action-btn {
    cursor: pointer;
    transition: color 0.2s;
    &:hover { color: var(--el-color-primary); }
  }

  &__user {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
  }
}
</style>
```

### 5.2 核心实现逻辑

1. **flex布局space-between**：左右区域自动分配空间
2. **折叠按钮图标切换**：isCollapsed为true时显示Expand，否则显示Fold
3. **el-dropdown trigger="click"**：用户头像点击弹出下拉菜单
4. **el-badge**：消息铃铛上显示未读消息数红点
5. **defineOptions({ name: 'Navbar' })**：设置组件名称，keep-alive依赖

### 5.3 交互逻辑

1. 点击折叠按钮 → layoutStore.toggleCollapse() → 侧边栏宽度动画
2. 点击用户头像 → el-dropdown展开菜单 → 选择"个人中心"/"退出登录"
3. 点击全屏按钮 → layoutStore.toggleFullscreen() → Fullscreen API
4. 面包屑自动跟随路由变化（Breadcrumb组件内部computed实现）

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | `erp-web/src/layouts/components/Navbar.vue` | 顶部导航栏布局结构组件 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 左侧(折叠按钮+面包屑)和右侧(搜索+消息+用户+全屏)渲染正确 | 浏览器目视检查各区域 |
| 2 | flex布局space-between正确分配左右区域 | DevTools Elements检查flex属性 |
| 3 | 折叠按钮图标随isCollapsed切换(Fold↔Expand) | 点击折叠按钮观察图标变化 |
| 4 | 用户头像el-dropdown trigger为click | 点击头像弹出下拉菜单 |
| 5 | pnpm dev编译无TypeScript错误 | 终端日志无error输出 |

## 八、易错警示

> ⚠️ 用户头像使用el-dropdown时trigger必须设为click，hover在移动端体验差

> ⚠️ 折叠按钮图标Fold/Expand要从@element-plus/icons-vue正确导入

> ⚠️ el-badge的:hidden="!unreadCount"在无未读时隐藏红点，避免显示数字0

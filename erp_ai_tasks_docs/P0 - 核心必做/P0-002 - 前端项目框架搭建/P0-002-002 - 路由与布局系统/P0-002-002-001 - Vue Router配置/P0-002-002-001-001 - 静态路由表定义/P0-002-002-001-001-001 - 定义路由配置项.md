# P0-002-002-001-001-001 定义路由配置项

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-002-002-001-001-001 |
| 任务名称 | 定义路由配置项 |
| 所属模块 | P0-002 |
| 优先级 | P0 |
| 任务类型 | 配置/基础设施 |

## 二、任务目标

定义静态路由表src/router/routes/static.ts：常量LOGIN_ROUTE(/login)、ROOT_ROUTE(/重定向到/home)、HOME_ROUTE(/home使用AdminLayout)、ERROR_404(/404)、ERROR_403(/403)、NO_PERMISSION(/no-permission)、这些路由应用初始化时直接注册、无需鉴权

## 三、前置依赖

### 3.1 前置任务

- P0-002-002-001-001 静态路由表定义（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（Node 18+ / pnpm 9.x / Vite 6.x）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格

> **📦 模块上下文**
> - 任务类型：前端路由配置
> - 技术栈：Vue 3.5.x + Vue Router 4.x + TypeScript 5.x + Element Plus 2.8.x
> - 路由机制：静态路由(login/404/403/首页)在应用初始化时直接注册，不依赖后端菜单数据

### 5.1 配置结构

文件路径：`erp-web/src/router/modules/static.ts`

```typescript
import type { RouteRecordRaw } from 'vue-router'

// 登录页 - 无需鉴权，独立布局
export const LOGIN_ROUTE: RouteRecordRaw = {
  path: '/login',
  name: 'Login',
  component: () => import('@/views/login/index.vue'),
  meta: { title: '登录', titleI18n: 'login.title', hideMenu: true, hideTab: true }
}

// 根路由 - 重定向到首页
export const ROOT_ROUTE: RouteRecordRaw = {
  path: '/',
  name: 'Root',
  redirect: '/home',
  meta: { hideMenu: true, hideTab: true }
}

// 首页 - 使用AdminLayout布局
export const HOME_ROUTE: RouteRecordRaw = {
  path: '/home',
  name: 'Home',
  component: () => import('@/layouts/AppLayout.vue'),
  children: [
    {
      path: '',
      name: 'HomePage',
      component: () => import('@/views/home/index.vue'),
      meta: { title: '首页', titleI18n: 'home.title', icon: 'HomeFilled', affix: true, keepAlive: true }
    }
  ]
}

// 404页面
export const ERROR_404: RouteRecordRaw = {
  path: '/404',
  name: 'Error404',
  component: () => import('@/views/error/404.vue'),
  meta: { title: '404', hideMenu: true, hideTab: true }
}

// 403页面
export const ERROR_403: RouteRecordRaw = {
  path: '/403',
  name: 'Error403',
  component: () => import('@/views/error/403.vue'),
  meta: { title: '403', hideMenu: true, hideTab: true }
}

// 无权限页面
export const NO_PERMISSION: RouteRecordRaw = {
  path: '/no-permission',
  name: 'NoPermission',
  component: () => import('@/views/error/no-permission.vue'),
  meta: { title: '无权限', hideMenu: true, hideTab: true }
}

// 静态路由集合 - 导出供router/index.ts使用
export const staticRoutes: RouteRecordRaw[] = [
  LOGIN_ROUTE,
  ROOT_ROUTE,
  HOME_ROUTE,
  ERROR_404,
  ERROR_403,
  NO_PERMISSION
]
```

### 5.2 关键配置项

| 路由常量 | path | 用途 | meta关键配置 |
|---------|------|------|-------------|
| LOGIN_ROUTE | /login | 登录页面 | hideMenu:true, hideTab:true |
| ROOT_ROUTE | / | 根路径重定向 | redirect:'/home' |
| HOME_ROUTE | /home | 首页(布局容器) | affix:true, keepAlive:true |
| ERROR_404 | /404 | 404错误页 | hideMenu:true, hideTab:true |
| ERROR_403 | /403 | 403无权限页 | hideMenu:true, hideTab:true |
| NO_PERMISSION | /no-permission | 无权限提示页 | hideMenu:true, hideTab:true |

### 5.3 与路由守卫的集成

1. 静态路由在`router/index.ts`中通过`createRouter({ routes: staticRoutes })`直接注册
2. 路由守卫`guards.ts`中通过`whiteList = ['/login', '/404', '/403', '/no-permission']`判断是否为白名单路由
3. HOME_ROUTE的meta.affix=true表示首页标签固定，不可被关闭
4. 所有静态路由的meta.hideMenu/hideTab控制菜单和标签页是否显示

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | `erp-web/src/router/modules/static.ts` | 静态路由配置常量 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | static.ts导出6个路由常量(LOGIN/ROOT/HOME/404/403/NO_PERMISSION) | DevTools Console执行`import`检查导出 |
| 2 | 每个路由name全局唯一无重复 | TypeScript编译无报错+人工检查 |
| 3 | HOME_ROUTE使用懒加载`()=>import('@/layouts/AppLayout.vue')` | 检查component属性为函数 |
| 4 | 所有静态路由meta.hideMenu/hideTab正确设置 | 浏览器访问各路由确认无侧边栏/标签页 |
| 5 | pnpm dev启动无编译错误 | 终端日志无error输出 |

## 八、易错警示

> ⚠️ 路由name必须全局唯一，重复会导致导航异常和keep-alive缓存错乱

> ⚠️ component必须使用`() => import()`懒加载形式，不能直接import后赋值，否则Vite无法代码分割

> ⚠️ LOGIN/404/403/NO_PERMISSION路由必须静态注册，不能依赖动态加载，否则未登录时无法访问

> ⚠️ ROOT_ROUTE不要设置component，只需redirect，否则会导致嵌套路由渲染异常

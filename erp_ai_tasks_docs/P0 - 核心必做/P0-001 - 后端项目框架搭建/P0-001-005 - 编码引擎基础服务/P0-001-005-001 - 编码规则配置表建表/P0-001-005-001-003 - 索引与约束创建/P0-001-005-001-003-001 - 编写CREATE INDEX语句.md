# P0-001-005-001-003-001 编写CREATE INDEX语句

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-001-005-001-003-001 |
| 任务名称 | 编写CREATE INDEX语句 |
| 所属模块 | P0-001 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

编写sys_code_rule主表DDL与sys_code_rule_segment从表DDL：主表字段id(BIGINT主键)、rule_code(VARCHAR 50唯一)、rule_name、module_code、description、separator(VARCHAR 10段分隔符)、current_value(BIGINT当前序列)、is_enabled(TINYINT)、create_by/create_time/update_by/update_time/is_deleted/tenant_id；从表含rule_id外键、segment_type(1固定2日期3序列4变量)、segment_order、segment_value、segment_length、segment_format；建立唯一索引uk_rule_module(rule_code, tenant_id)、idx_segment_order(rule_id, segment_order)

## 三、前置依赖

### 3.1 前置任务

- P0-001-005-001-003 索引与约束创建（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**
> - 任务类型：框架基础设施开发
> - 技术栈：JDK 17, Spring Boot 3.4.x, MyBatis-Plus 3.5.5, PostgreSQL 15+, Redis 7.x
>
> 💡 开发本任务时，请严格遵循上述技术约束。

### 5.1 详细规格
编写sys_code_rule主表DDL与sys_code_rule_segment从表DDL：主表字段id(BIGINT主键)、rule_code(VARCHAR 50唯一)、rule_name、module_code、description、separator(VARCHAR 10段分隔符)、current_value(BIGINT当前序列)、is_enabled(TINYINT)、create_by/create_time/update_by/update_time/is_deleted/tenant_id；从表含rule_id外键、segment_type(1固定2日期3序列4变量)、segment_order、segment_value、segment_length、segment_format；建立唯一索引uk_rule_module(rule_code, tenant_id)、idx_segment_order(rule_id, segment_order)

### 5.2 实现要求
1. 严格按照上述规格实现，不要遗漏任何配置项、字段或方法
2. 代码结构清晰、命名规范、注释完整
3. 使用项目统一的基础类(BaseEntity/RT/PageResult等)
4. 配置类必须加@Component/@Configuration注解

### 5.3 验证要点
1. 编译无错误、无警告
2. 单元测试覆盖核心逻辑
3. 与上下游模块集成正确
4. 符合全局代码规范与架构约束

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/resources/db/migration/V1__create_sys_code_rule.sql | 编码规则DDL |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | DDL语法正确，执行无报错 | 执行SQL验证 |
| 2 | 索引与约束正确创建 | 检查information_schema |
| 3 | 字段类型与规范一致 | 检查表结构 |

## 八、易错警示

> ⚠️ DDL要用Flyway/Liquibase管理，不要手动执行

> ⚠️ 唯一索引必须含tenant_id，避免多租户冲突

> ⚠️ BIGINT主键用雪花算法，不要用AUTO_INCREMENT

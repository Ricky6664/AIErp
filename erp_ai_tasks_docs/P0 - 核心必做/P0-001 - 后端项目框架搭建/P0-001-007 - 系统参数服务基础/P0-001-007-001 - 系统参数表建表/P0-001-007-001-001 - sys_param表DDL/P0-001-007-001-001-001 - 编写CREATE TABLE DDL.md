# P0-001-007-001-001-001 编写CREATE TABLE DDL

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-001-007-001-001-001 |
| 任务名称 | 编写CREATE TABLE DDL |
| 所属模块 | P0-001 |
| 优先级 | P0 |
| 任务类型 | DDL/数据库建表 |

## 二、任务目标

编写sys_param表DDL：字段id(BIGINT主键)/param_category(VARCHAR 50分类)/param_key(VARCHAR 100键)/param_value(TEXT值)/value_type(1字符串2数字3布尔4JSON5日期)/description/sort_order/is_system(TINYINT系统参数不可删)/公共字段；唯一索引uk_category_key(param_category, param_key, tenant_id)、idx_category(param_category)

## 三、前置依赖

### 3.1 前置任务

- P0-001-007-001-001 sys_param表DDL（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用


| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-数据库规范 | 数据库设计与DDL规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写规范约束 |
| 全局规范-AI开发执行手册 | AI开发执行流程与规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P0-001模块开发指南）
> - 本模块技术栈：JDK 17, Spring Boot 3.4.x, MyBatis-Plus 3.5.5, PostgreSQL 15+, Redis 7.x
> - 本模块关键约束：严格遵循模块开发指南中的规范要求
>
> 💡 开发本任务时，请严格遵循上述技术约束。
### 5.1 表结构设计
按设计文档定义表名、字段、数据类型。
### 5.2 必含公共字段
id(BIGINT PK), tenant_id, created_by, created_time, updated_by, updated_time, is_deleted, version
### 5.3 索引设计
主键索引 + 业务唯一索引(含is_deleted) + 外键关联索引 + 常用查询字段索引
### 5.4 Flyway迁移脚本
命名: V{日期}{序号}__{描述}.sql，含回滚注释

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/resources/db/migration/V3__create_sys_param.sql | 系统参数DDL |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | DDL语法正确，执行无报错 | 执行SQL验证 |
| 2 | 索引与约束正确创建 | 检查information_schema |
| 3 | 字段类型与规范一致 | 检查表结构 |

## 八、易错警示

> ⚠️ DDL要用Flyway/Liquibase管理，不要手动执行

> ⚠️ 唯一索引必须含tenant_id，避免多租户冲突

> ⚠️ BIGINT主键用雪花算法，不要用AUTO_INCREMENT

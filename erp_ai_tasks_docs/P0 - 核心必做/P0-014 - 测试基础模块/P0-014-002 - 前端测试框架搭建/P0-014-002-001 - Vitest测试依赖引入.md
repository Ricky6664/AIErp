# P0-014-002-001 Vitest测试依赖引入

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-014-002-001 |
| 任务名称 | Vitest测试依赖引入 |
| 所属模块 | P0-014 |
| 优先级 | P0 |
| 任务类型 | 配置/基础设施 |

## 二、任务目标


在package.json中引入前端测试框架依赖：vitest 1.x(测试核心)、@vue/test-utils 2.x(Vue组件测试)、happy-dom(DOM环境)、@vitest/coverage-v8(覆盖率)、msw 2.x(API Mock服务)，并配置vitest.config.ts

## 三、前置依赖

### 3.1 前置任务

- 无特定前置任务依赖

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用




| 规范文档名 | 引用原因 |
|-----------|--------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量标准 |
| 全局规范-测试开发规范 | 测试开发与验证规范约束 |

## 五、详细开发规格




> **本任务模块上下文**（来源：P0-014模块开发指南）
> - 后端测试框架：JUnit 5.10.x + Mockito 5.x + AssertJ 3.x + Testcontainers 1.19.x + H2 2.x + JaCoCo 0.8.x
> - 前端测试框架：Vitest 1.x + @vue/test-utils 2.x + happy-dom + @vitest/coverage-v8 + MSW 2.x
> - 覆盖率门禁：后端Service>=80%/Controller>=70%/Mapper>=60%，前端组件>=70%
> - 测试命名：类名<被测类>Test，方法名should_<行为>_when_<条件>，结构given/when/then三段式

### 5.1 package.json依赖声明
- vitest ^1.6.0, @vue/test-utils ^2.4.6, happy-dom ^14.12.0
- @vitest/coverage-v8 ^1.6.0, msw ^2.3.0

### 5.2 vitest.config.ts配置
- environment: happy-dom, globals: true
- coverage.provider: v8, reporter: text-summary + lcov
- coverage.thresholds: lines=70, branches=60

### 5.3 验证
- pnpm install, pnpm vitest run

## 六、交付物清单



| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | package.json | Vitest/@vue/test-utils/happy-dom/@vitest/coverage-v8/MSW依赖 |
| 2 | vitest.config.ts | Vitest配置文件 |
| 3 | src/test-setup.ts | 测试全局setup文件 |

## 七、验收标准



| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | vitest + @vue/test-utils安装成功 | pnpm list验证 |
| 2 | happy-dom + coverage-v8 + msw安装成功 | pnpm list验证 |
| 3 | vitest.config.ts配置正确 | pnpm vitest run成功 |
| 4 | 覆盖率阈值生效 | 报告验证 |

## 八、易错警示


> ⚠️ 测试依赖版本必须与模块开发指南锁定版本一致，版本不一致可能导致API不兼容

> ⚠️ spring-boot-starter-test已包含JUnit 5，但需要排除junit-vintage-engine避免JUnit 4/5混用

> ⚠️ h2database版本必须与PostgreSQL兼容模式配置匹配，高版本H2有breaking changes

> ⚠️ testcontainers版本必须与Docker Engine版本兼容

> ⚠️ JaCoCo插件版本必须与JDK版本匹配，JDK 17+需要JaCoCo 0.8.7+

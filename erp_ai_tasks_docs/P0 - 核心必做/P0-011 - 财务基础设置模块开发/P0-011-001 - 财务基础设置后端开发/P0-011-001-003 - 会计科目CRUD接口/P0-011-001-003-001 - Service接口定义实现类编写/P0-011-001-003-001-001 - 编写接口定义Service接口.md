# P0-011-001-003-001-001 编写接口定义Service接口

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-011-001-003-001-001 |
| 任务名称 | 编写接口定义Service接口 |
| 所属模块 | P0-011 |
| 优先级 | P0 |
| 任务类型 | Service服务层 |

## 二、任务目标

定义会计科目Service接口(IAccountService)，继承IServiceX<AccountEntity>，声明CRUD方法(create/update/delete/getById/pageList)及业务方法签名，入参使用AccountCreateDTO/UpdateDTO/QueryDTO，返回AccountVO，写操作方法标注@Transactional注解

## 三、前置依赖

### 3.1 前置任务

- P0-011-001-003-001 Service接口定义+实现类编写（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量要求 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：finance模块开发指南）
> - 本模块涉及数据表：fin_currency_rate, fin_bank_account, fin_account, fin_voucher_word, fin_accounting_period
> - 本模块涉及API：/api/finance/currency-rate, /api/finance/bank-account, /api/finance/account, /api/finance/voucher-word, /api/finance/accounting-period, /api/finance/workbench
> - 本模块业务规则：币种编码全局唯一; 汇率日期不得晚于当前日期+30天; 银行账号全局唯一; 会计科目编码按级次规则(4-2-2-2)全局唯一; 末级科目标记新增修改时自动计算; 会计期间不可重叠; 科目余额表凭证审核时自动更新
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 接口定义
- 接口名：IAccountService，继承 IServiceX<AccountEntity>
- 包路径：com.erp.finance.service

### 5.2 方法签名
```java
public interface IAccountService extends IServiceX<AccountEntity> {
    AccountVO create(@Valid AccountCreateDTO dto);
    AccountVO update(Long id, @Valid AccountUpdateDTO dto);
    void delete(Long id);
    AccountVO getById(Long id);
    PageResult<AccountVO> pageList(AccountQueryDTO query);
}
```

### 5.3 入参DTO/返回VO
- CreateDTO字段：accountCode(科目编码-按级次4-2-2-2), accountName(科目名称), parentId(上级科目ID), level(科目级次), accountType(科目类型), category(科目类别:资产/负债/所有者权益/成本/损益), balanceDirection(余额方向:借/贷), isLeaf(是否末级-自动计算), isCash(是否现金科目), isBank(是否银行科目), isForeignCurrency(是否外币核算), isAuxiliary(是否辅助核算)
- UpdateDTO：与CreateDTO相同字段 + @NotNull Long id
- QueryDTO：pageNum, pageSize, 
- VO：accountCode(科目编码-按级次4-2-2-2), accountName(科目名称), parentId(上级科目ID), level(科目级次), accountType(科目类型), category(科目类别:资产/负债/所有者权益/成本/损益), balanceDirection(余额方向:借/贷), isLeaf(是否末级-自动计算), isCash(是否现金科目), isBank(是否银行科目), isForeignCurrency(是否外币核算), isAuxiliary(是否辅助核算) + id + createdTime + updatedTime

### 5.4 注解要求
- 写操作方法(create/update/delete)标注 @Transactional(rollbackFor = Exception.class)
- 接口方法JavaDoc注释完整(@param, @return)


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-finance-module/src/main/java/com/erp/finance/service/IAccountService.java | Service接口 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | IAccountService接口继承IServiceX | 代码审查 |
| 2 | CRUD方法签名完整(create/update/delete/getById/pageList) | 代码审查 |
| 3 | 入参DTO使用@Valid注解 | 代码审查 |
| 4 | 写操作方法标注@Transactional | 代码审查 |
| 5 | JavaDoc注释完整(@param/@return) | 代码审查 |

## 八、易错警示

> ⚠️ 事务注解@Transactional仅加在写操作方法(create/update/delete)，读操作不加

> ⚠️ DTO字段校验注解(@NotNull/@NotBlank/@Size)与ServiceImpl校验保持一致

> ⚠️ 接口方法JavaDoc注释必须完整，包含@param和@return说明

> ⚠️ 方法返回类型统一使用VO对象，不直接返回Entity

> ⚠️ 异常统一抛出BusinessException，不要抛原生RuntimeException

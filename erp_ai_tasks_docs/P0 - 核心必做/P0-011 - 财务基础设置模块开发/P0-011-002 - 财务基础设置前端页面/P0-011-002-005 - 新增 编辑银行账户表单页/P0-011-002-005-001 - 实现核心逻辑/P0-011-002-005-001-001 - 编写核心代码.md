# P0-011-002-005-001-001 编写核心代码

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-011-002-005-001-001 |
| 任务名称 | 编写核心代码 |
| 所属模块 | P0-011 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

开发银行账户P07单一表单页核心代码，实现页面搭建、API数据绑定、筛选交互、操作列逻辑，调用/api/finance/bank-account接口，确保数据渲染正确与交互流畅

## 三、前置依赖

### 3.1 前置任务

- P0-011-002-005-001 实现核心逻辑（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量要求 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：finance模块开发指南）
> - 本模块涉及数据表：fin_currency_rate, fin_bank_account, fin_account, fin_voucher_word, fin_accounting_period
> - 本模块涉及API：/api/finance/currency-rate, /api/finance/bank-account, /api/finance/account, /api/finance/voucher-word, /api/finance/accounting-period, /api/finance/workbench
> - 本模块业务规则：币种编码全局唯一; 汇率日期不得晚于当前日期+30天; 银行账号全局唯一; 会计科目编码按级次规则(4-2-2-2)全局唯一; 末级科目标记新增修改时自动计算; 会计期间不可重叠; 科目余额表凭证审核时自动更新
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 页面搭建
基于P07单一表单页搭建，弹窗形式
- el-form组件构建表单，el-dialog弹窗容器

### 5.2 表单字段
账户名称(必填), 银行账号(必填唯一), 开户银行(必填), 开户支行(文本), 币种(下拉选择), 账户类型(下拉)

### 5.3 交互逻辑
- 新增模式：表单为空，提交 POST /api/finance/bank-account
- 编辑模式：GET /api/finance/bank-account/{id} 获取详情回显，提交 PUT /api/finance/bank-account/{id}
- 表单校验：必填项、格式校验、唯一性异步校验(编码字段)
- 提交成功后关闭弹窗并刷新父页面列表

### 5.4 组件集成
- 组件：el-form, el-dialog, 人员选择器/部门选择器/树形选择器(按需)
- 提交按钮loading状态防重复提交


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-ui/src/views/finance/bankaccount/index.vue | 页面组件 |
| 2 | erp-ui/src/api/finance/bankaccount.ts | API调用层 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | 页面路由注册正确，可正常访问 | 浏览器访问 |
| 2 | API数据绑定正确，列表/表单渲染正常 | 浏览器验证 |
| 3 | 筛选/搜索交互正常，防抖生效 | Network面板 |
| 4 | 操作列(编辑/删除/状态切换)正常 | 浏览器验证 |
| 5 | 与后端接口联调通过 | 接口联调 |

## 八、易错警示

> ⚠️ 前端API调用统一使用封装的request工具，不要直接用axios

> ⚠️ 表单校验规则与后端保持一致，前端校验是用户体验，后端校验是安全保证

> ⚠️ Vxe Table大数据量(>100行)要开启虚拟滚动(scroll-y)

> ⚠️ 组件命名遵循PascalCase，文件名与组件名一致

> ⚠️ 注意Hydration错误：SSR模式下不要在setup中使用document/window

> ⚠️ 防抖(debounce)用在搜索输入，节流(throttle)用在按钮提交

# P0-012-002-003-001-001 编写核心代码

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-012-002-003-001-001 |
| 任务名称 | 编写核心代码 |
| 所属模块 | P0-012 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

开发员工中心P06主从表单页核心代码，实现主从表单页面搭建、主表与从表字段数据绑定、表单校验、主从联动提交逻辑，调用/api/hrm/employee接口，确保主从数据一致性与交互流畅

## 三、前置依赖

### 3.1 前置任务

- P0-012-002-003-001 实现核心逻辑（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量要求 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-页面类型定义-展示类P01至P05 | 页面类型与组件规范约束 |
## 五、详细开发规格
> **📦 本任务模块上下文**（来源：hrm模块开发指南）
> - 本模块涉及数据表：hrm_employee, hrm_employee_archive, hrm_recruitment, hrm_attendance, hrm_salary, hrm_salary_detail
> - 本模块涉及API：/api/hrm/employee, /api/hrm/employee-archive, /api/hrm/recruitment, /api/hrm/attendance, /api/hrm/salary, /api/hrm/workbench
> - 本模块业务规则：工号全局唯一(事务内生成); 部门岗位关联校验(岗位需属于所选部门); 薪资精度4位小数误差不得超0.01; 薪资发放状态单向流转(待发放->已发放); 考勤日期员工唯一; 身份证号列表脱敏(保留前6后4)
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 页面搭建
基于P06主从表单页搭建，弹窗形式(大)
- 主表区域：el-form组件构建主表表单
- 从表区域：Vxe Table展示从表明单(行增删改)
- 主从通过关联字段(主表ID)联动

### 5.2 表单字段
主表: 工号(自动生成只读), 姓名(必填), 性别(单选), 身份证号(18位格式校验), 手机号(11位校验), 邮箱(格式校验), 部门(树形选择), 岗位(下拉联动部门), 入职日期(日期选择), 员工状态(下拉); 从表-员工档案: 学历, 专业, 毕业院校, 紧急联系人, 银行卡号

### 5.3 交互逻辑
- 新增模式：主从表单均为空，提交 POST /api/hrm/employee(主从一起保存)
- 编辑模式：GET /api/hrm/employee/{id} 获取详情(含从表数据)回显，提交 PUT /api/hrm/employee/{id}
- 主表校验：必填项、格式校验、关联性校验
- 从表校验：行必填项、金额精度、至少一行数据
- 从表行操作：新增行、删除行、编辑单元格
- 提交成功后关闭弹窗并刷新父页面列表

### 5.4 组件集成
- 组件：el-form, el-dialog, VxeTable(从表), 人员选择器/部门选择器(按需)
- 主从事务一致性：后端@Transactional保证主从表原子提交
- 提交按钮loading状态防重复提交

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-ui/src/views/hrm/employeecenter/index.vue | 页面组件 |
| 2 | erp-ui/src/api/hrm/employeecenter.ts | API调用层 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | 页面路由注册正确，可正常访问 | 浏览器访问 |
| 2 | API数据绑定正确，列表/表单渲染正常 | 浏览器验证 |
| 3 | 筛选/搜索交互正常，防抖生效 | Network面板 |
| 4 | 操作列(编辑/删除/状态切换)正常 | 浏览器验证 |
| 5 | 与后端接口联调通过 | 接口联调 |

## 八、易错警示

> ⚠️ 前端API调用统一使用封装的request工具，不要直接用axios

> ⚠️ 表单校验规则与后端保持一致，前端校验是用户体验，后端校验是安全保证

> ⚠️ Vxe Table大数据量(>100行)要开启虚拟滚动(scroll-y)

> ⚠️ 组件命名遵循PascalCase，文件名与组件名一致

> ⚠️ 注意Hydration错误：SSR模式下不要在setup中使用document/window

> ⚠️ 防抖(debounce)用在搜索输入，节流(throttle)用在按钮提交

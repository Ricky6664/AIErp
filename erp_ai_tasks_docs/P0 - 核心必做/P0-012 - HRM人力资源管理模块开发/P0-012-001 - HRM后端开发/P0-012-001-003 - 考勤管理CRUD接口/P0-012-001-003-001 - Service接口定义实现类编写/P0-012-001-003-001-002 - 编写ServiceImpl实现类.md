# P0-012-001-003-001-002 编写ServiceImpl实现类

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-012-001-003-001-002 |
| 任务名称 | 编写ServiceImpl实现类 |
| 所属模块 | P0-012 |
| 优先级 | P0 |
| 任务类型 | Service服务层 |

## 二、任务目标

实现考勤管理ServiceImpl，继承ServiceImpl，完成考勤管理CRUD业务逻辑，包含唯一性校验/状态流转校验/关联完整性校验，使用@Transactional(rollbackFor=Exception.class)管理事务，异常统一抛出BusinessException

## 三、前置依赖

### 3.1 前置任务

- P0-012-001-003-001 Service接口定义+实现类编写（父任务）
- P0-012-001-003-001-001 编写接口定义Service接口（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量要求 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：hrm模块开发指南）
> - 本模块涉及数据表：hrm_employee, hrm_employee_archive, hrm_recruitment, hrm_attendance, hrm_salary, hrm_salary_detail
> - 本模块涉及API：/api/hrm/employee, /api/hrm/employee-archive, /api/hrm/recruitment, /api/hrm/attendance, /api/hrm/salary, /api/hrm/workbench
> - 本模块业务规则：工号全局唯一(事务内生成); 部门岗位关联校验(岗位需属于所选部门); 薪资精度4位小数误差不得超0.01; 薪资发放状态单向流转(待发放->已发放); 考勤日期员工唯一; 身份证号列表脱敏(保留前6后4)
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 实现类定义
- 类名：AttendanceServiceImpl extends ServiceImpl<AttendanceMapper, AttendanceEntity> implements IAttendanceService
- @Service + @Transactional 类级别注解
- 注入：AttendanceMapper, CodeGeneratorService(编码引擎)

### 5.2 核心业务逻辑
- **create**：编码引擎生成编码 → 唯一性校验(编码+is_deleted=false) → DTO转Entity → mapper.insert → Entity转VO返回
- **update**：存在性校验 → 唯一性校验(排除自身) → DTO转Entity → mapper.update → VO返回
- **delete**：存在性校验 → 关联数据校验(如有) → mapper.deleteById(软删除)
- **pageList**：构建QueryWrapper(筛选+分页) → mapper.selectPage → VO列表返回

### 5.3 业务校验
- 唯一性校验：同一员工同一考勤日期唯一性校验
- 状态流转校验：按业务规则校验状态变更合法性
- 关联数据完整性：删除前校验关联表数据

### 5.4 事务与异常
- @Transactional(rollbackFor = Exception.class) 加在写操作方法
- 校验失败统一抛出 BusinessException(ErrorCode.XXX, "具体错误信息")
- 关键操作记录操作日志(OperateLogService)


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-hrm-module/src/main/java/com/erp/hrm/service/impl/AttendanceServiceImpl.java | Service实现类 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | AttendanceServiceImpl继承ServiceImpl并实现接口 | 代码审查 |
| 2 | create方法：编码生成+唯一性校验+持久化 | 单元测试 |
| 3 | update方法：存在性+唯一性校验+更新 | 单元测试 |
| 4 | delete方法：存在性+关联校验+软删除 | 单元测试 |
| 5 | 异常统一抛出BusinessException | 代码审查 |
| 6 | @Transactional(rollbackFor=Exception.class)正确 | 代码审查 |

## 八、易错警示

> ⚠️ @Transactional(rollbackFor=Exception.class)要加在ServiceImpl方法上，不要加在private方法上

> ⚠️ 唯一性校验SQL必须包含 is_deleted = false 条件，避免与已删除数据冲突

> ⚠️ 编码生成调用要在事务内完成，且保证幂等性

> ⚠️ 关联数据校验(删除前)要查询关联表，有数据时抛BusinessException并返回关联清单

> ⚠️ 多租户隔离(tenant_id)由框架自动注入，不要在SQL中硬编码

> ⚠️ 避免大事务：事务方法内不要调用外部HTTP接口或耗时操作

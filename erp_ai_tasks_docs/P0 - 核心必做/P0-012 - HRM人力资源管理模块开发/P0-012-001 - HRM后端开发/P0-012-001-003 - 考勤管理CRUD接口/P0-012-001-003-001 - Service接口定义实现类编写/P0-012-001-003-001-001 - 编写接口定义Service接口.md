# P0-012-001-003-001-001 编写接口定义Service接口

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-012-001-003-001-001 |
| 任务名称 | 编写接口定义Service接口 |
| 所属模块 | P0-012 |
| 优先级 | P0 |
| 任务类型 | Service服务层 |

## 二、任务目标

定义考勤管理Service接口(IAttendanceService)，继承IServiceX<AttendanceEntity>，声明CRUD方法(create/update/delete/getById/pageList)及业务方法签名，入参使用AttendanceCreateDTO/UpdateDTO/QueryDTO，返回AttendanceVO，写操作方法标注@Transactional注解

## 三、前置依赖

### 3.1 前置任务

- P0-012-001-003-001 Service接口定义+实现类编写（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量要求 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：hrm模块开发指南）
> - 本模块涉及数据表：hrm_employee, hrm_employee_archive, hrm_recruitment, hrm_attendance, hrm_salary, hrm_salary_detail
> - 本模块涉及API：/api/hrm/employee, /api/hrm/employee-archive, /api/hrm/recruitment, /api/hrm/attendance, /api/hrm/salary, /api/hrm/workbench
> - 本模块业务规则：工号全局唯一(事务内生成); 部门岗位关联校验(岗位需属于所选部门); 薪资精度4位小数误差不得超0.01; 薪资发放状态单向流转(待发放->已发放); 考勤日期员工唯一; 身份证号列表脱敏(保留前6后4)
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 接口定义
- 接口名：IAttendanceService，继承 IServiceX<AttendanceEntity>
- 包路径：com.erp.hrm.service

### 5.2 方法签名
```java
public interface IAttendanceService extends IServiceX<AttendanceEntity> {
    AttendanceVO create(@Valid AttendanceCreateDTO dto);
    AttendanceVO update(Long id, @Valid AttendanceUpdateDTO dto);
    void delete(Long id);
    AttendanceVO getById(Long id);
    PageResult<AttendanceVO> pageList(AttendanceQueryDTO query);
}
```

### 5.3 入参DTO/返回VO
- CreateDTO字段：employeeId(员工ID-关联hrm_employee), attendanceDate(考勤日期), checkInTime(上班打卡时间), checkOutTime(下班打卡时间), workHours(工作时长小时DECIMAL(4,1)), attendanceType(考勤类型:正常/迟到/早退/缺勤/加班), overtimeHours(加班时长小时)
- UpdateDTO：与CreateDTO相同字段 + @NotNull Long id
- QueryDTO：pageNum, pageSize, 
- VO：employeeId(员工ID-关联hrm_employee), attendanceDate(考勤日期), checkInTime(上班打卡时间), checkOutTime(下班打卡时间), workHours(工作时长小时DECIMAL(4,1)), attendanceType(考勤类型:正常/迟到/早退/缺勤/加班), overtimeHours(加班时长小时) + id + createdTime + updatedTime

### 5.4 注解要求
- 写操作方法(create/update/delete)标注 @Transactional(rollbackFor = Exception.class)
- 接口方法JavaDoc注释完整(@param, @return)


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-hrm-module/src/main/java/com/erp/hrm/service/IAttendanceService.java | Service接口 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | IAttendanceService接口继承IServiceX | 代码审查 |
| 2 | CRUD方法签名完整(create/update/delete/getById/pageList) | 代码审查 |
| 3 | 入参DTO使用@Valid注解 | 代码审查 |
| 4 | 写操作方法标注@Transactional | 代码审查 |
| 5 | JavaDoc注释完整(@param/@return) | 代码审查 |

## 八、易错警示

> ⚠️ 事务注解@Transactional仅加在写操作方法(create/update/delete)，读操作不加

> ⚠️ DTO字段校验注解(@NotNull/@NotBlank/@Size)与ServiceImpl校验保持一致

> ⚠️ 接口方法JavaDoc注释必须完整，包含@param和@return说明

> ⚠️ 方法返回类型统一使用VO对象，不直接返回Entity

> ⚠️ 异常统一抛出BusinessException，不要抛原生RuntimeException

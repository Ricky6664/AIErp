# P0-012-001-005-001-001 编写核心代码

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-012-001-005-001-001 |
| 任务名称 | 编写核心代码 |
| 所属模块 | P0-012 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

实现HRM工作台聚合Service核心业务逻辑，包含聚合数据组装/业务规则处理/缓存策略(@Cacheable TTL=5min)，使用@Transactional管理事务，确保数据准确性与查询性能

## 三、前置依赖

### 3.1 前置任务

- P0-012-001-005-001 实现核心逻辑（父任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用
| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与编写要求 |
| 全局规范-AI开发执行手册 | AI开发执行流程与质量要求 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |

## 五、详细开发规格
> **📦 本任务模块上下文**（来源：hrm模块开发指南）
> - 本模块涉及数据表：hrm_employee, hrm_employee_archive, hrm_recruitment, hrm_attendance, hrm_salary, hrm_salary_detail
> - 本模块涉及API：/api/hrm/employee, /api/hrm/employee-archive, /api/hrm/recruitment, /api/hrm/attendance, /api/hrm/salary, /api/hrm/workbench
> - 本模块业务规则：工号全局唯一(事务内生成); 部门岗位关联校验(岗位需属于所选部门); 薪资精度4位小数误差不得超0.01; 薪资发放状态单向流转(待发放->已发放); 考勤日期员工唯一; 身份证号列表脱敏(保留前6后4)
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 核心功能
- 实现HRM工作台聚合Service聚合查询方法
- 组装KPI卡片数据（核心统计指标）
- 组装图表趋势数据（按时间维度分组统计）
- 使用@Cacheable缓存策略(TTL=5min)

### 5.2 代码结构
```java
@Service
public class HrmWorkbenchAggregateServiceImpl {
    @Cacheable(value = "workbench", key = "'hrm:' + #tenantId", unless = "#result == null")
    public HrmWorkbenchAggregateVO getWorkbenchData(Long tenantId) {
        // 1. 查询KPI聚合数据
        // 2. 查询图表趋势数据
        // 3. 组装返回VO
    }
}
```

### 5.3 关键逻辑
- 多表聚合SQL查询，使用数据视图(DV_xxx)
- 缓存策略：@Cacheable + @CacheEvict(数据变更时清除)
- 多租户隔离：tenant_id参数过滤

### 5.4 验证
- 聚合数据与手动SQL查询结果一致
- 缓存命中率≥90%
- 异常场景有明确错误提示


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-hrm-module/src/main/java/com/erp/hrm/service/impl/HrmWorkbenchAggregateServiceImpl.java | 核心业务实现 |
| 2 | erp-hrm-module/src/main/java/com/erp/hrm/vo/HrmWorkbenchAggregateVO.java | 聚合数据VO |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|--------|
| 1 | 聚合数据计算正确 | 接口测试 |
| 2 | @Cacheable缓存策略生效 | 缓存监控 |
| 3 | 多租户隔离正确 | 接口测试 |
| 4 | 查询性能达标 | 性能测试 |
| 5 | 异常降级处理正确 | 模拟异常 |

## 八、易错警示

> ⚠️ @Cacheable的key表达式要包含tenantId，避免跨租户数据混淆

> ⚠️ 缓存TTL设置要合理(5min)，过短影响性能，过长数据不及时

> ⚠️ @CacheEvict要在数据变更操作后触发，避免缓存脏数据

> ⚠️ 聚合SQL查询要设置超时时间(30s)，避免慢查询阻塞

> ⚠️ 异常场景要有降级方案，不能因为聚合查询失败导致整个工作台崩溃

# P0-009-001-002-001-002 编写ServiceImpl实现类

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-009-001-002-001-002 |
| 任务名称 | 编写ServiceImpl实现类 |
| 所属模块 | P0-009 |
| 优先级 | P0 |
| 任务类型 | Service服务层 |

## 二、任务目标

实现SrmTagDefinitionServiceImpl，继承ServiceImpl<SrmTagDefinitionMapper, SrmTagDefinition>，调用SrmTagDefinitionMapper完成srm_tag_definition表数据操作，包含业务校验（标签名称全局唯一；标签颜色格式#RRGGBB...）、事务管理、统一BusinessException异常处理


## 三、前置依赖

### 3.1 前置任务

- P0-009-001-002-001 Service接口定义+实现类编写（父任务）
- P0-009-001-002-001-001 编写接口定义Service接口（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-后端代码规范 | 后端代码开发规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档格式与内容规范 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行标准 |


## 五、详细开发规格

> **📦 本任务模块上下文**（来源：P0-009模块开发指南）
> - 本模块涉及数据表：srm_supplier, srm_supplier_class, srm_tag_definition, srm_supplier_contact, srm_supplier_comm, srm_supplier_address, srm_supplier_tag_rel, srm_supplier_attachment, srm_supplier_evaluation, srm_supplier_finance
> - 本模块涉及API：/api/srm/supplier-class, /api/srm/tag-definition, /api/srm/supplier, /api/srm/supplier-contact, /api/srm/supplier-address
> - 本模块业务规则：功能点优先级：设计文档11.4节定义的12个功能点（工作台/供应商中心/供应商分类/标签定义/供应商基本信息/供应商联系人/供应商地址/供应商标签/供应商画像/供应商附件/供应商评价/供应商财务配置）按顺序开发；V11.0补充的供应商品类、供应商准入作为扩展项在基线完成后开发。; 供应商名称唯一性：供应商名称在同一公司主体下必须唯一，新增/修改时需进行唯一性校验，校验失败时返回明确的业务异常提示。
>
> 💡 开发本任务时，请结合上述模块上下文理解业务场景和数据关系。

### 5.1 SrmTagDefinitionServiceImpl类结构
- 继承 `ServiceImpl<SrmTagDefinitionMapper, SrmTagDefinition>`
- 实现 `SrmTagDefinitionService` 接口
- 注入依赖：SrmTagDefinitionMapper、操作日志Service、编码生成Service（如需要）
### 5.2 核心业务逻辑
- **分页查询**：构建QueryWrapper，支持条件过滤+排序+分页
- **新增**：DTO→Entity转换 → 业务校验 → 编码生成(如需) → 数据库插入 → 操作日志
- **修改**：存在性校验 → DTO→Entity转换 → 业务校验 → 数据库更新 → 操作日志
- **删除**：存在性校验 → 关联引用检查 → 逻辑删除(is_deleted=1) → 操作日志
### 5.3 业务校验规则
- 标签名称全局唯一；标签颜色格式#RRGGBB
### 5.4 异常处理
- 所有业务异常统一抛出 `BusinessException(ErrorCode.XXX, "描述")`
- 校验失败场景：数据不存在、名称重复、关联引用存在、状态流转非法等


## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | src/main/java/com/erp/module/srm/service/impl/SrmTagDefinitionServiceImpl.java | SrmTagDefinitionServiceImpl实现类 |
| 2 | src/main/java/com/erp/module/srm/mapper/SrmTagDefinitionMapper.java | Mapper接口（如未创建） |


## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | CRUD方法实现完整，调用SrmTagDefinitionMapper正确 | 代码审查 |
| 2 | 业务校验规则实现正确 | 代码审查+单元测试 |
| 3 | 事务管理正确（@Transactional配置正确） | 代码审查 |
| 4 | 异常统一抛出BusinessException | 代码审查 |
| 5 | 操作日志记录正确 | 代码审查+接口测试 |


## 八、易错警示

> ⚠️ 事务边界要准确，避免大事务；只在写操作方法上加@Transactional，读操作使用readOnly=true

> ⚠️ 异常统一抛出BusinessException，不要直接throw RuntimeException

> ⚠️ 编码生成调用要幂等，避免重试时生成重复编码

> ⚠️ 删除操作前必须检查关联引用，防止产生孤立数据

> ⚠️ LambdaQueryWrapper中的字段引用必须使用方法引用(::)，不要用字符串硬编码

> ⚠️ 多租户隔离(tenant_id)由框架自动填充，不要在业务代码中手动设置


# P0-005-013-008-002-001-002 实现预览+打印执行

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-005-013-008-002-001-002 |
| 任务名称 | 实现预览+打印执行 |
| 所属模块 | P0-005 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

开发通用业务功能组件模块的「实现预览+打印执行」功能，实现组件核心逻辑、数据处理、配置驱动渲染、事件通信，确保组件可复用性和TypeScript类型安全

## 三、前置依赖

### 3.1 前置任务

- P0-005-013-008-002-001 实现打印逻辑（父任务）
- P0-005-013-008-002-001-001 实现模板渲染（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行规范要求 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P0-005模块开发指南）
> - 本模块涉及数据表：sys_dict_type, sys_dict_data, sys_param
> - 本模块业务规则：所有20种录入组件必须支持v-model受控模式、:disabled只读模式、字段配置集成（field_type映射+校验规则绑定+联动规则绑定）。

### 5.1 组件Props
- modelValue: any - v-model双向绑定值
- fieldConfig: FieldConfig - 字段配置（类型/校验/联动规则）
- disabled: boolean - 只读/禁用模式
- placeholder: string - 占位提示

### 5.2 组件Events
- update:modelValue - 值变更事件（v-model双向绑定）
- change - 值确认变更事件
- focus/blur - 焦点事件

### 5.3 组件Slots
- prefix - 前缀插槽
- suffix - 后缀插槽
- default - 默认内容插槽

### 5.4 核心逻辑
1. setup()中声明响应式状态(ref/reactive)和计算属性(computed)
2. 实现v-model双向绑定（emit('update:modelValue', newValue)）
3. 集成字段配置（field_type映射组件类型+校验规则绑定+联动规则绑定）
4. 错误处理：try-catch包裹异步操作，错误信息统一展示

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-ui/src/components/common-business/ | Vue组件(.vue) |
| 2 | erp-ui/src/components/common-business/__tests__/ | 组件单元测试(vitest) |
| 3 | erp-ui/src/types/common-business.d.ts | TypeScript类型定义 |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 组件Props/Events/Slots接口定义完整且TypeScript类型正确 | TypeScript编译 |
| 2 | v-model双向绑定正确工作，emit('update:modelValue')事件正确触发 | 手动验证 |
| 3 | :disabled只读模式下所有交互正确禁用 | 手动验证 |
| 4 | 字段配置集成正确（field_type映射+校验规则+联动规则） | 多场景测试 |
| 5 | 组件单元测试覆盖率>80%（vitest + @vue/test-utils） | vitest覆盖率报告 |

## 八、易错警示

> ⚠️ v-model双向绑定必须通过emit('update:modelValue')触发，不能直接修改props

> ⚠️ 组件defineProps使用TypeScript类型定义(defineProps<{...}>())而非运行时声明

> ⚠️ 表格大数据量(>1000行)使用vxe-table虚拟滚动，避免DOM节点过多导致卡顿
# P0-005-012-001-001-002 客户数据加载与搜索

## 一、任务标识

| 属性 | 值 |
|------|-----|
| 任务编号 | P0-005-012-001-001-002 |
| 任务名称 | 客户数据加载与搜索 |
| 所属模块 | P0-005 |
| 优先级 | P0 |
| 任务类型 | 综合开发任务 |

## 二、任务目标

开发ERP业务档案选择组件模块的前端功能，使用Vue 3 Composition API + TypeScript，实现组件渲染、数据绑定、交互逻辑、状态管理(Pinia)，确保与后端API接口联调通过

## 三、前置依赖

### 3.1 前置任务

- P0-005-012-001-001 客户选择器弹窗（父任务）
- P0-005-012-001-001-001 弹窗容器与列表布局（前序兄弟任务）

### 3.2 前置资源

- 项目代码仓库已就绪
- 开发环境已搭建（JDK17 + Maven + PostgreSQL + Redis）

## 四、关联规范引用

| 规范文档名 | 引用原因 |
|-----------|---------|
| 全局规范-项目架构与开发约束 | 项目架构、技术约束与任务依赖关系 |
| 全局规范-前端代码规范 | 前端代码开发规范约束 |
| 全局规范-末端任务文档编写规范 | 末端任务文档结构与内容规范要求 |
| 全局规范-AI开发执行手册 | AI辅助开发流程与执行规范要求 |

## 五、详细开发规格


> **📦 本任务模块上下文**（来源：P0-005模块开发指南）
> - 本模块涉及数据表：sys_dict_type, sys_dict_data, sys_param
> - 本模块业务规则：所有20种录入组件必须支持v-model受控模式、:disabled只读模式、字段配置集成（field_type映射+校验规则绑定+联动规则绑定）。

### 5.1 组件Props
- modelValue: any - v-model双向绑定值
- fieldConfig: FieldConfig - 字段配置（类型/校验/联动规则）
- disabled: boolean - 只读/禁用模式
- placeholder: string - 占位提示

### 5.2 组件Events
- update:modelValue - 值变更事件（v-model双向绑定）
- change - 值确认变更事件
- focus/blur - 焦点事件

### 5.3 组件Slots
- prefix - 前缀插槽
- suffix - 后缀插槽
- default - 默认内容插槽

### 5.4 核心逻辑
1. setup()中声明响应式状态(ref/reactive)和计算属性(computed)
2. 实现v-model双向绑定（emit('update:modelValue', newValue)）
3. 集成字段配置（field_type映射组件类型+校验规则绑定+联动规则绑定）
4. 错误处理：try-catch包裹异步操作，错误信息统一展示

## 六、交付物清单

| 序号 | 文件路径 | 说明 |
|:---:|---------|------|
| 1 | erp-ui/src/views/ERP业务档案选择组件/ | Vue页面组件 |
| 2 | erp-ui/src/api/ERP业务档案选择组件/ | API调用层(TypeScript) |
| 3 | erp-ui/src/composables/ERP业务档案选择组件/ | 组合式函数(如适用) |

## 七、验收标准

| 序号 | 检查项 | 验证方法 |
|:---:|--------|---------|
| 1 | 页面路由注册正确，URL可正常访问 | 浏览器验证 |
| 2 | 组件渲染无报错，无Hydration错误 | 浏览器Console检查 |
| 3 | 数据绑定正确（列表数据/表单数据双向绑定） | 手动验证 |
| 4 | 交互逻辑（搜索/分页/CRUD/弹窗）正常工作 | 手动操作验证 |
| 5 | 与后端API接口联调通过，数据正确展示 | 接口联调 |

## 八、易错警示

> ⚠️ 表格大数据量(>1000行)使用vxe-table虚拟滚动，避免DOM节点过多导致卡顿

> ⚠️ 表单校验规则必须与后端@Valid校验保持一致，防止前端通过但后端拒绝

> ⚠️ 组件defineProps使用TypeScript类型定义而非运行时声明，确保类型安全
